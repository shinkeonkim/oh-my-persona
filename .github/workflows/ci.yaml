name: ci
on:
  pull_request:
  push:
    branches: [main]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
        with: {python-version: "3.12"}
      - run: uv sync --extra dev
      - run: uv run persona validate
      - run: uv run persona chunk
      - run: uv run ruff check .
      - run: uv run pytest
      - run: docker build -t oh-my-persona:test .
