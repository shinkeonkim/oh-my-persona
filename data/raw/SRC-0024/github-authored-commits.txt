GitHub repository: https://github.com/shinkeonkim/anime-ontology
Author account: shinkeonkim

2026-07-04T14:59:21Z	08ffd1daf62d45de08ad95b3990715fc5697e39e	https://github.com/shinkeonkim/anime-ontology/commit/08ffd1daf62d45de08ad95b3990715fc5697e39e	rival_of 관계 추가 + 배치 실행기 견고화 (720화 실사용 중 발견) - 720화 전체를 OpenAI(gpt-4o-mini)로 처리하던 중 23화에서 모델이 스키마에 없는 predicate "rival_of"(라이벌 관계)를 만들어내 검증에 계속 실패했고, 그 예외가 잡히지 않아 배치 전체가 죽는 문제 발견 - core.ttl/extraction/schema.py/prompts.py/builder.py에 rival_of를 정식 관계로 추가(적대보다는 경쟁하며 성장하는 관계, 예: 나루토-사스케). 프롬프트에도 목록에 없는 predicate를 지어내지 말라는 지시를 추가 - pipeline.run_episodes를 견고하게 수정: 한 화 처리가 실패해도 예외를 잡아 경고만 남기고 다음 화로 계속 진행. 실패한 화 목록은 마지막에 stderr로 요약. 모든 화가 실패한 경우에만 에러를 던짐 - scripts/run_episode.py가 직접 루프를 돌던 것을 run_episodes() 호출로 단순화해 진행/실패 처리 로직 중복을 없앰 - (참고) 이 커밋 전에 이미 1~22화는 정상 처리/저장되어 있었음(캐시로 재호출 없이 이어서 진행 가능)
2026-07-04T14:05:22Z	a69836c41eb6aa8866961f85212ab45af046d479	https://github.com/shinkeonkim/anime-ontology/commit/a69836c41eb6aa8866961f85212ab45af046d479	720화로 데이터 확장 대응: SRT 파서, 합본 화 처리, UTF-16 자막 디코딩 수정 - srt_parser.py: 일부 화(43개)가 SAMI 대신 SubRip(.srt)으로 되어 있어 파서 추가, registry에 등록 - discovery.py: "221~222.smi"처럼 두 화가 합본으로 발매된 파일(27건)이 뒷 번호로만 잡히고 앞 번호가 통째로 누락되던 문제 수정. 합본이면 같은 파일을 두 화 번호 모두에 매핑(내용을 화 단위로 나눌 근거가 없어 중복 매핑이 최선) - subtitles/encoding.py: smi/srt 파서가 공유하는 디코딩 헬퍼로 분리하면서 UTF-16 BOM 파일 처리 추가. 720화 중 정확히 1개 화(667화)가 UTF-16으로 저장돼 있었는데, UTF-8로 디코딩해도 예외 없이 널 바이트 섞인 문자열이 되어 조용히 큐 0개로 파싱되던 실제 버그를 발견해 수정 - 위 세 가지를 고친 뒤 720화 전체 자막이 오류/이상 없이 파싱됨을 확인 (총 276,734개 큐). 기존 1~5화 결과는 md5 동일(회귀 없음)
2026-07-04T13:27:55Z	8303987139ee98ddf6ea9a5119e3352a58ce4116	https://github.com/shinkeonkim/anime-ontology/commit/8303987139ee98ddf6ea9a5119e3352a58ce4116	파이프라인에 자막 없는 영상 폴백(STT+장면분석) 통합 - pipeline._collect_cues: 자막이 있으면 자막을, 없으면 ffmpeg 오디오 추출 -> STT(TranscriptionProviderProxy)로 대사를 확보. SCENE_ANALYSIS_ENABLED=true면 장면 분석(화면 텍스트 OCR) 결과를 "(화면 텍스트) ..." 형태로 대사 큐에 병합. 장면 분석 실패는 경고만 남기고 STT 결과로 계속 진행(하드 실패 아님) - run_episode/run_episodes에 transcription provider 주입 파라미터 추가(llm과 동일 패턴) - config.py에 env_flag 헬퍼 추가 (SCENE_ANALYSIS_ENABLED 등 불리언 환경변수용) - 검증: 자막 없는 가짜 영상으로 전체 파이프라인(오디오 추출->STT->캐시->추출-> 온톨로지 저장)이 끝까지 동작하고 재실행 시 캐시로 즉시 완료됨을 확인. 기존 자막 기반 나루토 1~5화 결과는 md5 동일(회귀 없음) - README/architecture 문서에 설치 방법(optional extras)과 동작 방식 정리
2026-07-04T13:23:36Z	b76bbc3d51ba5b1408b314993da7f893c0cdf869	https://github.com/shinkeonkim/anime-ontology/commit/b76bbc3d51ba5b1408b314993da7f893c0cdf869	기본 장면 분석(장면 전환 감지 + 화면 텍스트 OCR) 구현 - scene_detection.py: OpenCV 없이 ffmpeg의 scene 필터(+showinfo)로 장면 전환 시점(ms)을 추출. 오디오 추출에도 쓰는 ffmpeg 하나로 해결해 의존성을 늘리지 않음 - ocr.py: 장면 전환 시점마다 ffmpeg로 프레임을 캡처해 pytesseract(한국어+영어)로 화면에 찍힌 텍스트를 읽음. tesseract/패키지 미설치 시 명확한 에러로 안내 - scene_analysis.py: 위 둘을 묶어 analyze_scenes()로 VisualCue 목록 생성, OCR 호출 비용을 고려해 화당 장면 수 상한(max_scenes)을 둠 - 실제 나루토 영상으로 장면 전환 감지(9건) 확인, OCR은 트루타입 폰트로 렌더링한 한국어/영어 텍스트에서 정확히 인식되는 것을 확인(애니메이션 타이틀 카드처럼 양식화된 폰트에서의 인식 한계는 OCR 기술 자체의 한계로 별개 사안)
2026-07-04T13:23:30Z	7212e266e87f25e070e151eef07eb01380b5ac24	https://github.com/shinkeonkim/anime-ontology/commit/7212e266e87f25e070e151eef07eb01380b5ac24	Update README with project details and image Added project overview and image to README.
2026-07-04T13:16:56Z	dd0ef87f7635db860400fa876ad074017fa2c23d	https://github.com/shinkeonkim/anime-ontology/commit/dd0ef87f7635db860400fa876ad074017fa2c23d	자막 없는 영상용 STT Provider 추상화 구현 (local_whisper/openai) - LLMProviderProxy와 동일한 패턴으로 transcription 패키지 구성: TranscriptionProvider 인터페이스 + LocalWhisperProvider(faster-whisper, API 키 불필요, 기본값)/OpenAIWhisperProvider + TranscriptionProviderProxy (.env의 TRANSCRIPTION_PROVIDER/TRANSCRIPTION_FALLBACK_PROVIDERS로 교체) - audio.py: ffmpeg로 영상에서 16kHz mono wav 오디오 추출 - STT 결과는 SubtitleCue로 반환해 자막 파싱 경로와 동일한 인터페이스로 추출 파이프라인에 들어가도록 함 - cache.py: 화 단위 STT 결과 캐시 (재실행 시 오디오 추출/인식 재실행 방지) - 무거운 의존성(faster-whisper 등)은 기본 설치에 넣지 않고 optional-dependencies(stt/vision extras)로 분리 - 나루토 1화 앞부분 오디오로 local_whisper 실제 동작 확인 (모델 다운로드, 타임스탬프 포함 SubtitleCue 변환까지 정상. 인식 품질은 모델 한계로 편차 있음)
2026-07-04T13:02:31Z	9107e014f527f99f19b8439cde24a6f5ec1c6ce9	https://github.com/shinkeonkim/anime-ontology/commit/9107e014f527f99f19b8439cde24a6f5ec1c6ce9	파일럿 품질 이슈 수정: 타입 충돌 재사용 + 존칭 정규화 + 사건 프롬프트 강화 - builder.py: 이름이 이미 다른 클래스로 존재하는데 다른 클래스로 또 요청되면 (LLM이 관계 predicate/대상을 잘못 짝지은 경우) 잘못된 타입의 중복 엔티티를 만드는 대신 기존 엔티티를 재사용하도록 변경. "나루토"/"구미호"가 Location으로 잘못 생성되던 문제가 실제로 사라짐 - builder.py: 매칭 전용 정규화(_match_key)로 "~선생님/~님/~쨩/~군/~양/~씨" 같은 존칭 접미사를 뗀 뒤 비교. rdfs:label(표시용 이름)은 원문 그대로 유지 - extraction/prompts.py: 대표 이름은 성+이름으로, 존칭 붙은 형태는 별칭으로 넣도록 지시 추가. 사건(Event)은 서술적 요약이 아닌 구체적 사건만 넣도록 강화 - 나루토 1~5화를 --force로 재추출해 검증: 캐릭터 수 19->15로 감소(중복 제거 효과), "카카시 선생님"/"하타케 카카시"가 이번엔 처음부터 하나로 병합됨. 재실행 idempotency(md5 동일)도 재확인 - docs/pilot-review.md에 수정 전/후 비교와 남은 한계(첫 등장부터 잘못 분류되는 경우, 표기가 아예 다른 동의어)를 정리
2026-07-04T12:33:42Z	871e2f96df333006d018470d758994c5baa778de	https://github.com/shinkeonkim/anime-ontology/commit/871e2f96df333006d018470d758994c5baa778de	자연어 질의 웹 뷰(FastAPI) 추가 - webapp/app.py: NLQueryEngine을 감싸는 FastAPI 앱, POST /api/query - webapp/static/: 질문 입력, 답변, 생성된 Cypher(펼쳐보기), 결과 표, 탐색된 서브그래프 시각화를 보여주는 단일 페이지. 그래프는 외부 CDN 없이 순수 JS로 구현한 간단한 force-directed 레이아웃(드래그로 위치 조정 가능) - scripts/run_web.py CLI로 서버 실행 (python scripts/run_web.py) - 실제 나루토 데이터로 end-to-end 검증: API 응답 스키마 확인, 안전장치(정적 검사 + Neo4j 읽기 전용 트랜잭션) 둘 다 쓰기 쿼리를 정상적으로 차단하는 것을 확인 - README/architecture 문서에 사용법과 설계(이중 안전장치, 스키마 설명 재사용) 정리
2026-07-04T12:10:45Z	620a79a200d1025e57ceb4ade9f7af2fe665fa7b	https://github.com/shinkeonkim/anime-ontology/commit/620a79a200d1025e57ceb4ade9f7af2fe665fa7b	자연어 -> Cypher 질의 엔진 구현 (query 패키지) - schema_context.py: core.ttl에서 라벨/관계/프로퍼티 설명을 자동 생성해 LLM에게 전달 (스키마가 바뀌면 자동으로 같이 갱신됨) - cypher_generation.py: LLM Provider Proxy로 자연어->Cypher 생성. few-shot 예시를 넣어 로컬 소형 모델(ollama)에서도 문법 오류/모호한 이름 매칭 문제를 크게 줄임 - safety.py: 쓰기 절(CREATE/MERGE/DELETE 등) 차단 + LIMIT 강제 - executor.py: RoutingControl.READ로 실행(서버 차원에서도 쓰기 거부, 이중 방어), Node/Relationship/Path/스칼라를 표(rows)와 시각화용 서브그래프(nodes/edges)로 변환 - answer.py: 쿼리 결과(행 + 엔티티 설명)를 근거로 자연어 답변 합성 - engine.py: 생성->안전검사->실행 실패 시 오류를 LLM에 피드백해 최대 2회 재시도 - 버그 수정: Neo4j 내보내기 시 OWL 서브클래스 폐쇄(build_superclass_closure)를 반영하지 않아 ":Ability" 같은 상위 클래스로 조회가 안 되던 문제, 답변 합성이 노드 name만 보고 description을 못 봐서 "정보 없음"으로 답하던 문제를 실제 질의 테스트로 발견해 함께 수정
2026-07-04T12:04:10Z	27b05e60ae6fc65e6b0f6ae17e91b08b4f6f3fd7	https://github.com/shinkeonkim/anime-ontology/commit/27b05e60ae6fc65e6b0f6ae17e91b08b4f6f3fd7	Neo4j 연결설정/RDF-프로퍼티그래프 매핑을 공용 모듈로 분리 - neo4j_client.py: Neo4jSettings + 드라이버 컨텍스트 매니저 (export와 앞으로 추가할 자연어 쿼리 엔진이 함께 사용) - ontology/property_graph_mapping.py: RDF 클래스/속성 -> Neo4j 라벨/관계 타입 변환 규칙(local_name/to_label/to_relationship_type/DATATYPE_PROPERTY_FIELDS)을 export/neo4j_export.py에서 분리. 내보내기와 자연어->Cypher 스키마 설명이 같은 규칙을 참조하도록 해 둘이 어긋나는 것을 방지
2026-07-04T11:58:02Z	a6cab590a07cc5d0eaaeb27d9e3154b89cc14729	https://github.com/shinkeonkim/anime-ontology/commit/a6cab590a07cc5d0eaaeb27d9e3154b89cc14729	자막(.smi) 원본을 git 이력에서 완전히 제거하고 추적 대상에서 제외 - 저작권 있는 팬섭 자막 텍스트가 최초 커밋(원본은 041cdf7, 이번 히스토리 재작성 이후 4a177f2)부터 잘못 커밋되어 있었음. git filter-branch로 전체 히스토리에서 모든 *.smi를 제거하고(원격 저장소 없음 - 로컬 전용이라 안전), .gitignore에 *.smi/*.srt/*.ass/*.vtt를 추가해 앞으로도 추적되지 않게 함. 파일 자체는 디스크에는 그대로 유지(파이프라인 입력으로 계속 사용, git에만 안 올라감) - 자막을 백업/복원하는 과정에서 macOS(APFS)가 기존 mp4는 NFD, 복원된 smi는 NFC로 서로 다른 유니코드 정규화 형식을 쓰는 바람에 discovery.py의 stem 매칭이 깨지는 실제 버그를 발견. 같은 이름이라도 정규화 형식이 다르면 다른 문자열로 취급되어 영상/자막 페어링이 실패하고 에피소드 번호가 중복되는 문제였음 - discovery.py에서 stem을 NFC로 정규화한 뒤 비교하도록 수정해 근본 해결. 다른 도구/OS에서 온 파일이 섞여도(NFC/NFD 혼용) 안전하게 페어링됨
2026-07-04T11:26:30Z	581ba7b3a33c29f8c180cd2ad43c855f1d70940e	https://github.com/shinkeonkim/anime-ontology/commit/581ba7b3a33c29f8c180cd2ad43c855f1d70940e	나루토 1~5화 파일럿 실행 및 리뷰 - 로컬 Ollama(gemma4)로 1~5화 전체 파이프라인 실행, data/naruto/ontology/naruto.ttl에 431개 인스턴스 트리플(캐릭터19/장소10/조직5/기술14/선천능력5/물건8/유물5/사건14) 누적 - 화를 넘나드는 동일 인물 병합(나루토/사쿠라/사스케 등)과 idempotent 재실행을 확인 - 리뷰 중 발견한 이슈를 반영해 builder.py에 타입 충돌 경고 추가: 같은 이름이 이미 다른 클래스로 존재하는데 다른 클래스로 또 생성 요청되면 원인 파악용 경고를 남김 (LLM이 관계 predicate의 대상 타입을 잘못 짝지은 경우를 드러냄) - .gitignore에 .claude/(로컬 도구 상태) 추가 - docs/pilot-review.md에 정량 결과, 잘된 점, 품질 이슈(호칭 불일치로 인한 미병합, 타입 충돌), 다음 단계 제안을 정리
2026-07-04T11:17:04Z	bbed4f1f7c67c4470e6ab48de0aea2ad2d301d99	https://github.com/shinkeonkim/anime-ontology/commit/bbed4f1f7c67c4470e6ab48de0aea2ad2d301d99	Neo4j 내보내기 모듈 및 프로젝트 전용 Docker Neo4j 구성 추가 - export/neo4j_export.py: RDF 그래프를 라벨/관계로 변환해 URI를 유일 키로 MERGE. 재실행해도 노드/관계가 중복되지 않음 - ontology.strip_core_schema를 export가 재사용해, owl:Class 등 스키마 정의 트리플이 인스턴스 데이터처럼 Neo4j에 섞여 들어가는 문제를 방지 - env/require_env를 llm 전용 config에서 최상위 anime_ontology/config.py로 분리해 Neo4j 등 다른 기능도 같은 .env 로딩 로직을 재사용하도록 정리 (단일 책임) - docker-compose.yml: 호스트에 이미 떠 있는 다른 Neo4j와 충돌하지 않도록 7688/7475 포트로 이 프로젝트 전용 Neo4j 컨테이너 구성 - scripts/export_neo4j.py CLI 추가, 로컬 Docker Neo4j에 실제 내보내기 및 재실행 idempotency(노드 26/관계 39 유지)까지 검증 완료
2026-07-04T07:20:24Z	7a65534c78f2fdcfd300f119c92d9eef630d404a	https://github.com/shinkeonkim/anime-ontology/commit/7a65534c78f2fdcfd300f119c92d9eef630d404a	온톨로지 빌더 및 에피소드 파이프라인 오케스트레이션 구현 - builder.py: 추출 결과를 RDF 트리플로 변환. rdfs:label/aliasName 기반 이름 색인으로 기존 엔티티를 재사용해 화가 반복 처리돼도 URI가 중복 생성되지 않음. 능력(kind)/조직(is_clan)/물건(is_artifact) 플래그를 core.ttl의 하위 클래스로 매핑 - 사건(Event)은 화 번호+화 내 순서로 URI를 고정해 캐시 재사용 시 URI가 안정적으로 수렴하도록 함(재실행 안전성) - pipeline.py: 자막 파싱 -> (캐시 확인) 추출 -> 온톨로지 병합 -> 저장을 오케스트레이션, run_episodes로 여러 화 순차 처리 지원 - scripts/run_episode.py: --episode/--episodes 로 실행하는 CLI - 나루토 1화로 파이프라인 전체를 검증: 재실행 시 캐시로 동일한 ttl(md5 동일)이 나와 idempotent함을 확인
2026-07-04T07:11:04Z	ce1c85fea9134a6846c9ec2f4582ded9c7908eca	https://github.com/shinkeonkim/anime-ontology/commit/ce1c85fea9134a6846c9ec2f4582ded9c7908eca	개체/관계 추출 모듈 구현 - schema.py: LLM 추출 결과 pydantic 모델(캐릭터/장소/조직/능력/물건/사건/관계), core.ttl의 클래스·속성과 대응되도록 필드 설계 - prompts.py: 벤더 중립적인 추출 프롬프트(JSON 스키마 강제, 크레딧 문장 무시 지시) - extractor.py: 자막을 글자 수 기준 청크로 나눠 LLMProvider(Proxy)로 호출, 코드블록 제거 후 JSON 파싱 + pydantic 검증, 실패 시 재시도 - cache.py: 화 단위 추출 결과 JSON 캐시로 재실행 시 LLM 재호출 방지 (git 미포함) - ollama(gemma4)로 나루토 1화 앞부분 추출 스모크 테스트 확인
2026-07-04T07:08:24Z	33ddca06fde97a2a29ae8cb6e41fb8968d54c042	https://github.com/shinkeonkim/anime-ontology/commit/33ddca06fde97a2a29ae8cb6e41fb8968d54c042	RDF/OWL 코어 온톨로지 스키마 및 저장소 구현 - core.ttl: 모든 시리즈가 공유하는 코어 클래스(Character/Organization/Location/ Ability/Item/Event 등)와 관계·데이터 속성 정의 - 애니메이션 특유 요소(비인간 캐릭터, 장소+조직의 다중 상속, 학습형/선천형 능력 구분, 관계 누적, 출처 추적용 mentionedIn)를 어떻게 스키마에 담았는지 docs/ontology-design.md에 문서화 - store.py: 코어 스키마와 시리즈별 ttl을 합쳐 로드하고, 저장 시에는 코어 트리플을 제외한 시리즈 고유 데이터만 파일로 남겨 재실행해도 파일이 불필요하게 커지지 않도록 구현 (그래프 diff로 idempotent 저장 검증)
2026-07-04T07:03:33Z	3168ee7ab99c0dbe961c77d8d84064aedaeb9613	https://github.com/shinkeonkim/anime-ontology/commit/3168ee7ab99c0dbe961c77d8d84064aedaeb9613	LLM Provider 추상화 계층(Proxy 패턴) 구현 - LLMProvider 인터페이스 + OpenAI/Ollama/Anthropic 구현체를 provider 레지스트리로 등록 - LLMProviderProxy: .env의 LLM_PROVIDER/LLM_FALLBACK_PROVIDERS 설정만으로 벤더를 교체할 수 있고, primary 실패 시 대체 provider로 자동 재시도 - 추출 모듈은 이후 LLMProviderProxy(=LLMProvider)만 의존하여 특정 벤더에 고정되지 않음 - ollama(primary) 직접 호출 및 openai(키 없음)->ollama fallback 동작을 스모크 테스트로 검증
2026-07-04T06:58:03Z	20c8da30f501c06105b286a1ef28ca87ed2c63c2	https://github.com/shinkeonkim/anime-ontology/commit/20c8da30f501c06105b286a1ef28ca87ed2c63c2	원본 데이터 탐색 및 SAMI 자막 파서 구현 - discovery: 시리즈 디렉토리에서 영상/자막을 stem 기준으로 페어링하고, 파일명 끝 숫자 또는 자연 정렬 순서로 에피소드 번호를 추론 (naruto 외 시리즈도 대응) - subtitles: SAMI(.smi) 정규식 파서 + 확장자별 파서 레지스트리(향후 srt/ass 확장 가능) - 나루토 1~100화 전체 파싱 검증 완료 (총 28,189개 대사 큐)
2026-07-04T06:53:59Z	4a177f2c9b5795f578e88fd812219ca9c899726a	https://github.com/shinkeonkim/anime-ontology/commit/4a177f2c9b5795f578e88fd812219ca9c899726a	프로젝트 스캐폴딩 및 나루토 원본 자막 데이터 추가 - 온톨로지 저장은 RDF/OWL(git 추적 가능한 ttl)을 정본으로 하고 필요 시 Neo4j로 내보내는 하이브리드 구조로 설계 - 개체/관계 추출은 특정 LLM 벤더에 고정되지 않도록 Provider 추상화(Proxy 패턴)를 적용할 예정이며 설계 배경을 docs/architecture.md에 정리 - 원본 영상(mp4)은 용량(6.4GB)과 저작권 문제로 gitignore 처리, 자막(smi)만 추적
