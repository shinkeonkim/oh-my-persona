GitHub repository: https://github.com/shinkeonkim/boj-solution-archiving-site
Pull request author: shinkeonkim

2026-08-28T09:40:42Z	#5	MERGED	https://github.com/shinkeonkim/boj-solution-archiving-site/pull/5	fix(deploy): defensively handle stale boj.db directory from old subPath attempt	merged=2026-08-28T10:12:44Z	closed=2026-08-28T10:12:44Z	commits=1	files=1	+6/-1	Hit this on the live cluster: an earlier deploy attempt (before the subPath fix landed) left /app/db-data/boj.db as a directory inside the PVC (Kubernetes' subPath auto-creation on a fresh volume). touch succeeds silently on an existing directory, so the app kept crashing with 'unable to open database file' even after switching to the symlink approach. Detect and fix that case defensively on every container start, and add set -e so future failures in this script aren't silently swallowed.
2026-08-28T08:51:23Z	#4	MERGED	https://github.com/shinkeonkim/boj-solution-archiving-site/pull/4	fix(deploy): stop using subPath for boj.db, symlink instead	merged=2026-08-28T09:09:45Z	closed=2026-08-28T09:09:45Z	commits=2	files=2	+22/-7	Live cluster error: 'error mounting .../web/0 to rootfs at /app/boj.db: ... not a directory'. The image bakes in a committed boj.db file; subPath on a fresh/empty PVC gets auto-created as a directory by Kubernetes, conflicting with the file already at that path in the image. Mount the db PVC as a plain directory and symlink /app/boj.db to it at container start instead - database.py auto-creates the schema on startup so an empty file is fine.
2026-08-28T08:03:27Z	#3	MERGED	https://github.com/shinkeonkim/boj-solution-archiving-site/pull/3	fix(deploy): use longhorn-r1 storageClass	merged=2026-08-28T08:14:24Z	closed=2026-08-28T08:14:24Z	commits=1	files=1	+4/-1	Live cluster error: 'precheck new replica failed: insufficient storage'. Default longhorn StorageClass wants 2 replicas; this 3-node cluster doesn't have enough per-node headroom after the minimal-available-percentage buffer. SQLite is single-file/single-writer already, no need for storage-level replication - switch to longhorn-r1 (numberOfReplicas=1, already exists in-cluster).
2026-08-28T08:00:42Z	#2	MERGED	https://github.com/shinkeonkim/boj-solution-archiving-site/pull/2	fix(ci): set up docker-container buildx driver before build	merged=2026-08-28T08:14:11Z	closed=2026-08-28T08:14:11Z	commits=1	files=1	+2/-0	cache-to: type=gha requires the docker-container driver, which the default docker driver doesn't support ("Cache export is not supported for the docker driver"). This fix was written for PR #1 but merged before landing on main. Adds docker/setup-buildx-action@v3.
2026-08-28T04:04:31Z	#1	MERGED	https://github.com/shinkeonkim/boj-solution-archiving-site/pull/1	feat: add CI/Helm chart for GitOps deploy, remove stale infra/	merged=2026-08-28T07:45:33Z	closed=2026-08-28T07:45:33Z	commits=1	files=10	+164/-60	project-lens가 자동 생성한 PR입니다 (신규 homelab 배포 준비 작업). ## 변경 사항 - `.github/workflows/ci.yml`: main 푸시 시 `ghcr.io/shinkeonkim/boj-archive`로 빌드/푸시. - `deploy/charts/boj-archive/`: 이 레포가 소유하는 Helm 차트 (Deployment/Service/Ingress/PVC). `url-shortener`/`aws-study-site`와 동일하게, oh-my-homelab의 ArgoCD Application이 이 차트를 참조하는 방식으로 뒤이어 연결할 예정입니다. - **replicas는 1로 고정, Recreate 전략** — `database.py`가 경로 오버라이드 없이 `sqlite:///boj.db` 하나만 열기 때문에, db PVC를 정확히 그 경로에 subPath로 마운트합니다. 두 파드가 동시에 같은 파일을 열면 안 됩니다. - 비밀(`ADMIN_PASSWORD`/`SECRET_KEY`)은 외부에서 공급하는 Secret 이름만 참조하고 이 레포에는 절대 커밋하지 않습니다. - `infra/`(ingress.yml/namespace.yml/service.yml) 삭제 — 현재 GitOps 체계 밖의 유물이었습니다. `service.yml`이 `192.168.0.100`이라는, 지금 클러스터와 무관한 고정 IP를 가리키는 수동 `Endpoints`를 쓰고 있어서 오히려 혼란을 유발했습니다. 위 Helm 차트 + 곧 추가할 ArgoCD Application으로 대체합니다. ## 확인한 것 - `helm lint` / `helm template` 정상. - `docker build .` 성공, 빌드된 이미지 실행 후 `/`에 실제로 200 응답 확인. - `scraper.py`의 Playwright는 `headless=False`로 사람이 직접 브라우저에 로그인하는 로컬 전용 스크립트이고 `main.py`(실제 배포되는 FastAPI 서버)는 이를 import하지 않습니다 — 그래서 Dockerfile에 Playwright 브라우저 설치는 추가하지 않았습니다. ## 다음 단계 oh-my-homelab 레포에 이 차트를 참조하는 ArgoCD Application + 필요한 비밀을 별도로 추가합니다.
