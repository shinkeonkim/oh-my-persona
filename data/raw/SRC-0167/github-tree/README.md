# Terraform Associate 004 Study Website

Terraform Associate 004와 Terraform 1.12 학습을 위한 한영 병기 정적 웹 사이트입니다. 기존 학습 자료는 `src/content/docs/archive/`로 마이그레이션되었으며, 사이트의 핵심 경로는 공식 시험 목표 1a-8d 순서로 구성됩니다.

This is a bilingual static study website for Terraform Associate 004 and Terraform 1.12. Migrated material lives under `src/content/docs/archive/`, while the core path follows official objectives 1a-8d.

Repository: https://github.com/kokoa-study-room/terraform-associate-004-study-notes

## Run locally

```bash
bun install --frozen-lockfile
bun run dev
```

Production search is generated by Pagefind during the static build.

```bash
bun run build
bun run preview
```

The production output is written to `dist/`. The default canonical origin is `https://terraform-study.shinkeonkim.com`; `SITE_URL` can override it for another environment.

```bash
SITE_URL="https://preview.example.com" bun run build
```

## Content structure

- `src/content/docs/domains/`: bilingual Domain 1-8 curriculum
- `src/content/docs/guide/`: learning path, labs, and practice workflow
- `src/content/docs/reference/`: objectives, glossary, corrections, source map, and authoring rules
- `src/content/docs/archive/`: migrated legacy study material
- `src/content/docs/practice/`: original practice questions derived from objective coverage research
- `public/lab-files/`: reviewed downloadable Lab 01-03 solutions
- `public/practice-files/`: reviewed hello-world configuration artifacts

## Refresh official sources

```bash
bun run sources:update
bun run tutorials:update
```

`sources:update` resolves curated Terraform 1.12 paths from `hashicorp/web-unified-docs` and writes immutable source metadata to `src/data/official-source-index.json`. `tutorials:update` snapshots every item returned by the Terraform-filtered Tutorials Library into `src/data/tutorial-catalog.json`, including product relationship, edition, collection, difficulty, headings, and this site's study-scope classification. Both artifacts are committed so normal builds remain network-independent.

Review the upstream BUSL 1.1 license before redistributing original HashiCorp content. The tutorial catalog stores metadata and links, not tutorial bodies.

## Editorial policy

- Official HashiCorp objectives and versioned Terraform 1.12 documentation are primary.
- Public practice and dump sites may inform topic coverage only.
- Leaked or memorized live-exam questions are not copied.
- New questions must be original, scenario-based, bilingual, and cite the concept source used to explain the answer.

## Cloudflare Workers

The repository follows the same Workers Builds and Static Assets pattern as the reference CV project. Bun installs dependencies and builds the Astro site; Wrangler deploys `dist/` and binds the custom domain declared in `wrangler.json`.

```bash
bun install --frozen-lockfile
bun run build
```

Use these Workers Builds settings:

- Project name: `terraform-study`
- Production branch: `main`
- Build command: `bun run build`
- Deploy command: `bun run deploy`
- Custom domain: `terraform-study.shinkeonkim.com`

See [`DEPLOYMENT.md`](./DEPLOYMENT.md) for the dashboard procedure and production verification checklist.
