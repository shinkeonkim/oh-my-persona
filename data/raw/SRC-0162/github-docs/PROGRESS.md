# 진행 상황

컴파일러 교안 사이트 구축 기록. 작업 단위마다 이 파일을 갱신하고 커밋한다.

- 사람이 읽을 서술형 기록은 사이트의 [개발 로그](site/blog/)에도 남긴다.
- 최신 동향 조사의 1차 자료는 [`research/RESEARCH-NOTES.md`](research/RESEARCH-NOTES.md).

---

## 전체 계획

| # | 작업 | 상태 |
|---|---|---|
| 1 | 사이트 기반 설정 (Mermaid / KaTeX / 한국어 / 사이드바 / 홈) | ✅ 완료 |
| 2 | 최신 경향·연구 리서치 | ✅ 완료 |
| 3 | 1부 — 컴파일러 개요, 언어와 문법 | ✅ 완료 |
| 4 | 2부 — 정규언어, 정규 표현, 유한 오토마타, 표현 방법 | ✅ 완료 |
| 5 | 3부 — LEX 3개 장 + flex 실습 코드 | ✅ 완료 |
| 6 | 4부 — CFG, 문법 유형, 구문 분석, LL, LR, LR 구현 | ✅ 완료 |
| 7 | 5부 — YACC 3개 장 + bison 실습, 통합 미니 컴파일러 | ✅ 완료 |
| 8 | 6부 — 최신 경향과 연구, 도구 지형도 | ✅ 완료 |
| 9 | 실습 하네스 — examples/ 빌드·테스트 자동화 | ✅ 완료 (작업 5에서 구축, 이후 확장) |
| 10 | 최종 검증 — 링크 `throw` 복구, 전체 빌드, README 정리 | ✅ 완료 |
| 11 | **감사 후 보강** — 누락 주제, 생략된 계산, 용어 정의, 확인 문제 해설 | ✅ 완료 |
| 12 | **재감사와 보강** — LR(1) 계산, 용어 사전 갱신, 참고 문헌, 부트스트랩, 재진입/유니코드, 문서 lint | ✅ 완료 |
| 13 | **3차 감사** — 14·17장 인터랙티브 신설, 예제 10·11, 끊어진 연결 복구, 용어 사전 전수 검사 | ✅ 완료 |
| 14 | **4차 감사** — admonition 제목 39곳 복구, 검색 도입, 교안 표를 테스트로 묶음 | ✅ 완료 |
| 15 | **5차 감사** — 인쇄 지원, 해설 수치 독립 검증, 용어 누락을 검사로 전환 | ✅ 완료 |
| 16 | **2부 수식 설명 보강** — 3~5장에 "수식 읽는 법" 14블록 + 확인 문제 3개 | ✅ 완료 |
| 17 | **공개 저장소 연결** — clone 안내, 실습 진입점마다 저장소 링크 | ✅ 완료 |

---

## 기록

### 2026-08-07 · 작업 1–3

**한 일**

- `bun create docusaurus@latest site classic --typescript` 로 스캐폴딩.
- 템플릿 기본 콘텐츠(tutorial-basics 등) 제거, 교안용 디렉터리 구조 생성.
- Mermaid(`@docusaurus/theme-mermaid`), KaTeX(`remark-math` + `rehype-katex`) 도입.
- 로케일을 `ko`(`ko-KR`)로 설정. 블로그를 `/log` 경로의 **개발 로그**로 용도 변경.
- 홈페이지 재작성 — 히어로, 컴파일러 파이프라인 도해, 6부 학습 로드맵 카드.
- 최신 동향 리서치 수행 후 `research/RESEARCH-NOTES.md` 에 출처와 함께 정리.
- 문서 작성: `intro`, `1. 컴파일러 개요`, `2. 언어와 문법`, `실습 환경 구성`.

**설계 판단**

- `markdown.format: 'detect'` — 교안 본문에 `{a, b}`, `<expr>` 같은 표기가 계속
  등장하는데 MDX가 이를 JSX로 해석해 빌드를 깬다. `.md` 는 CommonMark 로 처리하고,
  React 컴포넌트가 필요한 페이지만 `.mdx` 로 쓰기로 했다.
- `onBrokenLinks: 'warn'` (집필 기간 한정) — 앞 장이 뒷 장을 참조하는 구조라
  순서대로 쓰면 반드시 끊긴 링크가 생긴다. 처음부터 최종 URL 로 쓰고
  마지막에 `'throw'` 로 되돌려 한 번에 검증한다.
- 사이드바는 완성된 파트만 등록 — 어느 커밋에서도 사이트가 빌드되는 상태를 유지한다.

**검증**

- `bun run build` 통과. 경고는 아직 작성하지 않은 뒷장으로의 링크뿐.
- 실습 환경 확인: flex 2.6.4 / bison 2.3 / gcc / make (macOS).
  bison 2.3 은 오래된 버전이므로 `-Wcounterexamples` 를 쓰려면
  Homebrew bison 3.8+ 이 필요하다는 점을 실습 환경 문서에 명시.

### 2026-08-07 · 작업 4 — 2부 정규언어

**한 일**

- 인터랙티브 컴포넌트 3종 작성 (`site/src/components/automata/`).
  - `AutomatonDiagram` — 상태/간선 스펙에서 SVG 전이도를 그린다. 자기 고리,
    양방향 간선 자동 곡률 배분, 같은 방향 간선 라벨 병합, 활성 상태·간선 강조.
  - `AutomatonSimulator` — NFA/DFA 공용. 내부적으로 **항상 상태 집합**을 들고
    다니며 ε-closure 를 취하므로 DFA·NFA·ε-NFA 를 코드 하나로 처리한다.
    입력 테이프, 단계 이동, 수락/거부 판정, 사용자 입력 편집.
  - `SubsetConstruction` — 부분집합 구성을 한 행씩 진행하며 표를 채운다.
    Dragon Book 의 A~E 명명과 결과가 일치하는지 확인했다.
- 문서 4장 작성: 3. 정규언어 / 4. 정규 표현 / 5. 유한 오토마타 / 6. 정규언어의 표현 방법.
  5·6장은 React 컴포넌트를 쓰므로 `.mdx`, 나머지는 `.md`.

**빌드 중 발견해 고친 문제 두 가지**

1. **KaTeX 안의 `$` 가 수식을 조기 종료시킨다.**
   `$\text{$\varepsilon$-closure}(T)$` 처럼 `\text{}` 안에 `$` 를 넣으면
   remark-math 가 안쪽 `$` 를 닫는 구분자로 읽어 수식이 깨진다.
   `\varepsilon\text{-closure}` 형태로 바꿔 해결.

2. **Docusaurus 3 는 `:::tip 제목` 공백 제목 문법을 조용히 버린다.**
   빌드 경고 없이 `:::` 가 그대로 본문에 찍혀 나온다. 실험으로 확인한 결과
   `:::note`(제목 없음)와 `:::tip[제목]`(대괄호)만 동작한다.
   전체 문서의 admonition 58개를 대괄호 문법으로 일괄 변환했다.
   → 앞으로 admonition 은 반드시 `:::type[제목]` 으로 쓴다.

**검증**

- `bun run build` 통과.
- 로컬 서버 + 브라우저로 실제 렌더링 확인:
  Thompson NFA 11-상태 다이어그램의 곡선 라우팅, 시뮬레이터 단계 이동과
  수락 판정, 부분집합 구성 표 진행, admonition 58개 렌더링(잔여 리터럴 `:::` 0개).
- 다이어그램 viewBox 높이를 실제 내용에 맞게 줄여 아래쪽 빈 공간 제거.

### 2026-08-07 · 작업 5 — 3부 LEX + flex 실습

**한 일**

- 실습 하네스 구축: `examples/Makefile`, `examples/common/rules.mk`.
  `tests/NAME.in` / `NAME.expected` / `NAME.args` 규약으로 diff 비교.
- flex 예제 4종 작성 및 테스트 통과 (총 12케이스).
  - `01-lex-wordcount` — lex 파일 3부 구조
  - `02-lex-tokenizer` — 최장 일치·규칙 순서·catch-all
  - `03-dfa-by-hand` — 전이표 구동 / 직접 코딩 DFA, 두 구현 일치 검증
  - `04-lex-states` — 시작 조건, 중첩 주석, 이스케이프 해석, `<<EOF>>`
- 문서 3장 + 실습 페이지 작성: 7. LEX / 8. LEX 입력 및 파싱 /
  9. LEX 입력 파일 작성 / LEX 실습.

**문서에 실은 도구 출력은 모두 실제 실행 결과**

- `flex -v` 상태 수 (wordcount 15 NFA/7 DFA, tokenizer 273 NFA/92 DFA)
- `flex -d` 매치 추적 (`--accepting rule at line NN`)
- `flex -b` 되감기 보고서(`lex.backup`)
- 규칙 순서를 뒤집었을 때의 `rule cannot be matched` 경고

**설계 판단**

- `common/rules.mk` 에서 `LEX`/`YACC` 를 `?=` 가 아니라
  `$(origin ...)` 검사로 덮어썼다. make 는 `LEX=lex`, `YACC=yacc` 를
  내장 기본값으로 갖고 있어 `?=` 로는 바뀌지 않는다.
  특히 `yacc` 는 bison 의 yacc 호환 모드라 출력 파일 이름이 달라진다.
- 예제의 진단 출력 앞에 `fflush(stdout)` 을 넣었다.
  그러지 않으면 stdout 버퍼링 때문에 오류 줄이 전부 앞으로 몰려
  테스트 비교가 불안정해진다.

**검증**

- `cd examples && make test` — 4개 예제 12케이스 전부 통과.
- `bun run build` 통과.

### 2026-08-07 · 작업 6 — 4부 구문 분석

**한 일**

- 파싱 컴포넌트 4종 (`site/src/components/parsing/`).
  - `simulate.ts` — LL(1)·LR 알고리즘 본체를 React 와 분리한 순수 함수.
  - `simulate.test.ts` — **교안에 싣는 파싱 표를 기계로 검증한다.**
    유효 입력 12개 수락 / 무효 입력 8개 거부, `id + id * id` 의 축약 순서가
    `[6,4,2,6,4,6,3,1]` 인지, LL 과 LR 이 같은 언어를 인정하는지. 44개 통과.
  - `LLParser`, `LRParser` — 스택/입력/동작 추적표를 단계별로 표시.
  - `FirstFollow` — 고정점 계산을 라운드별로 표시.
- 예제 2종 추가.
  - `05-recursive-descent` — 손으로 쓴 LL(1) 계산기. `-t` 로 호출 추적.
  - `06-lr-table-driven` — 14장의 SLR 표를 그대로 옮긴 표 구동 파서.
    `-t` 로 스택/입력/동작 추적. 의미 값 스택으로 실제 계산까지 한다.
- 문서 6장: 10. CFG / 11. 문법의 유형 / 12. 구문 분석 / 13. LL / 14. LR /
  15. LR 파서의 구현.

**빌드 중 발견해 고친 문제**

- **remark-math 는 수식 안의 `\$` 를 이스케이프로 보지 않는다.**
  `$\mathrm{ACTION}[i, \$] = \textbf{accept}$` 에서 수식이 `\$` 에서 조기 종료되고,
  남은 `\textbf{accept}` 의 `{accept}` 가 MDX 표현식으로 해석되어
  `accept is not defined` 로 **빌드가 실패**했다.
  KaTeX 에 `$` 를 넣는 방법이 `\$` 와 `` \char`$ `` 뿐인데 둘 다 리터럴 `$` 를
  포함하므로, 입력 끝 기호가 들어가는 집합은 **수식 대신 코드 스팬**으로 적기로 했다.
  (`FOLLOW(E)` = `` `{ +, ), $ }` ``)

**설계 판단**

- 파싱 알고리즘을 `simulate.ts` 로 분리한 것은 **테스트 가능성** 때문이다.
  교재의 파싱 표가 한 칸이라도 틀리면 학습자가 며칠을 헤맨다.
  눈으로 검토하는 대신 `bun test` 로 확인한다.
- `bun test` 를 위해 `@types/bun` 을 추가하고 tsconfig 에 등록했다.
  `bun run check` = typecheck + test + build.

**검증**

- `bun test` 44개 통과, `npx tsc --noEmit` 오류 없음, `bun run build` 통과
  (끊긴 링크/앵커 0개).
- `cd examples && make test` — 6개 예제 20케이스 통과.
- 브라우저로 LR 시뮬레이터 14단계 추적이 `06-lr-table-driven` 의
  실제 실행 결과와 일치하는지 확인.

### 2026-08-07 · 작업 7 — 5부 YACC + 통합 프로젝트

**한 일**

- 예제 2종 추가.
  - `07-yacc-calc` — flex + bison 계산기. 모호한 문법 + 우선순위 선언으로
    **충돌 0개**. `%prec UMINUS`, `error` 토큰 복구, 심볼 테이블.
  - `08-mini-compiler` — **통합 프로젝트**. 소스 → 토큰 → AST → 타입 검사
    → 3-주소 코드. 파일 4개(mini.l/.y/.c/.h)로 컴파일러 6단계 중 ①~④.
- 문서 3장 + 실습 2페이지: 16. YACC 개요 / 17. 문법과 액션 /
  18. 충돌과 우선순위 / YACC 실습 / 통합 프로젝트.

**설계 판단**

- `07-yacc-calc` 에서 `%right UMINUS` 를 `%right '^'` **아래**가 아니라
  위에 두었다. 그래야 `-2^2 = -(2^2) = -4` 로 수학·파이썬 관례를 따른다.
  처음엔 반대로 두어 `4` 가 나왔고, 문서의 설명과 어긋나 바로잡았다.
- 줄 번호를 flex 의 `yylineno` 가 아니라 **파서가 직접** 세게 했다.
  `yylineno` 는 `\n` 을 매치하는 즉시 증가하는데, 그 `\n`(EOL)은 파서가
  lookahead 로 먼저 읽으므로 안쪽 축약의 액션에서는 이미 한 줄 앞서 있다.
  실제로 `1 / 0` 오류가 9행 대신 10행으로 보고되는 것을 확인하고 고쳤다.
- `08-mini-compiler` 는 dangling else 충돌 1개를 `%expect 1` 로 남겼다.
  단순한 경고 억제가 아니라 **회귀 방지 장치**라는 점을 문서에 명시했다.
- 파싱과 의미 분석·코드 생성을 **분리된 패스**로 두었다.
  액션은 `node_*()` 호출 한 줄만 하고 로직은 `mini.c` 에 둔다.

**검증**

- `cd examples && make test` — 8개 예제 26케이스 전부 통과.
- bison 충돌: `07` 0개, `08` 1개(`%expect 1` 로 선언됨).
- `bun run build` 통과. 남은 끊긴 링크는 아직 안 쓴 6부 2페이지뿐.

### 2026-08-07 · 작업 8·10 — 6부와 최종 검증

**한 일**

- 6부 2장: 19. 최신 경향과 연구 / 20. 도구 지형도.
  주장마다 각주로 원자료를 달았다(`research/RESEARCH-NOTES.md` 와 대응).
- 부록 2편: 용어 사전(한/영 대조 + 장 링크), 한 장 요약(알고리즘·옵션·흔한 실수).
- `onBrokenLinks` / `onBrokenAnchors` 를 `'throw'` 로 복구.
- 푸터 전체 링크 복원, README.md 작성.

**최종 점검에서 고친 것**

- 한국어 헤딩의 앵커 슬러그를 두 번 틀렸다.
  `### 모호성 제거 ② — dangling else` 의 슬러그는 `모호성-제거---dangling-else`
  (하이픈 **셋**). `②` 와 `—` 가 제거되며 남은 공백 셋이 하이픈이 된다.
  `### 예측 LL(*) / ALL(*)` 도 `예측-ll--all`.
  → 직접 세는 대신 빌드가 잡게 두는 편이 낫다. `throw` 로 되돌린 이유다.

**6부에서 정한 관점**

LLM 관련 서술에서 두 영역을 명확히 구분했다.
성과가 몰린 곳은 **최적화 휴리스틱 탐색과 auto-tuning**(정답이 여럿, 실행해 봐야 앎)이고,
어휘·구문 분석처럼 **결정론적 정확성**이 필요한 영역은 여전히 형식 문법의 것이다.
CompCert가 파서를 검증 대상에서 뺀 것(Menhir가 이미 검증된 파서를 준다)이
이론 확립의 실용적 가치를 보여 주는 방증으로 인용했다.

**최종 검증 결과**

| 검사 | 결과 |
|---|---|
| `bun run typecheck` | 오류 0 |
| `bun test` | 44 pass / 0 fail |
| `bun run build` | 성공, 끊긴 링크·앵커 0 (`throw` 모드) |
| `cd examples && make test` | 8개 예제 26케이스 전부 통과 |
| bison 충돌 | 07: 0개 / 08: 1개(`%expect 1` 로 선언) |

**최종 산출물**

- 교안 20장 + 실습 4페이지 + 부록 2편
- 실행 가능한 예제 8개 (flex 4, 손코딩 C 2, flex+bison 2)
- 인터랙티브 컴포넌트 6종 (오토마타 3, 파싱 3)
- 개발 로그 3편, 리서치 노트 1편

### 2026-08-08 · 작업 11 — 감사 후 보강

"자료가 부실한 곳 / 설명이 생략된 곳 / 용어 정의가 불충분한 곳"을 기계적으로 점검하고
드러난 결함을 전부 고쳤다.

**감사에서 드러난 것**

| 항목 | 상태 |
|---|---|
| 속성 문법·구문 지향 번역 | **전혀 없음** — 가장 큰 구멍 |
| 연산자 우선순위 파싱 | 전혀 없음 |
| 입력 버퍼링·보초 기법 | 사실상 없음 |
| 백패칭·4중자 | 이름만 언급 |
| LR(0) 항목 집합 계산 과정 | **통째로 생략** (정의 → 완성된 11개 집합으로 점프) |
| followpos 계산 과정 | 구문 트리도, 중간 계산도 없음 |
| 선수 지식 안내 | 0건 |
| 확인 문제 정답 | 0개 (문제는 120개) |
| 심볼 테이블·lookahead·렉심·멱집합·비둘기집 원리 | 정의 없이 사용 |

**고친 것**

- **새 장 2개** — 14. 연산자 우선순위 파싱, 17. 구문 지향 번역.
  장 번호를 재조정(14→15, 15→16, 16→18, 17→19, 18→20, 19→21, 20→22)하고
  본문 참조·앵커·예제 주석까지 역순 치환으로 일괄 갱신.
- **새 절 2개** — 8.3 입력 버퍼링(이중 버퍼·보초), 19.7 4중자/3중자, 19.8 백패칭.
- **생략된 계산 복원** — 15.3에 CLOSURE(I0) 회차별 전개와 GOTO 계산,
  6.7에 구문 트리와 노드별 nullable/firstpos/lastpos 표.
- **선수 지식 페이지** 신설, 미정의 용어 5개를 첫 등장 자리에서 정의,
  용어 사전의 잘못된 링크 수정.
- **확인 문제 120개 전체에 접이식 해설** 삽입.

**내용 오류 하나를 잡았다**

4장에서 `(a+)\1` 을 "정규가 아닌 예"로 들었는데 **틀렸다**.
알파벳이 한 글자면 `{ww | w ∈ a+} = {a^2n}` 이고 이는 `(aa)+` 로 표현되는 정규언어다.
`([ab]+)\1` 로 고치고 알파벳 크기가 왜 결정적인지 설명을 붙였다.

**표기 문제 두 가지**

- 수식 안의 `\|` 37건 → `\lvert` `\rvert`.
  KaTeX에서 `\|` 는 이중선(‖)이라 절댓값 기호가 잘못 렌더링되고 있었다.
  표 안에서는 맨 `|` 를 못 쓰므로 파이프를 아예 안 쓰는 표기로 통일.
- 헤딩 안의 수식 → 제거 + `{#id}` 명시.
  목차에는 KaTeX가 적용되지 않아 원본 LaTeX이 노출되고 있었다.

**최종 검증**

| 검사 | 결과 |
|---|---|
| `bun run typecheck` | 오류 0 |
| `bun test` | 44 pass |
| `bun run build` | 성공, 끊긴 링크·앵커 0 (throw) |
| `cd examples && make test` | 8개 예제 24케이스 통과 |
| 확인 문제 : 해설 | 120 : 120 |

### 2026-08-08 · 작업 12 — 재감사와 보강

작업 11 이후 다시 세 질문(부실한 곳 / 생략된 과정 / 용어 정의)으로 점검했다.
**감사 자체를 스크립트로 만든 것**이 이번의 핵심이다.

**드러난 것**

| 항목 | 상태 |
|---|---|
| LR(1)·LALR(1) 항목 집합 | **정의만 있고 계산이 없음** — 작업 11에서 LR(0)만 고쳤다 |
| 용어 사전 | 14·17장 용어 **0개**. LL(1)/SLR/LALR/LR(1) 자체가 없음 |
| 장 번호 표시 | 링크 라벨 **31곳**이 재번호 전 값 (앵커는 유효해 빌드가 통과) |
| 인라인 수식 안의 `\$` | **9곳**이 조용히 깨진 채 렌더링 |
| 수식 안의 `\|` | 3곳이 ‖(이중선)로 렌더링 |
| 수식 안의 `①②③④` | KaTeX 폰트에 없어 `No character metrics` |
| 참고 문헌 | intro에 4줄. 원논문 없음 |
| CFG → PDA 변환 | "흉내 낸다"로만 서술, 구성 없음 |
| 부트스트랩·T-다이어그램 | 없음 |
| flex 재진입·유니코드 | 없음 |
| 예제 저장소 | 컴파일된 바이너리 8개 + 생성된 `.c` 3개가 커밋되어 있음 |

**고친 것**

- **15.7 전면 확장** — `*p = q` 문법의 FOLLOW 계산 → LR(1) CLOSURE 회차별 전개 →
  정준 집합 $I_0 \sim I_{13}$ → LALR 병합 4쌍(14 → 10 상태) → `bison -v` 로 대조.
  `state 4` 의 `'=' shift` 가 손계산과 일치함을 실제 출력으로 확인했다.
- **용어 사전 89 → 150항목.** 14·17장 용어, LL(1)/SLR/LALR/LR(1)/코어/완결 항목,
  촘스키 계층 유형별 용어, 보초·이중 버퍼·4중자·3중자·간접 3중자, 멱집합·PEG·표준형.
- **참고 문헌 페이지 신설** — Knuth 1965(LR)·1968(속성 문법), DeRemer 1969,
  Thompson 1968, Earley 1970, Tomita 1985, Pratt 1973, Ford 2004 등을 장별로 연결.
- **10.3에 CFG → PDA 하향식 3규칙 변환**과 괄호 문법 추적표.
  비결정성이 어디서 나오고 그것이 왜 LL/LR로 갈라지는지까지.
- **1.5 부트스트랩** 신설 — T-다이어그램, 3단계 자립, stage2/stage3 바이트 동일성,
  크로스 컴파일, Thompson의 "Reflections on Trusting Trust".
- **9.10 재진입 스캐너와 유니코드** 신설 + **예제 09-lex-reentrant**.
  `[가-힣]` 이 일본어·중국어·`é` 까지 매치하는 것을 실제 출력으로 보였다.

**감사를 스크립트로 만들었다**

`site/scripts/lint-docs.ts` (`bun run lint`, `bun run check` 에 포함).
전부 **빌드는 통과하는데 결과가 틀리는** 종류다.

| 규칙 | 왜 |
|---|---|
| 인라인 수식 안의 `\$` | remark-math 가 거기서 수식을 끊는다 |
| 수식 안의 `\|` | KaTeX 가 ‖ 로 그린다 |
| 공백 제목 admonition | Docusaurus 3 가 경고 없이 버린다 |
| 이중 백틱 코드 스팬 | 백틱이 본문에 그대로 찍힌다 |
| 링크 표시 장 번호 | 재번호 후 앵커만 고치면 남는다 |
| 헤딩 안의 수식 | 목차에 원본 LaTeX 이 노출된다 |
| 확인 문제 수 ≠ 해설 수 | 풀이가 빠진 장 |

`onBrokenLinks: 'throw'` 가 링크를 지키듯, 이제 렌더링 결과도 회귀 테스트가 있다.

**저장소 정리**

`make` 로 다시 만들 수 있는 것을 커밋하고 있었다.
바이너리 8개(arm64 전용)와 flex가 생성한 `.c` 3개를 추적 해제하고
`.gitignore` 에 규칙을 명시했다. clean 상태에서 `make test` 통과를 확인했다.

**최종 검증**

| 검사 | 결과 |
|---|---|
| `bun run lint` | 문제 없음 |
| `bun run typecheck` | 오류 0 |
| `bun test` | 44 pass |
| `bun run build` | 성공, 끊긴 링크·앵커 0, KaTeX 경고 0 |
| `cd examples && make test` | 9개 예제 27케이스 통과 |
| 확인 문제 : 해설 | 123 : 123 |

### 2026-08-08 · 작업 13 — 3차 감사

이번에는 **자산 인벤토리**로 훑었다. 장별로 mermaid·인터랙티브·수식·표·
코드블록 수를 세니 결함이 바로 드러났다.

**드러난 것**

| 항목 | 상태 |
|---|---|
| 14장(연산자 우선순위) | **mermaid 0개, 인터랙티브 0개** — 유일하게 그림이 없는 장 |
| 17장(구문 지향 번역) | 인터랙티브 0개 |
| `examples/09-lex-reentrant` | 만들어 놓고 **교안·치트시트·setup 어디에서도 연결 안 됨** |
| 홈 로드맵 | 20장 시절 그대로 — 14·17장 누락 |
| 용어 사전 `완전 DFA` | **본문에 없는 말** (본문은 "전함수") — 2차 감사에서 내가 만든 오류 |
| 용어 사전 링크 5건 | 정의된 곳이 아니라 엉뚱한 절을 가리킴 |
| 14·17장 실습 예제 | 없음 |

**만든 것**

- **`OperatorPrecedenceParser`** (14장) — 우선 관계 표를 함께 그리고
  **지금 보고 있는 칸을 강조**한다. `id + id * id` 의 6단계에서
  `+` 행 `*` 열의 ⋖ 하나가 파스 트리를 정하는 것을 눈으로 좇을 수 있다.
  우결합 `^` 를 넣은 두 번째 표도 함께 둬서 결합성 차이를 대조한다.
- **`AttributeEval`** (17장) — 주석 달린 파스 트리에서 속성값이
  위상 정렬 순서대로 채워진다. 표의 마지막 열이 **후위 순회 순위**인데,
  합성 속성 예제에서는 단조 증가하고 상속 속성 예제에서는 `1, 9, 6, 3, 3, 6, 9` 로
  오르내린다. 그 오르내림이 곧 "yacc에서 중간 액션이 필요한 이유"다.
- **`examples/10-operator-precedence`** — 우선 관계 표를 `PREC[6][6]` C 배열로.
  `06-lr-table-driven` 과 입력·출력 형식을 맞춰 같은 식을 두 파서에 넣어 비교할 수 있다.
- **`examples/11-attribute-eval`** — 속성 문법을 C로 구현.
  의존 그래프를 위상 정렬하고, "후위 순회로 되는가"를 판정해
  **막히는 지점을 지목**한다. 웹 컴포넌트와 같은 알고리즘을
  TypeScript·C 두 번 구현해 결과를 맞췄다.

알고리즘은 전부 React 밖의 순수 함수(`precedence.ts`, `attributes.ts`)로 두고
`bun test` 로 검증한다 — 테스트 44개 → **87개**.

**고친 연결**

- 9.10절 본문 → 예제 09, `lex-labs` 에 실습 5 추가 (yacc-labs 는 6~8로 재번호)
- 치트시트 예제 표, `setup.md` 디렉터리 트리에 09·10·11 추가
- 홈 로드맵에 14·17장 추가

**용어 사전 전수 검사**

127개 항목 전부에 대해 "한국어 표기가 링크한 장의 본문에 실제로 있는가"를 확인했다.
5건이 걸렸다 — `완전 DFA`(→ 전함수), `스캐너 / 토크나이저`, `동등 클래스`(장이 틀림),
`결정적 CFL`(→ DCFL), `하향식 파싱`(→ 하향식). 링크가 엉뚱한 절을 가리키던 5건도 고쳤다.

**이 검사를 lint 규칙 8번으로 넣었다.** 일부러 되돌려 놓고 lint가
잡는지 확인한 뒤 되살렸다.

**최종 검증**

| 검사 | 결과 |
|---|---|
| `bun run lint` | 문제 없음 (규칙 8개) |
| `bun run typecheck` | 오류 0 |
| `bun test` | 87 pass |
| `bun run build` | 성공, 끊긴 링크·앵커 0 |
| `cd examples && make test` | 11개 예제 34케이스 통과 |
| 확인 문제 : 해설 | 125 : 125 |

브라우저에서 두 컴포넌트의 실제 렌더링도 확인했다
(표 강조, 트리 표시, 단계 이동, 다크 모드).

### 2026-08-08 · 작업 14 — 4차 감사

이번에는 **빌드된 HTML을 직접 읽는** 검사를 처음 만들었고,
그 자리에서 가장 큰 렌더링 결함이 나왔다.

**admonition 제목 39곳이 통째로 버려지고 있었다**

```md
:::info[정의 — 알파벳 $\Sigma$ 위의 정규 표현]
```

이 제목이 화면에는 "정보"라는 기본 라벨로 나오고,
제목 글은 본문 첫 줄에 밋밋하게 붙고 있었다.

원인은 `markdown.format: 'detect'` 였다.
제목에 서식(백틱·수식·굵게)이 들어가면 remark 가 `mdxAdmonitionTitle` 요소로 감싸
본문 앞에 넣고 테마가 그것을 제목 자리로 끌어올리는데,
**`.md`(CommonMark) 경로에서는 그 요소 이름이 소문자로 내려온다.**
테마는 camelCase 로만 찾으므로 못 찾는다.

`.mdx` 파일은 멀쩡했고 `.md` 만 깨졌다 — 39곳 전부 `.md` 였다.
`src/theme/Admonition` 를 스위즐해 소문자 이름까지 찾도록 고쳤다.
제목 있는 admonition 325개 중 버려진 것 0개.

**검색이 없었다**

`navbar` 에 `{type: 'search'}` 만 있고 플러그인이 없어 **아무것도 렌더되지 않고 있었다.**
경고도 없다. 22장 9만 낱말짜리 사이트에 검색이 없는 셈이었다.

`@easyops-cn/docusaurus-search-local` 를 넣었다. 오프라인 색인이라 외부 서비스가 필요 없고,
`lunr.ko.js` 가 있어 한국어도 된다. "백패칭"으로 검색해 7건이 절 단위로 잡히는 것을 확인했다.
UI 문자열 55개도 한국어로 번역했다(`i18n/ko/code.json`).

**표를 네 군데에 두고 있었다**

같은 SLR 표가 교안 마크다운, `grammars.ts`, 예제 06의 C 배열에 각각 있다.
하나를 고치고 나머지를 잊으면 교안이 거짓말을 하게 된다.

`doc-tables.test.ts` 가 **교안의 마크다운 표를 직접 파싱해** 나머지와 한 칸씩 대조한다.
SLR(1)·LL(1)·우선 관계 표 셋과 C 배열 둘. 테스트 87 → **97개**.
일부러 표의 한 칸을 틀리게 바꿔 잡히는 것을 확인했다.

**그 밖에 잡은 것**

| 문제 | 곳 |
|---|---|
| 코드 스팬 안의 `\lvert` 가 글자 그대로 노출 | 3·4장 핵심 표 5곳 |
| 표 안에서 수식이 `\|` 로 잘림 (`$O(n^2 \cdot \|\Sigma\|)$`) | 6장 3곳 |
| 수식 안의 ✅ ❌ — KaTeX 폰트에 없음 | 3곳 |

앞의 둘은 2차 감사에서 `\|` 를 일괄 치환한 부작용이다.

**검사가 두 단계가 되었다**

| 검사 | 보는 것 |
|---|---|
| `bun run lint` | 마크다운 원본 (규칙 10개) |
| `bun test` | 코드 + **교안의 표** (97개) |
| `bun run lint:build` | **빌드된 HTML** — 원본은 멀쩡한데 렌더링에서 깨진 것 |

마지막 것이 이번에 새로 만든 것이고, admonition 문제를 잡은 것도 그것이다.

**읽기 부담 측정**

장별 문장 수·평균 낱말·긴 문장 비율을 재 보았다.
평균 8~11 낱말, 25 낱말 넘는 문장은 장당 0~5개 — 눈높이 자체는 문제가 없었다.
다만 15.7절이 1,974 낱말로 앞의 여섯 절을 합친 것과 맞먹어,
그 자리에 "여기서 한 번 쉬어 가도 된다" 안내를 넣었다.

**최종 검증**

| 검사 | 결과 |
|---|---|
| `bun run lint` | 문제 없음 (규칙 10개) |
| `bun run typecheck` | 오류 0 |
| `bun test` | 97 pass |
| `bun run build` | 성공, 끊긴 링크·앵커 0, KaTeX 경고 0 |
| `bun run lint:build` | 문제 없음 (56개 페이지) |
| `cd examples && make test` | 11개 예제 34케이스 |

### 2026-08-08 · 작업 15 — 5차 감사

**인쇄하면 풀이가 한 줄도 안 나왔다**

확인 문제 풀이 125개가 전부 `<details>` 안에 있다.
닫힌 `<details>` 는 `<summary>` 만 인쇄되므로, 종이로 뽑으면 문제만 나오고
풀이는 통째로 빠진다. 시험 준비로 출력하는 학생에게는 치명적이다.

CSS(`details > *:not(summary) { display: revert }`)로도 시도할 수 있지만
닫힌 `<details>` 의 내용을 감추는 방식이 브라우저마다 달라 확실하지 않다.
인쇄 직전에 `open` 을 켜고 끝나면 되돌리는 클라이언트 모듈을 넣었다.
원래 열려 있던 것은 기억해 두었다가 그대로 둔다.

인쇄용 스타일도 함께 넣었다 — 사이드바·목차·버튼 제거, 넘침 해제,
표·코드가 페이지 경계에서 잘리지 않게, 외부 링크는 주소를 각주처럼.
`prefers-reduced-motion` 도 함께 처리했다.

**해설의 수치를 독립 구현으로 검증했다**

교안 해설이 주장하는 오토마타 수치를 Thompson 구성 → 부분집합 구성 →
분할 정제를 파이썬으로 직접 구현해 대조했다.

| 주장 | 검증 |
|---|---|
| `(a\|b)*abb` 부분집합 5상태, 최소화 4상태 | ✅ 5 / 4 |
| `(a\|b)*` Thompson 8상태 ($2n$ 상한과 일치) | ✅ 8 |
| `(a\|b)*a(a\|b)(a\|b)` 최소 DFA 8상태 | ✅ 8 |
| 모호한 식 문법 shift/reduce 4개 (상태 2 × 연산자 2) | ✅ `bison -v` — State 9에 2개, State 10에 2개 |
| `02-lex-tokenizer` NFA 273 / DFA 92 | ✅ `flex -v` |

**전부 맞았다.** 내용 정확도 쪽은 이번에 문제가 없었다.

**표기가 갈린 곳 하나**

`정의부`(7회)와 `정의 절`(3회)이 같은 것을 가리키고 있었다.
`규칙부`·`사용자 코드부`와 맞춰 `정의부`로 통일했다.
다른 24쌍의 표기 후보도 훑었으나 나머지는 전부 의도된 구분이었다
(`환원`은 정지 문제 환원, `구문 트리`는 AST와 followpos 구문 트리 둘 다 정당).

`스트링`(100회)과 `문자열`(88회)은 층이 다른 말인데 그 약속이 어디에도 없어서,
2장에 표기 약속 표를 넣고 사전의 "헷갈리는 짝"에도 추가했다.

**용어 누락을 세 번째로 반복했다 → 검사로 전환**

`**한국어(english)**` 형태로 본문에서 정의해 놓고 사전에 넣지 않은 용어가
또 나왔다 — 부트스트랩, 셀프 호스팅, 재현 가능한 빌드, 후위 순회,
인터프리터, 단계/패스, 연쇄 오류, 공용체, 복구 표현식 등.

2·3·4차에서 모두 같은 일이 있었으므로 **lint 규칙 10번**으로 만들었다.
문장 속 번역일 뿐인 것은 `NOT_A_TERM` 에 적어 판단을 코드에 남긴다.

**그 밖에**

- SSR HTML에서 표 행 하나가 표 밖으로 떨어져 나와 본문에 그대로 찍히고 있었다
  (1장 — JIT 설명을 표 중간에 끼워 넣으면서 마지막 행이 고아가 됐다).
  같은 종류를 전수 검사했고 다른 곳은 없었다.
- 한 장 요약에 14장(우선 관계 표)·17장(속성과 계산 순서)이 빠져 있어 추가했다.
- 헤딩 계층 건너뜀 0건, 인바운드 링크 0인 문서 0건.

**최종 검증**

| 검사 | 결과 |
|---|---|
| `bun run lint` | 문제 없음 (규칙 11개) |
| `bun run typecheck` | 오류 0 |
| `bun test` | 97 pass |
| `bun run build` | 성공 |
| `bun run lint:build` | 문제 없음 (57쪽) |
| `cd examples && make test` | 11개 예제 34케이스 |
| 인쇄 시 `<details>` 펼침 | 6/6 → 인쇄 후 0/6 복원 확인 |

### 2026-08-08 · 작업 16 — 2부 수식 설명 보강

정규언어(3장)·정규 표현(4장)·유한 오토마타(5장)는 이 교안에서 수식이 가장 빽빽한 곳이다.
정의를 적어 놓고 "이 줄을 어떻게 읽는가"를 말하지 않은 자리가 많았다.

**"수식 읽는 법" 블록 14개를 넣었다.**

| 장 | 어디 | 무엇을 풀어 썼나 |
|---|---|---|
| 3.1 | 정규집합의 정의 | $\{a\}$ 는 기호가 아니라 **상자**. 접합·클로저의 집합 정의를 다시 적고 예로 확인. "유한 번" 조건이 없으면 어떤 언어든 정규가 되어 버리는 이유 |
| 3.2 | 우선형 문법 ↔ 오토마타 | **넌터미널 하나 = 상태 하나**. 우변에 넌터미널이 하나뿐이어야 하는 이유가 곧 "기억할 것이 유한하다" |
| 3.3 | 곱 구성 | 5-튜플을 성분별로. 상태 $(p,q)$ 가 "두 기계의 위치를 같이 적어 둔 것". 종결 집합만 바꾸면 ∩·∪·− 가 된다 |
| 3.4 | 펌핑 보조정리 | $\exists p\ \forall w\ \exists(x,y,z)\ \forall i$ — **누가 무엇을 고르는 게임인지** 표로. 부정하면 전부 뒤집히고, 그것이 증명의 네 걸음이 된다 |
| 4.1 | 정규 표현의 정의 | $L(r)$ 이 **표기(글자)와 언어(집합) 두 층을 잇는 함수** |
| 4.2 | 대수 법칙 | 이 등호는 "글자가 같다"가 아니라 $L(r) = L(s)$ 의 줄임말 |
| 4.4 | 정규 정의 | 첨자 $i-1$ 이 재귀를 막는다. 허용하면 괄호 짝 맞추기가 되어 정규언어를 벗어난다 |
| 5.1 | $\delta$ / $\hat\delta$ / $L(M)$ | 화살표를 "넣는 것 → 나오는 것"으로. 재귀 정의를 `ab` 로 직접 펼쳐 보이고, 조건제시법의 세로줄은 "단," |
| 5.2 | NFA 수락 조건 | DFA의 $\in$ 이 왜 $\cap \neq \emptyset$ 이 되는가 |
| 5.3 | ε-NFA / ε 제거 | 세 오토마타의 전이 함수를 세로로 놓아 **한 줄씩만 다름**을 보임. ε-closure 가 앞뒤로 두 번 나오는 이유 |

**확인 문제도 3개 늘렸다** (125 → 128). 셋 다 "읽기"를 묻는다.

- 3장 — $\exists p\ \forall w$ 와 $\forall w\ \exists p$ 의 차이를 반례로
- 4장 — `a|b*` 와 `(a|b)*` 를 $L(\cdot)$ 로 펼쳐 원소 하나로 구별
- 5장 — $\hat\delta(q_0, \texttt{ab})$ 를 정의대로 펼쳐 계산

**중첩 admonition 하나가 깨진 것을 `lint:build` 가 잡았다.**
Docusaurus 3 에서 `:::` 를 중첩하려면 바깥이 `::::` 여야 한다.
형제로 펴서 해결했다.

### 2026-08-08 · 작업 17 — 공개 저장소 연결

실습 코드를 공개하고 있는데 정작 **"어디서 받는가"가 문서 어디에도 없었다.**
`cd examples` 로 시작하는 안내가 여럿인데, 그 `examples/` 가 어디 있는지는 안 알려 준다.

**저장소** [kokoa-study-room/compiler-study-site](https://github.com/kokoa-study-room/compiler-study-site)

**실습 환경 구성에 3절을 새로 넣었다** (기존 3~6절은 4~7절로 밀었다).

- `git clone` + zip 대안
- 받자마자 `make test` 로 확인 — 통과 출력까지 함께
- 막히는 증상 5가지와 각각 어디를 볼지 (표)
- 버전 차이로 테스트만 실패하는 경우는 정상이라는 안내
- `git pull` 후 `make clean` 을 먼저 해야 하는 이유
  (생성된 `.c` 와 실행 파일은 저장소에 없고 빌드 때마다 새로 생긴다)
- `site/` 로 교안 자체를 로컬에서 띄우는 법

**저장소 구조 절**도 최상위 디렉터리부터 보이도록 고쳤다.
`examples/` 만 보여 주니 "이게 어디 안에 있는 건지" 알 수 없었다.

**실습 진입점마다 clone 한 줄씩** — LEX 실습, YACC 실습, 통합 프로젝트.
어느 페이지로 바로 들어와도 시작할 수 있게 했다.

**`GITHUB_REPO` 를 실제 주소로.** placeholder(`your-org`)로 두는 바람에
navbar 의 GitHub 링크와 각 문서의 "이 페이지 고치기" 링크가 전부 죽어 있었다.
