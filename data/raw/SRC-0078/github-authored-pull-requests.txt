GitHub repository: https://github.com/kmu-aws-capstone-team-4/interview-analysis-report
Pull request author: shinkeonkim

2026-04-29T08:37:08Z	#2	MERGED	https://github.com/kmu-aws-capstone-team-4/interview-analysis-report/pull/2	fix(db): SQLAlchemy 풀 크기 명시 및 application_name 라벨 추가	merged=2026-04-29T09:23:26Z	closed=2026-04-29T09:23:26Z	commits=1	files=1	+12/-2	## Summary - SQLAlchemy `create_engine()` 에 `pool_size=2, max_overflow=2, pool_timeout=10` 명시 - `connect_args={"application_name": "mefit-interview-analysis-report"}` 추가 ## Description ### 변경 사항 `db/connection.py` 의 엔진 옵션을 다음과 같이 보강: ```python engine = create_engine( DATABASE_URL, pool_pre_ping=True, pool_recycle=300, pool_size=2, # NEW max_overflow=2, # NEW pool_timeout=10, # NEW connect_args={ # NEW "application_name": os.getenv( "DB_APPLICATION_NAME", "mefit-interview-analysis-report" ), }, ) ``` ### 배경: 2026-04-29 RDS 커넥션 고갈 인시던트 mefit-backend-production RDS PostgreSQL 에서 max_connections (=79) 한계 도달: ``` psycopg.OperationalError: connection failed: FATAL: remaining connection slots are reserved for roles with privileges of the "pg_use_reserved_connections" role ``` 진단 결과: - 64/74 conn 점유 (일반 슬롯 86%) - 55 idle conn 누수, 최장 41시간 alive - 주범은 backend Celery worker 의 `close_old_connections()` 시그널 미연결 **본 서비스의 직접적 누수는 확인되지 않았으나**, 잠재 위험으로: - `pool_size`, `max_overflow` 미명시 → SQLAlchemy 기본값 (`pool_size=5, max_overflow=10`) 적용 - 결과: per-process 최대 15 conn → Celery prefork concurrency=2 환경에서 worker child 2개 × 15 = **최대 30 conn** - `application_name` 미설정 → `pg_stat_activity` 에서 본 서비스 식별 불가 ### 효과 | 항목 | Before | After | |---|---|---| | Per-process 최대 conn | 15 (5 + 10) | **4** (2 + 2) | | Worker 전체 최대 (concurrency=2) | 30 | **8** | | `pg_stat_activity` 식별 | 불가 (`(none)`) | `mefit-interview-analysis-report` | | `pool_timeout` | 무한 (기본 30s, 그러나 명시 X) | **10s 명시** | ### 인시던트 상세 monorepo 의 `backend/docs/incidents/2026-04-29-rds-connection-exhaustion.md` 의 §5.3 참조. ## Type of Change - [x] Bug fix (non-breaking) - [ ] New feature - [ ] Breaking change - [ ] Documentation update ## Testing - [x] yapf / ruff / mypy 등 정적 분석 통과 (LSP diagnostics: No issues) - [ ] **운영 배포 후 검증 필요** (체크리스트 below) ### 배포 후 검증 ```bash # backend pod 에서 진단 쿼리 실행 NS=mefit-backend-production POD=$(kubectl -n $NS get pod -l app=mefit-production-api -o jsonpath='{.items[0].metadata.name}') kubectl -n $NS exec -i $POD -c django -- python webapp/manage.py shell <<'PY' from django.db import connection with connection.cursor() as c: c.execute(""" SELECT application_name, count(*) FROM pg_stat_activity WHERE datname = current_database() GROUP BY 1 ORDER BY 2 DESC """) for row in c.fetchall(): print(row) PY ``` 기대 결과: `('mefit-interview-analysis-report', N)` 행 (N <= 8) 이 표시됨. ## Checklist - [x] 코드 스타일 검사 통과 - [x] 타입 힌트 유지 - [ ] 관련 문서 업데이트 (monorepo backend/docs/incidents/ 별도 PR) ## Related Issues Closes #1
