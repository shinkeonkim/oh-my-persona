GitHub repository: https://github.com/shinkeonkim/project-lens
Author account: shinkeonkim

2026-08-28T14:38:35Z	cd2b69d328e34bb19c52987b431f6e2357387ca7	https://github.com/shinkeonkim/project-lens/commit/cd2b69d328e34bb19c52987b431f6e2357387ca7	docs: rewrite README for current feature set, add docs index Top-level README still described Phase 0-5 with 14 tracked projects - badly stale given everything since (dashboard, audits, AdSense, Cloudflare integration, oh-my-homelab support). Rewrote it to reflect actual current capabilities, organized by category, with a real command reference table. Added docs/README.md as an index for the growing docs/ directory (9 files now) grouped by when you'd actually reach for each one. Also updated docs/COMMANDS.md, which had drifted since - missing every command added this session (dashboard, audits, ads-sync, adsense-status, cloudflare tunnel-route, set-ads-policy, report-all). (Tried adding .claude/commands/README.md as a slash-command index too, but the harness treats any .md file in that directory as an invokable command - reverted, docs/COMMANDS.md already covers this.)
2026-08-28T14:35:00Z	304680e7bfcf39aa26a9a669883b44802b5c98fe	https://github.com/shinkeonkim/project-lens/commit/304680e7bfcf39aa26a9a669883b44802b5c98fe	chore: move project inventory/PR review docs to gitignored docs/private/ Not secrets, but not something to publish either - they list every personal/org project this tool tracks, their real deployment domains, and real GA4/GTM IDs in one place. Ahead of making this repo public.
2026-08-28T09:37:27Z	006cdbdf1195257dca6144c1a79b09d14afc16cc	https://github.com/shinkeonkim/project-lens/commit/006cdbdf1195257dca6144c1a79b09d14afc16cc	fix: derive Cloudflare account_id from zone lookup, not /accounts /accounts needs a broader Account-level read permission the token doesn't have (and doesn't need to) - the zone lookup response already includes account.id, so use that instead. Keeps the token scoped to exactly DNS+Tunnel+Zone:Read as instructed, no extra permission round trip needed.
2026-08-28T09:25:24Z	badc6a815d300bcdf770119ac87f452855e1598e	https://github.com/shinkeonkim/project-lens/commit/badc6a815d300bcdf770119ac87f452855e1598e	feat: add Cloudflare API integration (creds + DNS/Tunnel routing) New project_lens/cloudflare/ package, stdlib-only (urllib) matching the rest of the codebase's minimal-dependency approach. Token stored via OS keychain (lens creds init --provider cloudflare --token), same pattern as Google credentials. lens cloudflare tunnel-route <hostname> <service> sets up both the DNS CNAME (pointing at <tunnel-id>.cfargotunnel.com, proxied) and the tunnel's public-hostname ingress rule - needed right now to route boj.코드.kr/paeon.코드.kr through the existing Cloudflare Tunnel, and reusable later for new Workers deployments (originally deferred task).
2026-08-28T06:50:48Z	470adc67ab6c42e0c7f4619ed8868dc0394e87e4	https://github.com/shinkeonkim/project-lens/commit/470adc67ab6c42e0c7f4619ed8868dc0394e87e4	feat: add lens ads-sync (ads.txt + GTM AdSense tag automation) Two pieces, both reusing existing infrastructure rather than building new injection paths: - ads.txt: framework-aware placement (public/, static/, apps/web/public/ monorepo convention) mirroring the GTM injection adapter's structure- detection philosophy, but simpler since it's just a static file drop, not content patching. ads.txt is per-hostname per the IAB spec, so each subdomain that carries ads needs its own copy - can't inherit from a parent domain. - GTM AdSense connection tag: added ensure_adsense_tag() to gtm.py, reusing the exact ensure_ga4_config_tag() pattern (Custom HTML tag on the existing "All Pages" trigger). Google officially supports GTM as an AdSense site-verification method, so this covers both verification and ad serving without touching app source code at all - unlike GTM tracking, no per-repo PR is needed for the ad script itself. `lens ads-sync --publisher-id <ca-pub-...> --yes` only ever touches projects with ads_policy=allowed (enforced, not just default-filtered). Dry-run against the real registry confirms it correctly targets exactly the 24 allowed projects.
2026-08-28T06:46:55Z	ef27e08d480e3db85ec959e1266f0b94755c2531	https://github.com/shinkeonkim/project-lens/commit/ef27e08d480e3db85ec959e1266f0b94755c2531	feat: add per-project ads_policy field, set for all 27 tracked projects New projects default to 'unreviewed' - deliberately not opt-out, since ads.txt/GTM ad-tag automation should only ever touch projects explicitly marked 'allowed'. Per explicit user instruction: excluded my-cv, my-resume, my-portfolio (personal-brand sites); allowed the other 24.
2026-08-28T05:59:12Z	e91c6ab22282842227e34958c7f286865ddf121a	https://github.com/shinkeonkim/project-lens/commit/e91c6ab22282842227e34958c7f286865ddf121a	feat: add Google AdSense account/site status check (read-only) Google Ads (already integrated) is for advertisers running campaigns TO drive traffic; AdSense is the opposite - displaying ads ON a site to earn revenue - and wasn't integrated at all. This is what "monetization" actually needs. Read-only by design: site registration and ad-serving approval are a manual Google review process that can't be automated via API, so google/adsense.py only wraps accounts().list()/sites().list(). Added the adsense.readonly OAuth scope (requires `lens creds init --provider google` re-auth, same as every previous scope addition this session) and `lens adsense-status` to report connected account(s) and each registered site's review state.
2026-08-28T05:57:21Z	b59a445c5871a1b90af5fe419379a1d2f64b0c15	https://github.com/shinkeonkim/project-lens/commit/b59a445c5871a1b90af5fe419379a1d2f64b0c15	feat: add lens vuln-audit command (Dependabot alerts) Reports open Dependabot alerts by severity per project, and separately flags repos where Dependabot alerts are disabled entirely (which reads as "0 vulnerabilities" but actually means "unknown" - important not to conflate the two). Hit a real gh CLI gotcha along the way: passing state=open via `-f` makes gh silently switch the request to POST (since -f/-F imply a write), which 404s against this GET-only endpoint. Fixed by putting it in the URL query string instead, with a regression test. Live run against the 27-project registry: 18 clean, 9 with Dependabot disabled, 0 open vulnerabilities.
2026-08-28T05:54:41Z	fac01e42890cf4486a795f1e6eb1fcd42260c58e	https://github.com/shinkeonkim/project-lens/commit/fac01e42890cf4486a795f1e6eb1fcd42260c58e	feat: add lens license-audit command Uses GitHub's dedicated license-detection endpoint (repos/{owner}/{repo}/license) rather than guessing from file presence - it returns 404 when GitHub can't detect a LICENSE file at all, and the detected license name otherwise. Run live: 26 of 27 tracked projects have no LICENSE file (only codekr does, MIT).
2026-08-28T05:53:17Z	b21e4e2456984695036f8433ebd43f0454021a78	https://github.com/shinkeonkim/project-lens/commit/b21e4e2456984695036f8433ebd43f0454021a78	fix: punycode-encode unicode hostnames before health check request site_url values use the human-readable Korean domain form (cache-fridge.코드.kr), but urllib doesn't auto-convert non-ASCII hostnames like a browser does — every unicode-domain site health-checked as "down" with a latin-1 encoding error, even though they're all live. IDNA-encode the hostname before building the request.
2026-08-28T05:52:14Z	63ce27a408f9fd23076f8be39de0e556f3d72e24	https://github.com/shinkeonkim/project-lens/commit/63ce27a408f9fd23076f8be39de0e556f3d72e24	feat: add site health check to dashboard Distinguishes "no traffic" from "site is actually down" — several cards show 0 GA4 visitors and it wasn't clear which case that was. Adds a stdlib-only (urllib) HTTP reachability check per project's site_url, parallelized alongside the existing PR-state/GA4 fetch. Renders a 정상/응답없음 badge next to the site link and a "사이트 응답 없음" summary stat.
2026-08-28T05:41:53Z	72ab5db3e93e727d8a87ec50f3770543afa92f1d	https://github.com/shinkeonkim/project-lens/commit/72ab5db3e93e727d8a87ec50f3770543afa92f1d	docs: rewrite /lens-add-project for current capabilities The command still said GA4/GTM automation "isn't supported yet" from back when project-lens was Phase 0 (metadata-only registration). That's been wrong for most of this session — rewrite to reflect the real add -> optional immediate /lens-sync flow, and carry forward the site_url cross-verification lesson (don't trust GitHub's homepageUrl blindly, it's been stale before).
2026-08-28T05:25:26Z	e9f71db2059de9a085a104bd7098acfd6f41c6e2	https://github.com/shinkeonkim/project-lens/commit/e9f71db2059de9a085a104bd7098acfd6f41c6e2	feat: link project names to GitHub in dashboard cards and GA4 table The GitHub link existed only as small text at the bottom of each card, easy to miss. Make the card title and the GA4 summary table's project name both link directly to github_url.
2026-08-28T04:18:16Z	bcf0f469a2e62d0dffaa6935c024d91f37530b65	https://github.com/shinkeonkim/project-lens/commit/bcf0f469a2e62d0dffaa6935c024d91f37530b65	feat: add lens readme-audit command Checks every registered project's README via `gh api repos/.../readme` and classifies it as missing / scaffold-default / thin / ok. The "scaffold-default" detection exists because qr-gen was found this session still shipping SvelteKit's stock README verbatim — length alone wouldn't have caught that. Run live against the real registry: 2 missing, 1 scaffold-default (qr-gen, as expected), 9 thin, out of 27. Drafting an actual replacement README for a flagged project is left as a conversational request ("<slug> README 제안해줘") rather than a rigid template generator, since good README content needs judgment a fixed CLI command can't provide.
2026-08-28T04:07:33Z	858bda3f4430b6e58054877de54f6f83631b40fc	https://github.com/shinkeonkim/project-lens/commit/858bda3f4430b6e58054877de54f6f83631b40fc	perf: parallelize dashboard data fetch, add in-page refresh (--serve) Dashboard generation was sequential per-project (gh CLI subprocess + GA4 API call each), taking noticeably longer as the registry grew to 27 projects. Extract the fetch into dashboard_data.collect_dashboard_rows() and parallelize the slow I/O (PR state + GA4 report) with a thread pool, keeping SQLite reads sequential since connections aren't thread-safe. ~6x wall-clock improvement in practice. Add `lens dashboard --serve [--port]`: a stdlib-only local HTTP server (127.0.0.1 only, no new dependencies) serving the dashboard with a "새로고침" button that POSTs /refresh and reloads in place, instead of needing to go back to the terminal and re-run the command.
2026-08-28T03:20:00Z	fbb771d8bc6a3e8114322b53653b6b989305c4d8	https://github.com/shinkeonkim/project-lens/commit/fbb771d8bc6a3e8114322b53653b6b989305c4d8	fix: don't set NEXT_PUBLIC_GTM_ID variable when codekr wiring wasn't used track sync's _handle_new_change() calls adapter.configure_remote() whenever the adapter defines it at all, regardless of which injection strategy actually ran. For aws-study-site this meant the generic Next.js fallback (ID hardcoded directly in layout.tsx) still triggered OhMyHomelabAdapter.configure_remote(), setting a NEXT_PUBLIC_GTM_ID GitHub Actions variable that nothing in the repo reads. Pass change_set.changed_files through to configure_remote() and skip the gh variable set unless the codekr-specific component file is among them. Manually deleted the stray variable already set on kokoa-study-room/aws-study-site.
2026-08-28T03:18:01Z	cd6607f8190c1e2fc20845a8551d0a8d9d7d9650	https://github.com/shinkeonkim/project-lens/commit/cd6607f8190c1e2fc20845a8551d0a8d9d7d9650	feat: add generic Next.js App Router GTM injection strategy kokoa-study-room/aws-study-site is Next.js App Router but doesn't match codekr's exact anchor strings (different import/className conventions), so OhMyHomelabAdapter would have fallen through to the manual-fix issue like base64-code/qr-gen did before. Add a JSX-safe generic strategy (dangerouslySetInnerHTML avoids JSX interpreting GTM's literal {{ }}) that finds any src/app/layout.tsx-style root layout and injects there. Wired in two places: - _static_site.py's shared dispatch, so any adapter (Cloudflare/Vercel/ GitHub Pages) benefits for Next.js App Router projects, not just homelab. - OhMyHomelabAdapter, as a fallback used only when the repo doesn't even have codekr's required file paths (Dockerfile/CI workflow/.env.example). A codekr-shaped repo with a merely-drifted anchor still returns None (issue fallback) rather than silently hardcoding the ID, since codekr specifically needs the CI-variable build-arg wiring (shared image across public/internal deploys) that hardcoding would break.
2026-08-27T17:04:55Z	cdd3a547769c55c1e76d115d300822498e388524	https://github.com/shinkeonkim/project-lens/commit/cdd3a547769c55c1e76d115d300822498e388524	feat: batch-track 12 more Cloudflare repos, refresh PR doc, add GA4 summary - Registered and synced base64-code, qr-gen, and 10 kokoa-lab repos (already deployed to Cloudflare Pages/Workers) via lens project add + lens track sync, using site URLs confirmed from wrangler config/README/ live domain probing. - Refreshed docs/TRACKED_PROJECTS_PR_REVIEW.md to cover all 26 tracked projects with real PR merge state and GA4 7-day figures. - Dashboard: add a GA4 summary table (sorted by traffic) plus aggregate 7-day visitor/session totals, so reviewing many separate GA4 properties no longer requires opening each console individually.
2026-08-27T16:59:22Z	97ce9b7f11d4a14e3d67c2b806a14aebdda54e69	https://github.com/shinkeonkim/project-lens/commit/97ce9b7f11d4a14e3d67c2b806a14aebdda54e69	fix: recognize SvelteKit src/app.html as GTM insertion point base64-code/qr-gen(SvelteKit) had no index.html at any known candidate path, so the adapter fell back to opening a manual-fix issue instead of a PR. Add src/app.html and app.html as known candidates, plus a generic rglob fallback (skipping node_modules/dist/build/etc.) that picks the first *.html with both <head> and <body> tags — so future frameworks with a non-standard entry path degrade gracefully instead of failing.
2026-08-27T16:45:58Z	c95f7cbb948e03d39e65839e000404c375aef2aa	https://github.com/shinkeonkim/project-lens/commit/c95f7cbb948e03d39e65839e000404c375aef2aa	feat: add PR review doc, local dashboard, fix stale-PR-link bug - docs/REPO_INVENTORY.md: full survey of all 231 repos across shinkeonkim/kokoa-lab/kokoa-study-room (via a forked research agent), annotated by the user into deploy/redeploy categories. - docs/TRACKED_PROJECTS_PR_REVIEW.md: reviewer-facing snapshot of all 14 tracked projects with real PR links, merge status, and GA4/GTM IDs (GTM public IDs fetched live via the API rather than guessed). - lens dashboard: generates a self-contained local HTML page (~/.project-lens/dashboard.html) summarizing every tracked project - status, PR state, live 7-day GA4 numbers where available. Opens in the browser by default (--no-open to skip, --offline to skip GA4 calls). No cloud involved, matching the "secrets and state stay local" principle already used for reports. Building the dashboard surfaced a real bug: get_latest_run() picks the most recent run regardless of type, so once `lens track report-all` added report-type runs (no pr_url) after the sync runs that created PRs, both `lens project show` and the new dashboard started showing "no PR" for projects that very much have one open. Added get_latest_pr_run() (most recent run WITH a pr_url) and used it specifically for PR display in both places, while leaving get_latest_run() for "what happened most recently" status. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T09:00:03Z	70499ce04885357c62aed22960c0789d44b15d9e	https://github.com/shinkeonkim/project-lens/commit/70499ce04885357c62aed22960c0789d44b15d9e	docs: confirm live GA4 reporting works end-to-end Google Analytics Data API was in fact already enabled on the project-lens-506802 GCP project - the earlier SERVICE_DISABLED error was propagation delay, not a real misconfiguration. Retried and confirmed real data flows through the whole path: lens track report returns actual GA4 numbers, lens track report-all aggregates across all 14 projects, and the launchd weekly schedule produces a complete, successful report (not just reaching Google's API as before). Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T08:44:09Z	30920655f8fca9aceb0c4dccb3c37f6d8893013d	https://github.com/shinkeonkim/project-lens/commit/30920655f8fca9aceb0c4dccb3c37f6d8893013d	feat: schedule weekly GA4 reports via local launchd (Phase 6) The schedule skill only creates cloud routines, which run in isolated sandboxes with no access to this Mac's OS keychain or local SQLite registry - both load-bearing for project-lens's "secrets stay local" design. A cloud routine would fail auth on every single run. Used a local launchd LaunchAgent instead, which runs in the user's session and does have keychain access (and survives sleep better than cron, since launchd catches up on missed runs after wake). com.project-lens.weekly-report.plist runs `lens track report-all` every Monday 9am local time, logging to ~/.project-lens/logs/weekly-report.{log,err.log}. Source of truth is scripts/launchd/ (version controlled); installed copy goes in ~/Library/LaunchAgents/. Verified for real via `launchctl kickstart -k`: the run reached actual Google API calls (not an AuthError), confirming headless keychain access works without an interactive unlock prompt - the main risk with this approach. Report content itself couldn't be verified yet since GA4 Data API activation was still pending at the time. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T08:31:53Z	70c1e25e18cebfb6931911471c7839c1e29709c7	https://github.com/shinkeonkim/project-lens/commit/70c1e25e18cebfb6931911471c7839c1e29709c7	feat: add lens track report-all for scheduled digests (Phase 6) Loops over every registered project with a GA4 property, fetches its report, continues past individual failures instead of aborting the batch, and writes the combined text to ~/.project-lens/reports/<date>.txt in addition to stdout — needed for non-interactive runs (launchd/cron) where there's no terminal to read. Each project's fetch still gets its own deploy_runs row, same as the single-project command. Refactored the report-formatting logic out of track_report into a shared _fetch_project_report_lines() so both commands produce identical per-project output. Also added errors.short(exc): GoogleAPICallError/GoogleAdsException stringify to multi-line gRPC metadata dumps, which is unreadable when report-all shows one line per failed project. Applied it to every GoogleAPIError-wrapping raise across ga4.py/gtm.py/ads.py/ ga4_reporting.py. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T08:13:23Z	2ef6edc316cf5c8867f82a02d165170f79410b52	https://github.com/shinkeonkim/project-lens/commit/2ef6edc316cf5c8867f82a02d165170f79410b52	feat: add Vercel and GitHub Pages adapters (Phase 6) Extracted the static-site injection logic (plain HTML, Docusaurus, Astro Starlight) that CloudflareWorkersAdapter had into a shared module (adapters/_static_site.py), since "how to find the deploy target" and "how to inject once you've found it" are independent concerns. VercelAdapter and GitHubPagesAdapter now reuse the same injection code and only implement their own detect()/project-root logic (vercel.json or a vercel package.json dep; CNAME or a Pages deploy workflow, trying repo root then docs/). No project currently deploys via either target - built ahead of need per explicit request. Framework-specific cases that need real code/CI changes (like the Next.js codekr adapter) aren't generalized here; those get built per-repo when a real target shows up, same as OhMyHomelabAdapter was. migrations/0005: SQLite can't ALTER a CHECK constraint, so this rebuilds the projects table to add 'vercel'/'github_pages' to deployment_type. Rebuilding a table that deploy_runs/tracking_configs reference by foreign key fails with FOREIGN KEY constraint failed unless foreign_keys is toggled off for the rebuild - caught this by testing against a copy of the real registry (14 projects, 20 runs) before applying it live; verified with PRAGMA foreign_key_check afterward. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T08:07:45Z	67d3514062c8516be9d73d8208ea8c128c37ab65	https://github.com/shinkeonkim/project-lens/commit/67d3514062c8516be9d73d8208ea8c128c37ab65	feat: implement lens track report (Phase 3/6 GA4+Ads reporting) google/ga4_reporting.py wraps the GA4 Data API (a separate service from the Admin API used elsewhere) via BetaAnalyticsDataClient, returning visitors/sessions/pageviews/bounce-rate/avg-session-duration for a date range. google/ads.py gains run_summary_report (GAQL query against the customer resource) for impressions/clicks/cost/conversions, tested the same way as the rest of ads.py: against the real installed v25 types, not guesses. lens track report <slug> [--range 7d|30d] is read-only (no --yes needed), prints the GA4 summary always and the Ads summary when the project has ads_customer_id set and a developer token is available. Live-testing against the real my-portfolio property surfaced two more real gaps (same pattern as the GTM containerversions scope bug): GA4 Data API needs its own analytics.readonly OAuth scope beyond analytics.edit, and separately needs to be enabled in the GCP project itself (SERVICE_DISABLED otherwise) - not just an OAuth scope issue. Both are now in SCOPES and documented in SECURITY.md/TROUBLESHOOTING.md so the next new Google API doesn't repeat the same debugging cycle. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T05:16:14Z	3053a8368b5ecdf0448d10eade2369190183eb7f	https://github.com/shinkeonkim/project-lens/commit/3053a8368b5ecdf0448d10eade2369190183eb7f	feat: support multiple GA4/GTM/Ads account profiles (Phase 6) settings.py now stores named AccountProfile entries instead of one global account set, with an org_profile_map (github_org -> profile name) and a default_profile fallback. Old flat settings.json files migrate transparently on load (real one on disk verified: still resolves to the same account under the "default" profile). - lens creds set-accounts --profile <name> creates/updates a profile - lens creds map-org <github_org> <profile> routes a GitHub org's projects to a non-default profile - lens creds check lists all profiles and org mappings - _provision_tracking and track_link_ads resolve the account profile from the project's github_org instead of a single global setting No current project needs a second profile (everything uses one GA4/ GTM account today), but this was requested explicitly to support separating personal vs. lab accounts later without a schema migration when that need shows up. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T05:12:49Z	8da9fabe8609edaac3c1e35a1c0f3c5481101bc4	https://github.com/shinkeonkim/project-lens/commit/8da9fabe8609edaac3c1e35a1c0f3c5481101bc4	fix: mark deploy_runs failed on any exception, not just LensError Batch-syncing all 12 remaining projects surfaced two runs stuck at status='running' forever: a raw TypeError from the Google client library and a google.auth RefreshError, neither of which is a LensError subclass, so the except clause in track_sync/link-ads never caught them and finish_run() was never called. Broadened both to except Exception so any failure closes out the run record, and added a regression test. Retroactively closed the two orphaned rows in the real registry. Also add docs/GOOGLE_ADS_GUIDE.md: a step-by-step manual for Developer Token application, re-authentication for the adwords scope, and using lens track link-ads, separate from SECURITY.md's denser reference. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T04:46:57Z	f24638081ec82dd5c6b4847b42664ca0a2c4d37a	https://github.com/shinkeonkim/project-lens/commit/f24638081ec82dd5c6b4847b42664ca0a2c4d37a	docs: mark Phase 5 complete in ROADMAP.md Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T04:46:35Z	a04b3428ab9f7d323f4a1f42c62173b32773f257	https://github.com/shinkeonkim/project-lens/commit/a04b3428ab9f7d323f4a1f42c62173b32773f257	feat: finish Phase 5 — troubleshooting guide, logs command, docs refresh - docs/TROUBLESHOOTING.md: one section per error type (AuthError, RepoAccessError, AdapterDetectionError, GoogleAPIError, DeployError, ValidationError) with causes and fixes, written from the actual incidents hit while building this (OAuth scope drift needing re-auth, the GA4 list_properties() and GTM containerversions scope bugs found during live testing). - lens logs show <run_id>: prints a deploy_runs record and, on failure, a short hint keyed by error_code pointing at TROUBLESHOOTING.md. - lens project show now also prints the project's most recent run (PR/issue link on success, error code + summary on failure) instead of only registry metadata. - Added the lens-logs slash command (was missing) and refreshed lens-status.md to describe what's actually there now. - README: current-status section with real PR links, a quick-start block, and a TROUBLESHOOTING.md link. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T04:43:02Z	c302672bd5e3a8730cf137279b5c157d26071a2f	https://github.com/shinkeonkim/project-lens/commit/c302672bd5e3a8730cf137279b5c157d26071a2f	feat: extend Cloudflare adapter for Docusaurus/Starlight, reach 100% coverage A full dry-run sweep across all 14 registered repos (Phase 5's coverage check) surfaced two real gaps: - compiler-study-site is genuinely Cloudflare Workers (wrangler.jsonc), but the site lives under site/ and CloudflareWorkersAdapter.detect() only looked at the repo root. Now searches one subdirectory level down too (monorepo-style layouts). - compiler-study-site (Docusaurus) and terraform-associate-004-study-notes (Astro Starlight) have no static index.html in source — it's a build output. Added dedicated injection strategies for both, using each framework's own official GTM mechanism instead of guessing at HTML: Docusaurus's classic-preset `googleTagManager` option, and Starlight's `head` config array. Both verified against the real repos: cloned, patched, ran the actual build (`bun run build`), and confirmed the GTM snippet shows up in the generated HTML output — not just that the source patch applies cleanly. All 14 registered repos now have a working injection path (11 plain HTML, 1 Docusaurus, 1 Starlight, 1 Next.js/oh-my-homelab from the previous commit). Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T04:33:16Z	352777f23e6792c4451ed22b63b63edb7500737a	https://github.com/shinkeonkim/project-lens/commit/352777f23e6792c4451ed22b63b63edb7500737a	feat: add oh-my-homelab adapter (Phase 4), verified live on codekr Investigated shinkeonkim/oh-my-homelab and shinkeonkim/codekr: codekr is not a static site but a Next.js (App Router) web app + Go/Kotlin backend services, deployed to Kubernetes via ArgoCD GitOps. That's a different shape of problem than the Cloudflare Workers adapter — GTM injection means adding a real feature to the target repo's own Next.js code, not inserting a snippet into an existing static file. OhMyHomelabAdapter (src/project_lens/adapters/oh_my_homelab.py): - detect() matches repos with a deploy/charts/ directory (the service-dev-guide convention oh-my-homelab documents) - inject_tracking() is an exact-string-anchor patch matched against codekr's actual file contents (not a generic regex pattern like the Cloudflare adapter) — adds a GoogleTagManager component, wires it into layout.tsx, and threads NEXT_PUBLIC_GTM_ID through Dockerfile/.env.example/CI build-args. Falls back to None (issue creation) if any anchor doesn't match, same safety net as the Cloudflare adapter. - GTM ID is never hardcoded into any file: Next.js bakes NEXT_PUBLIC_* at build time, so the value is delivered via a GitHub Actions repo variable instead. configure_remote() sets it via `gh variable set` — deliberately gated to the --yes path only, since dry-run must never touch real GitHub state. - oh-my-homelab itself (private, ArgoCD selfHeal: true — merges apply to the live cluster immediately) is never touched. All risk stays inside the public codekr repo, behind a normal PR. Verified for real: cloned codekr, applied the patch, and ran bun install/lint/typecheck/build/test locally (0 lint errors, 47 routes built, 184 existing tests passing) before running `lens track sync shinkeonkim-codekr --yes`, which created a real GA4 property (G-NZE9ZMZRZD), GTM container (GTM-TP9LLDRJ), set the NEXT_PUBLIC_GTM_ID repo variable, and opened codekr#667. Real GitHub CI on that PR passed the same checks. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T03:54:16Z	6d6bb8cba7a44066b0f383d62778884463309a57	https://github.com/shinkeonkim/project-lens/commit/6d6bb8cba7a44066b0f383d62778884463309a57	feat: add Google Ads linking (Phase 3) and missing slash commands - google/ads.py: find-or-create conversion actions via the Ads API, built against the installed google-ads v25 package's real types (client.get_type()/.enums can't be verified without live credentials, so this imports the concrete message/enum types directly instead) - google/ga4.py: ensure_google_ads_link links a GA4 property to a Google Ads customer_id (GA4 Admin API only, no Developer Token needed) - auth.py: store/load the Ads Developer Token via keyring, add the adwords OAuth scope (existing users must re-run `creds init --provider google` to pick it up) - lens creds init --provider google-ads, lens track link-ads (dry-run by default), tracking_configs gains ads_customer_id / ads_conversion_action_ids - Add the lens-sync, lens-creds-setup, lens-link-ads slash commands that were missing from Phase 1-2 - Update docs to match reality: COMMANDS.md now marks what's actually implemented, DATA_MODEL.md notes credentials_meta was replaced by keyring + settings.json, ROADMAP.md Phase 3 checklist Live verification is still pending — no Google Ads account or Developer Token yet, so this is validated with fake-based unit tests against real API types only, not a live account. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T03:39:25Z	712bc9bb2336b256238c807b56a7862faf0c92e1	https://github.com/shinkeonkim/project-lens/commit/712bc9bb2336b256238c807b56a7862faf0c92e1	feat: add Claude Code slash commands wrapping the lens CLI lens-add-project, lens-list, lens-status — thin wrappers that keep logic in the Python CLI and only translate/summarize for the user. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T03:39:19Z	18256dd91709f6efd3763ee3ddcc03eaa686b846	https://github.com/shinkeonkim/project-lens/commit/18256dd91709f6efd3763ee3ddcc03eaa686b846	feat: implement project registry and GA4/GTM automation CLI (Phase 0-2) - SQLite-backed project registry (projects, deploy_runs, tracking_configs) with idempotent upserts and migrations - GitHub integration via gh CLI: repo lookup, clone, branch/commit/ push/PR/issue creation - Workspace manager that clones into a temp dir and always cleans up - Cloudflare Workers adapter: detects wrangler projects, injects GTM snippets into HTML entry points, falls back to a GitHub issue when the injection point can't be found - Google OAuth (keyring-backed), GA4 Admin API and Tag Manager API v2 clients that find-or-create properties/streams/containers/tags and publish workspaces - `lens` CLI: project add/list/show/set-site-url, creds init/check/ accounts/set-accounts, track sync (dry-run by default, --yes to actually create GA4/GTM resources and open a PR) - Live-verified end-to-end against shinkeonkim/my-portfolio Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
2026-08-27T03:39:08Z	71c00a78a29b2f77876bd9ca17942a0d2173080f	https://github.com/shinkeonkim/project-lens/commit/71c00a78a29b2f77876bd9ca17942a0d2173080f	docs: add project-lens planning and design docs Architecture, SQLite data model, Claude Code command spec, deployment adapter design, credential handling manual, and phased rollout plan for a tool that registers, tracks, and configures GA4/GTM/Ads across many website repos. Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
