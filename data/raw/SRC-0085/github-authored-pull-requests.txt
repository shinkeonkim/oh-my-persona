GitHub repository: https://github.com/kmu-aws-capstone-team-4/voice-api
Pull request author: shinkeonkim

2026-04-27T14:39:51Z	#2	CLOSED	https://github.com/kmu-aws-capstone-team-4/voice-api/pull/2	feat(stt): faster-whisper 기반 STT 라우트 추가 (PR-5)	merged=	closed=2026-04-29T07:02:19Z	commits=8	files=1	+1/-1	## 배경 backend (PR-5, kmu-aws-capstone-team-4/backend#153) 의 답변 단위 STT 폴백 경로 (\`TranscribeRecordingTask\`) 가 호출하는 voice-api 엔드포인트를 신규 추가한다. Plan: backend repo \`.sisyphus/plans/interview-resilience-plan.md\` Section 6.1 (Gate-0), 6.2 ## Gate-0 결과 - \`faster-whisper\` 의존성 추가 → uv sync 성공 - Python 3.14 환경에서 \`from faster_whisper import WhisperModel\` import 통과 (smoke test OK) - 별도 Python 다운그레이드 불필요 ## 변경 요약 ### POST /api/v1/stt - 요청: \`{audio_url, language, prompt?}\` - 응답: \`{language, duration_ms, text, segments: [{start_ms, end_ms, text, confidence}]}\` - 인증: 기존 Bearer (\`verify_token\`) - 포화 시 \`503 Service Unavailable\` + \`Retry-After: 30\` 헤더 ### Service (\`app/services/stt.py\`) - \`WhisperModel\` 싱글톤 (\`asyncio.Lock\` 보호) — \`small\` / \`cpu\` / \`int8\` default - \`asyncio.Semaphore(1)\` 추론 큐 — per-instance 1 동시 추론 (모델 메모리 중복 방지). 포화 시 \`SttSaturatedError\` → 503 - \`audio_url\` 다운로드 → \`asyncio.to_thread\` 로 동기 \`transcribe\` 호출 → 임시 파일 정리 ### Schemas - \`SttRequest\` / \`SttResponse\` / \`SttSegment\` Pydantic 모델 (\`app/api/schemas/stt.py\`) ### 환경 변수 (선택) - \`STT_MODEL_SIZE\` (default \`small\`) - \`STT_DEVICE\` (default \`cpu\`) - \`STT_COMPUTE_TYPE\` (default \`int8\`) - \`STT_AUDIO_DOWNLOAD_TIMEOUT\` (default 60) ## uvicorn 설정 (운영 권고) - \`--workers 1\` (모델 메모리 중복 방지) - 스케일은 replica 수평 확장 ## Note — 테스트 인프라 voice-api 에는 기존에 tests 디렉토리 / pytest 인프라가 없다. 본 PR 은 Gate-0 + 라우트 코드만 도입하고, 테스트 인프라 도입 (pytest 추가 + tests 디렉토리 + 첫 테스트) 은 후속 PR 로 분리한다. backend (#153) 단위 테스트는 voice-api mock 으로 검증되어 있다. ## Acceptance Criteria - [x] \`faster-whisper\` 의존성 추가 + 호환성 확인 - [x] \`POST /api/v1/stt\` 엔드포인트 + Pydantic 스키마 - [x] \`asyncio.Semaphore(1)\` 큐 + 포화 시 503 + Retry-After - [x] 인증 (Bearer) 적용 - [ ] 단위 테스트 — 후속 PR (테스트 인프라 도입 시) Closes #1
