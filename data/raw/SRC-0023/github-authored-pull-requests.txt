GitHub repository: https://github.com/shinkeonkim/boardai
Pull request author: shinkeonkim

2026-07-21T01:06:37Z	#8	OPEN	https://github.com/shinkeonkim/boardai/pull/8	feat(protocol): M3 device bridge foundation - handshake + WebSocket + discovery contract	merged=	closed=	commits=6	files=13	+424/-11	## Summary Introduces the device bridge foundation for the camera-phone ↔ app-phone channel: an HMAC-SHA256 handshake, a shelf_web_socket server on the app side, and a phase-tracking client for the camera side. ## Commits - \`feat(protocol)\` — HMAC-SHA256 handshake (challenge/response with shared secret) - \`feat(protocol)\` — ServiceDiscovery contract + NoopServiceDiscovery (mDNS impl pending) - \`feat(protocol)\` — BridgeServer (shelf_web_socket + handshake) + BridgeClient (phase state machine) - \`test(protocol)\` — handshake unit tests + end-to-end pairing round trip on 127.0.0.1 ## What works - \`BridgeServer.start(port: 0)\` binds a WebSocket handler, emits a challenge on connect, and only admits clients whose HMAC over \`sessionId:nonce\` matches the shared secret. - \`BridgeClient.connect()\` walks through \`connecting → awaitingChallenge → authenticating → ready | failed\` phases, exposed via \`phaseStream\` for UI wiring. - End-to-end integration test on loopback demonstrates a full round-trip success and a mismatched-secret rejection. ## What's next (M3 remaining) - \`ServiceDiscovery\` real impl (multicast_dns / bonsoir bindings) once app UX picks a discovery library - QR payload encoding/parsing in the mobile app (existing \`PairingPayload\` already covers the shape) - Video channel multiplexing on top of the authenticated WebSocket (H.264 / MJPEG frames) - Reconnection + heartbeat handling ## Verification - \`melos run analyze\` clean (10 packages) - \`melos run test\` — 6 new protocol tests pass; entire suite green (splendor 13, mobile 2, persistence 2, protocol 6, others 1 each) ## Stacked PR chain **8/8** — base is \`shinkeonkim/vision-m2\`. Closes the initial + M2 + M3 stack. ## Milestone M3.1 + M3.3 (partial). M3.2 mDNS discovery + M3.4 reconnection are follow-ups.
2026-07-21T01:00:48Z	#7	OPEN	https://github.com/shinkeonkim/boardai/pull/7	feat(vision): M2 heuristic perception + web-friendly mobile bootstrap	merged=	closed=	commits=9	files=17	+4610/-30	## Summary Introduces the first working perception path across two surfaces: 1. **Dart (games/splendor)** — a heuristic recognizer that classifies detected regions into `token/card/noble` by shape (aspect ratio + relative area) and gems by HSV color, replacing the previous stub. Consumed by the Perception-Only demo harness and any device bridge that hands frames to the InferenceService interface. 2. **Python spike (spikes/splendor_card_vision)** — an OpenCV pipeline (Canny → contours → shape/color classification) that runs directly on \`private-assets/\` reference images via \`scv perceive summary/annotate\`, plus proof-of-life detections on real photos. Also enables Flutter web preview by adding a conditional session store bootstrap so the app compiles for browser targets (drift native replaced by an in-memory store). ## Commits Mobile web support: - \`feat(mobile)\` — web-friendly session store bootstrap for browser preview - \`fix(mobile)\` — wrap conditional early-returns in blocks for lint compliance Splendor perception: - \`feat(splendor/perception)\` — HSV color swatches + classifier (5 gems + gold + black/white) - \`feat(splendor/perception)\` — heuristic shape+color recognizer replaces stub - \`test(splendor/perception)\` — color and shape classification unit tests Python spike perception: - \`feat(spike/perception)\` — OpenCV pipeline (detect_regions + classify_regions + annotate) - \`feat(spike/cli)\` — scv perceive summary + annotate commands on reference images - \`test(spike/perception)\` — color/shape/detection tests - \`chore(spike)\` — commit uv.lock ## Verification **Dart:** - \`melos run analyze\` clean (10 packages) - \`melos run test\` — 13+ splendor tests including new color+shape recognizer tests - \`flutter build web --release\` succeeds (40MB static bundle in \`apps/mobile/build/web/\`) **Python spike (real reference images):** \`\`\` $ uv run scv perceive summary ── 1, 2, 3 단계 카드의 앞면과 뒷면 구성.jpeg card 2 · token:green 2 · unknown 19 ── 토큰과 귀족.jpeg card 3 · noble 1 · unknown 20 $ uv run scv perceive annotate wrote experiments/annotations/annotated_1, 2, 3 단계...jpg (23 regions) wrote experiments/annotations/annotated_토큰과 귀족.jpg (24 regions) \`\`\` The heuristic pipeline correctly separates card-shaped, noble-shaped, and token-shaped contours; token color is coarse but functional for the 6 canonical gems. ## Known limitations (tracked in docs/11-roadmap.md M2) - Only global thresholds; no per-scene calibration - Card level (L1/L2/L3) not derived from backside color yet — needs real scans - Noble face classification is bbox-only; content is not read - Many \`unknown\` regions in busy scenes → will be reduced with proper detection model (M2.2) ## Stacked PR chain **7/8** — base is \`shinkeonkim/spike-splendor-cards\`. ## Milestone M2 (partial) — heuristic perception pipeline foundation.
2026-07-20T12:36:43Z	#6	OPEN	https://github.com/shinkeonkim/boardai/pull/6	feat(spike): splendor card vision spike bootstrap (M0.5)	merged=	closed=	commits=3	files=12	+270/-0	## Summary Bootstraps the standalone Splendor card recognition spike as a completely independent Python subproject (uv-managed). This is the technical risk that gates M2 vision work. ## Layout \`\`\` spikes/splendor_card_vision/ pyproject.toml # uv + typer + opencv + torch (optional) README.md # goals, quickstart, mapping to docs/12 REPORT.md # M0.5 gate template (updated as work progresses) src/splendor_card_vision/ cli.py # scv assets probe / scv assets preview common/ # asset discovery approach_a/ # ID matching + lookup (empty placeholder) approach_b/ # per-field recognition (empty placeholder) tests/ # asset discovery unit tests notebooks/ # experiment notebooks \`\`\` ## Verification - \`uv sync\` succeeds - \`uv run pytest -q\` passes (asset discovery tests) - \`uv run scv assets probe\` detects the two reference images in \`private-assets/\` ## Stacked PR chain **6/6** — base is \`shinkeonkim/mobile-app\`. This closes the initial bootstrap chain. ## Milestone M0.5 — Splendor Card Vision Spike (bootstrap phase). Approach A/B implementation is a separate spike PR once actual card scans arrive.
2026-07-20T12:35:56Z	#5	OPEN	https://github.com/shinkeonkim/boardai/pull/5	feat(mobile): full app - home, setup, in-game with AI + SQLite persistence (M1.6 + M1.7)	merged=	closed=	commits=6	files=132	+5490/-0	## Summary Implements the user-facing Flutter app on top of the game plugin, wiring up game registry, session lifecycle, and SQLite persistence. ## Commits - \`chore(mobile)\` — flutter app skeleton + platform files (android/ios/linux/macos/web/windows) - \`feat(mobile)\` — home screen with registered plugins + drift bootstrap - \`feat(mobile)\` — SessionCoordinator (Rules + AI + persistence orchestration + ChangeNotifier) - \`feat(mobile)\` — SetupScreen (seat count / human vs AI / personality + difficulty) + InGameScreen (BoardView + score chips + human legalMoves list + AI GuidanceCard + terminal panel) - \`test(mobile)\` — home smoke + AI-only session coordinator round-trip verifying store persistence ## Flow Home → tap game → Setup → SessionConfig → InGame: - Human turn: legal moves as ListTiles → tap to apply - AI turn: coordinator auto-runs advanceAi(), shows GuidanceCard, user confirms - Every applied move persists snapshotJson + appendEvent to drift ## Verification - \`melos run analyze\` clean - \`melos run test\` — home widget renders \`테스트 게임\` chip; session coordinator applies 3 AI moves and persists snapshotSeq=3 ## Stacked PR chain **5/6** — base is \`shinkeonkim/splendor-game\`. ## Milestone M1.2 (mobile side) + M1.6 (in-game UI) + M1.7 (drift session persistence).
2026-07-20T12:35:08Z	#4	OPEN	https://github.com/shinkeonkim/boardai/pull/4	feat(splendor): full game package - rules, AI, plugin, demo harness (M1.2 + M1.3 + M1.4 + M1.5)	merged=	closed=	commits=7	files=152	+7125/-0	## Summary Introduces the Splendor game plugin. Includes complete game rules, heuristic + MCTS AI, plugin registration, and a standalone 3-mode demo harness. ## Commits - \`feat(splendor/contract)\` — TokenColor, CardInfo, NobleInfo, SplendorState, SplendorMove + synthetic 90-card / 10-noble catalog (copyright note: real card data replaces this later) - \`feat(splendor/rules+ui)\` — Rules.legalMoves/apply/isTerminal/summarizeMove, BoardView, GuidanceCard, SplendorFakeRecognizer - \`feat(splendor/ai)\` — Evaluator (8 dimensions x 3 personalities), Heuristic 1-ply AI, MCTS AI with per-difficulty tuning (medium/hard/expert) - \`feat(splendor/plugin)\` — SplendorPlugin implements GamePlugin, loads manifest via rootBundle - \`test(splendor)\` — 13 tests: 9 rules unit + 2 self-play (100 games, illegal=0) + 2 AI strength (heur > random, MCTS ≥ heur) - \`feat(splendor/example)\` — Perception-Only (camera + fake recognizer overlay), Decision-Only (3-AI self-play log), Interaction-Only (widget storybook) - \`chore(splendor)\` — flutter package metadata + iOS/Android camera permissions in the example harness ## Verification - \`melos run analyze\` clean (games/splendor) - \`melos run test\` — 13 passing, 100-game self-play with illegal=0 - \`melos run demo:splendor\` runs the example harness on physical devices ## Stacked PR chain **4/6** — base is \`shinkeonkim/core-packages\`. ## Milestone M1.2 (partial) + M1.3 (rules) + M1.4 (heuristic AI) + M1.5 (MCTS).
2026-07-20T12:34:09Z	#3	OPEN	https://github.com/shinkeonkim/boardai/pull/3	feat(core): shared contract, orchestrator, AI, perception, protocol, persistence, tools packages	merged=	closed=	commits=4	files=57	+3168/-0	## Summary Introduces the seven core Dart/Flutter packages that every game plugin depends on. Each package has: - Its own \`pubspec.yaml\` with minimal dependencies - Skeleton implementation of its role - At least one smoke or unit test ## Packages - \`boardai_contracts\` — Session, Rules, AiPlayer, InferenceService, SessionStore, GamePlugin, JournalEvent (pure Dart, no other boardai deps) - \`boardai_orchestrator\` — SessionOrchestrator scaffold - \`boardai_ai\` — MctsBudget + PersonalityRegistry (game-agnostic) - \`boardai_perception\` — CameraIngest wrapping the \`camera\` plugin + DummyInferenceService - \`boardai_protocol\` — PairingPayload contract (for QR + WebSocket) - \`boardai_persistence\` — InMemorySessionStore + DriftSessionStore + NativeDatabase opener - \`boardai_tools\` — CommandRunner for future simulator/replay CLIs ## Coupling guarantees Only \`boardai_contracts\` is depended on by everyone else; individual packages depend on \`boardai_contracts\` **only** — never on each other. Enforced by pubspec inspection and static analysis. ## Stacked PR chain **3/6** — base is \`shinkeonkim/repo-scaffold\`. ## Milestone M1.1 — Core Packages.
2026-07-20T12:33:37Z	#2	OPEN	https://github.com/shinkeonkim/boardai/pull/2	chore: bootstrap Melos monorepo + CI + repo hygiene (M1.1 infra)	merged=	closed=	commits=4	files=13	+430/-2	## Summary Bootstraps the monorepo infrastructure on top of the initial docs. Adds Melos configuration, root Dart workspace, CI workflows, developer bootstrap script, and repo hygiene files. ## What's included - \`.gitignore\` / \`.gitattributes\` covering Dart, Flutter, Python, ML artifacts, secrets - \`LICENSE\` placeholder (DD-001 unresolved) - \`melos.yaml\` + root \`pubspec.yaml\` (workspace only; melos manages sub-package overrides) - \`analysis_options.yaml\` — strict Dart linter/analyzer config - \`README.md\` — entrypoint with 'quick start' via \`scripts/bootstrap.sh\` - \`scripts/bootstrap.sh\` — auto \`dart pub global activate melos\` + \`melos bootstrap\` + \`uv sync\` - \`.github/workflows/dart.yml\` — analyze + test on macOS runner - \`.github/workflows/spikes.yml\` — Python spike CI on ubuntu runner - \`private-assets/README.md\` + \`private-assets/.gitignore\` — copyrighted local asset folder - \`services/README.md\` — placeholder for future FastAPI subprojects (M4.5, M5.5) ## Stacked PR chain **2/6** — base is \`shinkeonkim/docs-plan-and-tech\`. ## Milestone M1.1 — Repo Bootstrap (infrastructure portion).
2026-07-20T12:33:03Z	#1	OPEN	https://github.com/shinkeonkim/boardai/pull/1	docs: initial planning documents (M0)	merged=	closed=	commits=4	files=16	+3319/-0	## Summary Introduces the initial planning documents for BoardAI. This is the first PR in a stacked series that bootstraps the project. ## What's included Docs under \`docs/\` and the source planning document \`기획안.md\`: - \`README.md\` — documentation index with resolved decisions (D1~D8) - \`01-vision-and-goals.md\`, \`02-requirements.md\` — vision and functional/non-functional requirements - \`03-system-architecture.md\`, \`04-computer-vision.md\`, \`05-ai-player.md\`, \`06-mobile-app.md\`, \`07-multi-device.md\` — system design - \`08-game-framework.md\`, \`09-splendor-mvp.md\`, \`12-splendor-card-vision-spike.md\` — game plugin framework and Splendor spec - \`10-development.md\`, \`11-roadmap.md\`, \`13-ai-server.md\`, \`14-session-server.md\` — process, roadmap, optional servers ## Stacked PR chain This is PR **1/6** in the initial bootstrap chain. Follow-up PRs will introduce infrastructure, packages, splendor game, mobile app, and the card vision spike. ## Milestone M0 Planning & Design.
