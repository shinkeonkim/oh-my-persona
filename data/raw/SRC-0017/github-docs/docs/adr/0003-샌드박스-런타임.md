# ADR-0003. 코드 실행 샌드박스 런타임

- 상태: 채택
- 날짜: 2026-08-10

## 배경

임의의 사용자 코드를 실행해야 한다. 이것은 이 플랫폼에서 가장 위험한 지점이다.
요구사항은 "Golang 및 Containerd 환경을 활용하여 코드 실행 및 채점 환경을 구축한다" 이다.

## 결정

executor 내부에 **`Sandbox` 인터페이스**를 두고 런타임 구현을 교체 가능하게 한다.

```go
type Sandbox interface {
    Run(ctx context.Context, spec RunSpec) (RunResult, error)
}
```

구현체 이름에는 특정 런타임 제품명을 쓰지 않는다 (`containerSandbox`). 운영 환경의 런타임은
**containerd** 이고, 로컬 개발 환경은 같은 OCI 호환 엔진 API 를 노출하는 런타임을 쓴다 —
호출부는 어느 쪽인지 구분하지 않는다.

구현은 `CODEKR_SANDBOX_RUNTIME` 으로 고른다. 이름으로 고르게 한 이유는, 운영 노드의
런타임이 로컬 개발 환경과 다를 수 있는데 그 차이가 **첫 제출에서야** 드러나면 곤란하기
때문이다 — 알 수 없는 이름과 아직 없는 구현은 기동 시점에 실패한다. 기동 시 런타임에
실제로 닿는지도 확인한다(`Preflight`).

| 값 | 상태 |
|---|---|
| `engine-api` (기본) | OCI 호환 엔진 API 클라이언트. 실행기가 런타임 소켓을 마운트해 형제 컨테이너를 띄운다 |
| `containerd` | containerd 클라이언트로 직접 컨테이너를 만든다 (#68) |

**두 구현을 모두 남긴다.** `engine-api` 는 로컬 개발용이다 — macOS 에서 리눅스 VM 없이
쓸 수 있는 것은 그쪽뿐이다. 운영 노드는 `containerd` 를 쓴다.

**검증은 같은 기준으로 한다.** 두 구현은 같은 검사 정의(`internal/selftest`)를 통과해야
하고, 라이브 테스트도 `CODEKR_SANDBOX_RUNTIME` 을 따라 어느 쪽이든 돈다. 구현마다 기준이
다르면 한쪽에서 통과한 검증이 다른 쪽에서 뜻을 잃는다.

그 대신 **어느 구현이든 방어가 걸렸는지 노드에서 직접 확인할 수단**을 먼저 만들었다 —
`executor --self-test` 가 라이브 테스트와 같은 검사 정의(`internal/selftest`)를 돌린다.
구현이 바뀌어도 통과 기준은 아래 표 그대로다.

모든 구현이 지켜야 하는 제약:

| 제약 | 값 |
|---|---|
| 네트워크 | 완전 차단 (`none`) |
| rootfs | 읽기 전용, 작업 디렉터리만 쓰기 가능한 tmpfs |
| 사용자 | non-root 고정 UID (10001) |
| 권한 | 모든 capability drop, `no-new-privileges` |
| 메모리 | 문제별 제한, swap 비활성 (OOM 을 즉시 감지하기 위함) |
| CPU | 1 코어 |
| 프로세스 수 | 128 (fork bomb 방어) |
| 출력 | 64KiB 상한, 초과 시 `OUTPUT_LIMIT_EXCEEDED` |
| 실행 시간 | 문제별 제한 + 컴파일용 별도 상한 |

## 근거

- 인터페이스로 분리하면 로컬 개발 환경과 운영(containerd) 사이의 간극이
  executor 내부 한 파일에 갇힌다. 나머지 코드는 어느 쪽인지 모른다.
- gVisor/Firecracker 같은 더 강한 격리는 홈랩 노드에서 운영 비용이 크다.
  현 단계에서는 cgroup + namespace + capability drop 조합으로 충분하고,
  필요해지면 같은 인터페이스 뒤에서 교체할 수 있다.
  이 선택으로 남는 위험은 [위협 모델](../07_샌드박스_위협모델.md) 4장에 정리했다.

## 감수하는 것

- **런타임 소켓 마운트는 실질적으로 호스트 root 권한이다.** 로컬 개발과
  실행 전용으로 격리된 노드에서만 허용한다. 이 사실을
  [로컬 개발 가이드](../05_로컬_개발_가이드.md)에 경고로 남긴다.
- 컨테이너 생성 오버헤드(수십~수백 ms)가 테스트케이스마다 발생한다.
  MVP 에서는 허용하고, 병목이 되면 케이스들을 한 컨테이너에서 순차 실행하는
  배치 모드를 같은 인터페이스 안에서 추가한다.

## 갱신 (#70)

macOS 에서 **Docker Desktop 없이 containerd 를 띄우고 호스트에서 그 소켓에 직접 붙을 수
있다는 것을 확인했다** (colima `--runtime containerd`). cgroup v2 제한도 실제로 걸린다.

다만 **표준 입출력은 VM 경계를 넘지 못한다.** containerd 는 프로세스 입출력을 FIFO 로
주고받는데, 클라이언트와 shim 이 같은 커널의 파일 시스템을 봐야 한다.

그래서 containerd 네이티브 구현(#68)의 검증은 **실행기가 리눅스 안에 있을 때** 가능하다.
"로컬에서 검증할 방법이 없다" 는 근거는 절반만 유효하다 — 제어 평면은 여기서 검증되고,
실행 결과 수집은 리눅스 안에서 검증한다. 절차는 `docs/05_로컬_개발_가이드.md` 에 있다.
