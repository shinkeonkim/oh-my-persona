# 04. 로드맵 및 이슈 목록

## 1. 마일스톤

| 마일스톤 | 목표 |
|---|---|
| **M1 — MVP 문제 풀이 사이트** | 로그인·문제 탐색·코드 실행·실시간 채점·어드민 문제 관리가 로컬에서 동작 |
| **M2 — 운영/배포** | k8s GitOps 배포, 관측, 지원 언어 확장 |

## 2. 이슈 목록 (GitHub Issues)

모든 작업은 착수 전에 GitHub Issue 로 등록되어 있으며, 커밋 메시지에 이슈 번호를 포함한다.

### M1 — MVP

| # | 이슈 | 영역 | 선행 |
|---|---|---|---|
| [#1](https://github.com/shinkeonkim/codekr/issues/1) | 프로젝트 기획서 · 아키텍처 · 도메인/API 설계 문서 작성 | docs | — |
| [#2](https://github.com/shinkeonkim/codekr/issues/2) | 모노레포 스캐폴딩 및 로컬 개발 인프라 구성 | infra | #1 |
| [#3](https://github.com/shinkeonkim/codekr/issues/3) | Spring Boot 애플리케이션 기반 · Flyway 스키마 · Actuator | api | #2 |
| [#4](https://github.com/shinkeonkim/codekr/issues/4) | JWT 기반 인증/인가 (Spring Security) | api | #3 |
| [#5](https://github.com/shinkeonkim/codekr/issues/5) | 문제 도메인 및 목록/상세 조회 API (Querydsl) | api | #4 |
| [#6](https://github.com/shinkeonkim/codekr/issues/6) | 어드민 문제 관리 CRUD API | api | #5 |
| [#7](https://github.com/shinkeonkim/codekr/issues/7) | Go 코드 실행기 (OCI 샌드박스) | executor | #2 |
| [#8](https://github.com/shinkeonkim/codekr/issues/8) | Go 코드 채점기 (큐 기반) | judge | #7 |
| [#9](https://github.com/shinkeonkim/codekr/issues/9) | 제출/실행 API · WebSocket 실시간 중계 · 큐 모니터링 | api | #6, #8 |
| [#10](https://github.com/shinkeonkim/codekr/issues/10) | Next.js 기반 · 공통 레이아웃 · 디자인 시스템 | web | #2 |
| [#11](https://github.com/shinkeonkim/codekr/issues/11) | 로그인 · 회원가입 화면 | web | #10, #4 |
| [#12](https://github.com/shinkeonkim/codekr/issues/12) | 문제 목록/상세 · 코드 에디터 · 실시간 채점 뷰 | web | #11, #9 |
| [#13](https://github.com/shinkeonkim/codekr/issues/13) | 어드민 문제 관리 화면 · 큐 모니터링 | web | #12, #6 |
| [#14](https://github.com/shinkeonkim/codekr/issues/14) | CI 파이프라인 — 테스트 및 컨테이너 이미지 빌드 | infra | #2 |
| [#15](https://github.com/shinkeonkim/codekr/issues/15) | 로컬 E2E 검증 및 시드 데이터 | infra | #13 |

### M2 — 운영/배포

| # | 이슈 | 영역 |
|---|---|---|
| [#16](https://github.com/shinkeonkim/codekr/issues/16) | k8s 배포 매니페스트 및 GitOps 연동 | infra |
| [#17](https://github.com/shinkeonkim/codekr/issues/17) | 실행기 지원 언어/버전 확장 | executor |
| [#45](https://github.com/shinkeonkim/codekr/issues/45) | 샌드박스 containerd(CRI) 네이티브 구현 | executor |
| [#46](https://github.com/shinkeonkim/codekr/issues/46) | 소프트 삭제 행 보관 정책과 정리 배치 | infra |
| [#47](https://github.com/shinkeonkim/codekr/issues/47) | 실행기 프로세스의 런타임 소켓 권한 축소 | security · infra |
| [#48](https://github.com/shinkeonkim/codekr/issues/48) | seccomp 축소와 user namespace 재매핑 | security · executor |

### 백로그 — 제품 확장

MVP 파이프라인이 안정된 뒤에 다룬다.

| # | 이슈 | 영역 |
|---|---|---|
| [#33](https://github.com/shinkeonkim/codekr/issues/33) | 제출 소스 코드 공개 범위 정책 | api · web |
| [#34](https://github.com/shinkeonkim/codekr/issues/34) | 전체 제출 내역 및 검색·필터 | api · web |
| [#35](https://github.com/shinkeonkim/codekr/issues/35) | 문제 페이지를 내용·제출·이력 탭으로 분리 | web |
| [#36](https://github.com/shinkeonkim/codekr/issues/36) | 스트릭 및 활동 그래프 | api · web |
| [#37](https://github.com/shinkeonkim/codekr/issues/37) | 위험 코드에 대한 샌드박스 방어 수준 검증 | security · executor |
| [#38](https://github.com/shinkeonkim/codekr/issues/38) | 문제별 실행 제약의 종단간 적용 검증 | api · judge · executor |
| [#39](https://github.com/shinkeonkim/codekr/issues/39) | 어드민 정답 코드 기반 테스트케이스 검증 | api · web |
| [#40](https://github.com/shinkeonkim/codekr/issues/40) | 어드민 실행기 Scale Up·Scale Out 제어 | infra · web |
| [#41](https://github.com/shinkeonkim/codekr/issues/41) | 랭킹·티어·뱃지 시스템 기획 | docs |
| [#42](https://github.com/shinkeonkim/codekr/issues/42) | 복수 문제 유형 및 전용 채점 방식 기획 | docs |
| [#43](https://github.com/shinkeonkim/codekr/issues/43) | 대회 시스템 기획 | docs |

### 기획에서 갈라져 나온 구현 이슈

기획서는 [`docs/plans/`](plans/) 에 있다.

| # | 이슈 | 근거 기획서 |
|---|---|---|
| [#57](https://github.com/shinkeonkim/codekr/issues/57) | 랭킹 점수 도메인과 집계 파이프라인 | 랭킹 |
| [#58](https://github.com/shinkeonkim/codekr/issues/58) | 랭킹 화면과 프로필 실력 티어·뱃지 | 랭킹 |
| [#59](https://github.com/shinkeonkim/codekr/issues/59) | problem_kind 도입과 채점기 분기 | 문제 유형 |
| [#60](https://github.com/shinkeonkim/codekr/issues/60) | SQL 문제 유형과 격리 실행 환경 | 문제 유형 |
| [#61](https://github.com/shinkeonkim/codekr/issues/61) | 대회 도메인과 생명주기 | 대회 |
| [#62](https://github.com/shinkeonkim/codekr/issues/62) | 대회 제출 경로와 별도 채점 큐 | 대회 |
| [#63](https://github.com/shinkeonkim/codekr/issues/63) | 대회 순위표와 운영자 도구 | 대회 |

### M3 — 사용 검토에서 나온 이슈

MVP 를 실제로 써 보고 나온 지적들이다. **대부분 화면 쪽**인데, 기능이 되는 것과
쓸 만한 것은 다르다는 뜻이다.

| # | 이슈 | 성격 |
|---|---|---|
| [#70](https://github.com/shinkeonkim/codekr/issues/70) | macOS 에서 Docker 없이 컨테이너 런타임 검증 | 조사 |
| [#71](https://github.com/shinkeonkim/codekr/issues/71) | 어드민 API 권한 제한 점검·보강 | **보안** |
| [#72](https://github.com/shinkeonkim/codekr/issues/72) | 랜딩 페이지 재기획 (사용자 관점으로) | 기획 |
| [#73](https://github.com/shinkeonkim/codekr/issues/73) | 로그인 상태에서 회원가입 링크 노출 | 버그 |
| [#74](https://github.com/shinkeonkim/codekr/issues/74) | Footer 가 페이지 높이에 따라 올라옴 | 버그 |
| [#75](https://github.com/shinkeonkim/codekr/issues/75) | 코드 제출 탭의 문제 내용 영역 제거 | 화면 |
| [#76](https://github.com/shinkeonkim/codekr/issues/76) | 전체 제출 필터 UI 재설계 (리서치 선행) | 화면 |
| [#77](https://github.com/shinkeonkim/codekr/issues/77) | 전체 제출 목록 페이지네이션 노출 | 버그 |
| [#78](https://github.com/shinkeonkim/codekr/issues/78) | 채점 대기·진행 중 상태 표시 | 화면 |
| [#79](https://github.com/shinkeonkim/codekr/issues/79) | 문제·제출 목록 테이블로 재설계 | 화면 |
| [#80](https://github.com/shinkeonkim/codekr/issues/80) | 제출 버튼 중복 클릭 방지와 제출 후 이동 | 버그 |
| [#81](https://github.com/shinkeonkim/codekr/issues/81) | 스트릭·활동 그래프 연도별 보기 | 기능 |
| [#82](https://github.com/shinkeonkim/codekr/issues/82) | 제출 내역에 제출자 표시 | 기능 |
| [#83](https://github.com/shinkeonkim/codekr/issues/83) | 사용자 프로필 페이지 | 기능 |
| [#84](https://github.com/shinkeonkim/codekr/issues/84) | 문제 목록·상세에 제출 통계 노출 | 기능 |
| [#85](https://github.com/shinkeonkim/codekr/issues/85) | 랭킹을 여러 지표로 (문제 수 / 레이팅) | 기능 |
| [#86](https://github.com/shinkeonkim/codekr/issues/86) | 대회 순위표 프리즈 규칙 리서치·기획 보강 | 기획 |
| [#87](https://github.com/shinkeonkim/codekr/issues/87) | 문제집 기능 | 기능 |
| [#68](https://github.com/shinkeonkim/codekr/issues/68) | containerd 네이티브 샌드박스 구현 | 실행기 |

### M4 — 운영이 커지면서 필요해진 것

기능이 늘고 사람이 늘면 **지금은 하나로 뭉쳐 있어도 되는 것들이 갈라져야** 한다.
큐도, 권한도, 집계도 그렇다.

| # | 이슈 | 왜 지금 갈라야 하는가 |
|---|---|---|
| [#96](https://github.com/shinkeonkim/codekr/issues/96) | 런타임 이미지 자체 레지스트리 미러링 | 태그는 다시 붙는다. 무엇이 도는지 고정되지 않으면 샌드박스 검증의 의미가 약해진다 |
| [#97](https://github.com/shinkeonkim/codekr/issues/97) | 언어·버전별 실행 제한 | ✅ 완료 |
| [#102](https://github.com/shinkeonkim/codekr/issues/102) | 채점 큐 우선순위 | 어드민 검증이 사용자 제출 뒤에 서면 문제 등록이 막힌다. **우선순위는 백엔드만 정한다** |
| [#103](https://github.com/shinkeonkim/codekr/issues/103) | 어드민 역할 세분화 | 지금은 어드민이면 전부 할 수 있다. 대회 관리자는 자기 대회만이어야 한다 |
| [#104](https://github.com/shinkeonkim/codekr/issues/104) | 사용자별 기본 공개 범위 + 설정 페이지 | 매번 고르게 하는 것은 선택지가 아니라 실수할 기회다 |
| [#105](https://github.com/shinkeonkim/codekr/issues/105) | 스트릭 집계를 제출 테이블에서 분리 | `submissions` 는 가장 빨리 커진다. **재계산 경로 없이 하면 안 된다** |
| [#106](https://github.com/shinkeonkim/codekr/issues/106) | 웹 내 알림 (카테고리별) | 모르는 사이에 내 기록이 바뀌면 안 된다 |
| [#107](https://github.com/shinkeonkim/codekr/issues/107) | 재채점 정책 재정립 — 판정 번복과 알림 | **틀리면 틀린 것으로 바꾼다.** 그 위에 정답률·랭킹이 쌓이기 때문이다 |

**먼저 할 것:** #71(권한 점검)은 다른 것과 성격이 다르다. 보이지 않는 결함이고,
뚫려 있다면 지금도 뚫려 있다.

### M5 — 다시 써 보고 나온 화면 손질

기능이 아니라 **닿는 방식**의 문제들이다. 대부분 작고, **서버는 이미 되어 있는데
화면만 안 된** 경우가 섞여 있다.

| # | 이슈 | 성격 |
|---|---|---|
| [#131](https://github.com/shinkeonkim/codekr/issues/131) | 어드민 별도 레이아웃·사이드바 내비 | 구획이 늘어나는데 길이 헤더 링크 하나다 |
| [#132](https://github.com/shinkeonkim/codekr/issues/132) | 문제 목록 정렬 노출 | **서버는 이미 `sort` 를 받는다.** 화면이 안 보낼 뿐 |
| [#133](https://github.com/shinkeonkim/codekr/issues/133) | 활동 그래프 칸 툴팁 | 화면만으로는 안 된다 — **푼 문제 수가 집계에 없다** |
| [#134](https://github.com/shinkeonkim/codekr/issues/134) | 토스트를 우측 하단으로 | 가운데는 본문을 덮는다 |

### M6 — 사람이 사람에게 말할 자리

여기까지는 **혼자 문제를 푸는 사이트**였다. 사용자가 만드는 것은 제출뿐이었고,
제출은 서로에게 말을 걸지 않는다. 커뮤니티가 붙으면 성격이 달라진다 —
**남이 쓴 것을 화면에 그리기 시작하고**(XSS), **사람이 나가도 남는 것**이 생긴다(탈퇴).

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#137](https://github.com/shinkeonkim/codekr/issues/137) | 커뮤니티 게시판 — 게시글 도메인과 CRUD | 뼈대. 나머지가 여기에 붙는다 |
| [#138](https://github.com/shinkeonkim/codekr/issues/138) | 댓글·대댓글 (깊이 제한 없음, 로그인 필수) | 답이 달려야 질문이 의미를 가진다 |
| [#139](https://github.com/shinkeonkim/codekr/issues/139) | 문제별 질문/답변 | 막히는 순간은 문제를 보고 있을 때다 |
| [#140](https://github.com/shinkeonkim/codekr/issues/140) | 회원 탈퇴와 작성 기록 보존 | **탈퇴 기능이 아직 없다.** 댓글이 붙으면 미룰 수 없다 |
| [#136](https://github.com/shinkeonkim/codekr/issues/136) | 내 제출·코드를 누가 봤는지 알림 | 공개해 둔 것이 읽히는 줄 모른다. **판단이 구현보다 무겁다** |
| [#135](https://github.com/shinkeonkim/codekr/issues/135) | 알림 목록 카테고리별 탭 | 알림 종류가 늘면 한 줄기로는 못 찾는다 |

**순서:** #137 → #138 → #139. #140 은 #138 과 함께 정해야 한다 —
탈퇴한 사람의 댓글을 어떻게 할지 정하지 않고 댓글을 만들면 나중에 바꿀 수 없다.

**뼈대가 선 뒤에 나온 것들.** 글과 댓글은 되는데 **대화가 이어지지 않는다** —
답이 달려도 모르고, 스레드가 길어지면 누구에게 하는 말인지 사라진다.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#212](https://github.com/shinkeonkim/codekr/issues/212) | 댓글 알림 | 답을 받으려면 **자기 글을 기억하고 다시 찾아가야** 한다 |
| [#211](https://github.com/shinkeonkim/codekr/issues/211) | 댓글 수정 화면 | API 는 있는데 버튼이 없다. 고치려면 **지워야** 하고, 그러면 답글이 뜬다 |
| [#213](https://github.com/shinkeonkim/codekr/issues/213) | 긴 스레드 — 서버가 잘라 내린다 | `MAX_VISUAL_DEPTH` 는 **들여쓰기만** 잡았다. 이어받을 때 **겹치면 안 된다** |
| [#214](https://github.com/shinkeonkim/codekr/issues/214) | @멘션 (본문에 사용자 id 저장) | 여러 사람이 든 스레드에서 대상을 지목할 수단이 없다 |

**순서:** #212 → #214. 멘션은 알림 경로 위에 선다 — 부르기만 하고 전달되지 않으면
그냥 글자다.

### M7 — 숫자가 맞아야 순위가 뜻을 갖는다

점수와 랭킹이 붙고 나니 **계산의 빈 구석**이 드러났다. 표를 읽는 방식도 함께 손본다.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#194](https://github.com/shinkeonkim/codekr/issues/194) | 난이도를 바꿔도 반영된 점수가 안 따라온다 | **같은 문제를 푼 두 사람의 점수가 푼 시점 때문에 달라진다** |
| [#195](https://github.com/shinkeonkim/codekr/issues/195) | 난이도 UNRATED·NO_RATE | 정렬·티어 필터·인덱스가 **1~30 조밀한 정수**를 전제로 서 있다. #194 가 선행 |
| [#198](https://github.com/shinkeonkim/codekr/issues/198) | 접근 수준을 컨트롤러에 선언 | 경로 문자열이 세 곳에 있다. 컨트롤러만 봐선 누가 쓰는지 모른다 |
| [#199](https://github.com/shinkeonkim/codekr/issues/199) | 알림을 끄는 기능 제거 (두 장치 모두) | **의도하지 않고 만들어진 기능.** 열람 알림은 기본값이 꺼짐이라 **전원이 받기 시작한다** |
| [#193](https://github.com/shinkeonkim/codekr/issues/193) | 문제 목록 열 정렬 축과 뭉친 열 나누기 | 맞은 사람·정답률, 시간·메모리 제한이 한 칸에 뭉쳐 세로 비교가 안 된다 |
| [#204](https://github.com/shinkeonkim/codekr/issues/204) | 문제 번호 (`1000번: A + B`) | 문제를 부를 이름이 제목뿐이다. **`id` 를 그대로 쓰면 초안·삭제분이 번호를 먹는다** |
| [#205](https://github.com/shinkeonkim/codekr/issues/205) | 문제 목록 정렬 기준 확장 | 정답률 정렬은 **전체 집계**가 필요하다. 지금은 페이지분만 센다 |
| [#206](https://github.com/shinkeonkim/codekr/issues/206) | 테마 선택 (라이트·다크·시스템) | 라이트는 이미 기본이다. **고를 수가 없을 뿐** |
| [#207](https://github.com/shinkeonkim/codekr/issues/207) | 랭킹 비참여 설정 제거 (탈퇴자만 제외) | 일부만 보이는 순위표는 순위가 아니다. **#188 의 어드민 제외도 함께 걷는다** |
| [#208](https://github.com/shinkeonkim/codekr/issues/208) | 공개 문제집 | 링크 공유만으로는 **아무도 새 문제집을 발견할 수 없다.** 정리는 어드민이 한다 |
| [#209](https://github.com/shinkeonkim/codekr/issues/209) | 프로필에 그 사람의 공개 문제집 | PR #154 가 "공개를 열 때 함께"로 미뤄 둔 것 |
| [#197](https://github.com/shinkeonkim/codekr/issues/197) | 제출 목록의 링크가 열 이름과 어긋난다 | "문제" 열이 제출 상세로 간다. 비공개는 눌러 봐야 안다 |
| [#196](https://github.com/shinkeonkim/codekr/issues/196) | 활동 캘린더 격자 여백 회귀 | #133 의 툴팁 상자가 격자 높이에 끼어들었다 |

**순서:** #194 → #195. 난이도가 바뀔 때 점수가 따라가지 않으면 UNRATED 는
"영영 0점인 문제"가 된다.

### M8 — 운영이 코드를 안 고치고 손댈 수 있게

뱃지는 **운영이 손대는 물건**인데 손댈 방법이 배포뿐이다. 새 뱃지 하나에 enum 수정 +
조건 SQL + 배포가 든다. 정의와 조건을 데이터로 옮긴다.

회원도 같은 결이다 — **할 수 있는 일은 만들어 뒀는데 대상을 찾을 방법이 없다.**

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#223](https://github.com/shinkeonkim/codekr/issues/223) | 어드민 회원 목록·관리 | 회원에게 할 수 있는 일 넷이 전부 **숫자 id 를 알아야** 부를 수 있다 |
| [#224](https://github.com/shinkeonkim/codekr/issues/224) | 회원 정지 | 조치가 **"그대로 두기"와 "되돌릴 수 없이 지우기"** 둘뿐이다 |
| [#225](https://github.com/shinkeonkim/codekr/issues/225) | 어드민 관리 기록 | 역할 변경은 로그 한 줄, 강제 탈퇴는 그것조차 없다 |
| [#200](https://github.com/shinkeonkim/codekr/issues/200) | 뱃지 달성 규칙 DSL 설계 (기획) | **DSL 은 스키마보다 바꾸기 어렵다.** 지금 있는 네 종류를 적어 보고 정한다 |
| [#201](https://github.com/shinkeonkim/codekr/issues/201) | 뱃지 정의를 데이터로 | 오타 하나 고치려고 배포한다. 이것만으로 이미 값이 있다 |
| [#202](https://github.com/shinkeonkim/codekr/issues/202) | 뱃지 달성 규칙 엔진 | 조건이 `BadgeAwarder` 에 박혀 있다 |
| [#203](https://github.com/shinkeonkim/codekr/issues/203) | 어드민 뱃지 규칙 편집기 | **저장 전에 확인할 수 없으면** 옮긴 자리만 바뀐다 |

**순서:** #200 → #201 → #202 → #203. #201 은 #200 과 병행할 수 있다 — 정의만 옮기고
조건은 규칙 키로 가리킨다.

### M9 — 밖에서 이 사이트에 닿는 길, 그리고 잃지 않는 것

여기까지는 **이미 들어온 사람**을 위한 것이었다. 밖에서 보면 이 사이트는 아직
링크 한 줄이고, 검색에는 같은 제목이 32번 뜬다. 그리고 **처음 온 사람이 앉을 자리**가
없다 — 첫 문제부터 오버플로를 만난다.

운영 쪽도 하나 비어 있다. 백업이 배포에 묶여 있어서, **배포를 안 하면 백업도 안 뜬다.**

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#277](https://github.com/shinkeonkim/codekr/issues/277) | 카카오톡·슬랙 OG 카드 | **이미지는 이미 있다**(#261). `metadataBase` 가 없어 안 뜰 뿐이다 |
| [#278](https://github.com/shinkeonkim/codekr/issues/278) | 화면별 메타데이터와 색인 통제 | 32개 화면이 **전부 같은 제목**이다. 어드민·설정을 막는 것이 없다 |
| [#279](https://github.com/shinkeonkim/codekr/issues/279) | 부동소수점 오차 비교 채점 | ADR-0006 이 "필요할 때" 로 미룬 것. **필요해졌다** — #280 이 이것 없이는 안 된다 |
| [#280](https://github.com/shinkeonkim/codekr/issues/280) | 사칙연산 기본 문제 넷 (A+B, A−B, A×B, A÷B) | 정답이 int8 안에 든다. **입출력을 익히는 자리에서 자료형을 만나면 순서가 뒤바뀐다** |
| [#281](https://github.com/shinkeonkim/codekr/issues/281) | RPO 1일 · RTO 1시간 백업 구조 | 덤프가 **배포할 때만** 뜬다. RPO 가 배포 주기에 묶여 있고, 복원은 해 본 적이 없다 |
| [#284](https://github.com/shinkeonkim/codekr/issues/284) | 퓨니코드 도메인의 정본 주소 (리서치) | 네 이슈가 같은 주소를 적는다. **`metadataBase` 는 유니코드를 담지 못한다** |
| [#285](https://github.com/shinkeonkim/codekr/issues/285) | 어드민에서 데이터 초기화 | **운영 전인 지금이 문제 번호를 고를 마지막 기회다** (#204 는 되돌릴 수 없다). 스테이징처럼 쓰는 동안 계속 필요하므로 **버튼으로 만든다** |

**순서:** **#284 → #277 → #278.** 주소 표기를 먼저 정하지 않으면 `og:url`·`canonical`·
`sitemap` 이 각자 다른 답을 갖는다. #278 의 동적 화면 메타데이터와 #277 의 문제별
미리보기는 둘 다 #271 도 선행이다 — 서버가 API 를 부를 주소를 모른다.

**문제 쪽:** **#285 → #279 → #280.** 빈 상태를 만들고, 채점기가 실수를 받게 하고,
그 위에 문제 넷을 넣는다. **넣는 순서가 곧 1·2·3·4번이다.**

**따로 갈 것:** #281 은 나머지와 닿지 않는다. 그리고 **가장 급하다** —
나머지는 늦어도 잃는 것이 없지만 이것은 잃는다. **#285 가 지우기 전에 서 있어야 한다.**

### M10 — 손으로 만든 UI 층을 shadcn 위로 옮긴다

`Select` 하나에서 시작했는데 **컴포넌트 층 전체의 문제**로 드러났다. 드롭다운을 열면
#206 의 테마 선택이 무시되고, 툴팁은 어떤 곳은 브라우저 기본 `title=` 이고 어떤 곳은
손으로 만든 것이다. 하나씩 만들어 메우는 대신 **shadcn/ui 를 들이기로 정했다.**

부딪히는 지점이 셋이다 — **다크 모드 방식**(`light-dark()` 한 벌 vs `.dark` 두 벌),
**토큰 이름**(`ink`/`surface` vs `foreground`/`background`), 그리고
**지금까지 UI 라이브러리가 없었다**는 것.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#286](https://github.com/shinkeonkim/codekr/issues/286) | 없는 토큰 `line` 을 다섯 곳에서 쓴다 | 테두리가 **글자색**이 되고 진행 막대 바탕은 **안 그려진다.** 색을 잘못 고른 게 아니라 없는 이름을 적었다 |
| [#287](https://github.com/shinkeonkim/codekr/issues/287) | shadcn 도입 — 토큰·다크 모드를 잇는다 | 기반. `Select` 하나로 실제로 서는지 증명한다 |
| [#291](https://github.com/shinkeonkim/codekr/issues/291) | 나머지 UI 이관 | **한 PR 이 아니다.** 잎(Input·Badge) → Button → Card → Dialog·Tooltip → 토스트 |
| [#288](https://github.com/shinkeonkim/codekr/issues/288) | 제출 번호를 맨 왼쪽으로 | 줄을 식별하는 값이 **줄 끝**에 있다. 비공개 표시도 툴팁으로 바꾼다 |
| [#306](https://github.com/shinkeonkim/codekr/issues/306) | 모바일에서 선택기가 탭할 때마다 다시 열린다 | **#287 이 낸 회귀.** 열린 목록을 우리 것으로 만들었는데 지금은 그 목록을 못 쓴다 |

**순서:** #286 → #287 → #291. #286 을 먼저 하는 이유는 그것이 **토큰 층의 버그**라서다 —
#287 이 토큰을 손대기 전에 정리되어 있어야 한다.

**#306 이 #291 보다 먼저다.** 모바일에서 필터가 통째로 막힌 상태이고, 원인 후보 하나가
`Field` 의 `<label>` 감싸기라서 **#291 이 나머지를 옮기면 같은 문제를 열 곳 더 만든다.**

**#288 은 #291 을 기다리지 않는다.** 열 순서는 지금 옮기고, 툴팁만 `Tooltip` 이 생긴 뒤에
바꾼다. `Table` 과 `EmptyState` 는 **옮기지 않을 후보다** — 우리 `Column` 추상과
캐릭터(#261)는 shadcn 이 대신해 주지 않는다.

**선택기는 끝났다.** 배럴이 `export { SelectField as Select }` 로 이름을 이어받아서
화면을 고치지 않고도 전부 옮겨졌다 — 문제 목록의 카테고리·티어·정렬을 포함해
**네이티브 `<select>` 는 한 곳도 남지 않았다.** 남은 시스템 UI 는 아래 하나다.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#318](https://github.com/shinkeonkim/codekr/issues/318) | 체크박스가 아직 시스템 UI다 | #291 **1단계의 "잎" 에서 빠졌다.** 대회 화면에 남은 시스템 UI 가 이것뿐이다 |

**#318 은 #306 의 결론을 기다린다.** shadcn `Checkbox` 도 `<button>` 이라
`Field` 의 `<label>` 감싸기에 **같은 함정**으로 들어간다.

### M11 — 프로필이 사람의 자리가 된다

여기까지 프로필은 **서버가 계산한 숫자판**이었다. 푼 문제 수, 티어, 스트릭, 뱃지.
#83 이 그렇게 정했다 — "이미 공개된 것만 모은다".

그 뒤로 게시판(#137)·댓글(#138)이 붙어 **사람이 사람에게 말을 거는 사이트**가 됐다.
이제 프로필을 여는 이유가 "몇 문제 풀었나" 만이 아닌데, **그 사람이 쓴 것은 한 글자도
없고**, 이름은 못 바꾸며, 휴대폰에서는 자기 프로필에 갈 길조차 없다.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#309](https://github.com/shinkeonkim/codekr/issues/309) | 좁은 화면에 프로필 링크가 없다 | 데스크톱에서는 `UserLink`, 모바일에서는 **그냥 글자다.** #289 가 옮기며 링크를 잃었다 |
| [#310](https://github.com/shinkeonkim/codekr/issues/310) | 프로필 소개 문구 | 프로필이 **처음으로 사용자 입력을 싣는다.** 로그인 없이 열리는 화면이라 범위가 다르다 |
| [#307](https://github.com/shinkeonkim/codekr/issues/307) | `display_name` 과 `handle` 로 나눈다 | 오타로 가입하면 **계정을 지우는 것 말고 방법이 없다.** 푼 문제가 함께 사라진다 |

**보이는 이름(`display_name`)은 바꿀 수 있게, 주소가 되는 아이디(`handle`)는 못 바꾸게**
나눈다. **둘 다 유일하다.** 그러면 이름을 바꿔도 공유된 링크와 색인된 주소(#278)가 산다.
#204 가 문제 주소에서 한 판단(가리키는 것과 부르는 것을 나눈다)과 같은 결이다.

**순서:** #309 → #310 → #307. 앞의 둘은 작고 서로 닿지 않는다. #307 은 **한 PR 이
아니다** — 스키마·백필 → 주소 전환 → 변경 기능의 세 단계다.
그리고 **#285(데이터 초기화)와 함께 하면 가장 성가신 부분이 사라진다**:
기존 사용자의 `handle` 을 무엇으로 만들 것인가가 통째로 없어진다.

### M12 — 절반만 닫힌 문들

셋 다 **없는 기능이 아니라 반쯤 된 기능**이다. 코드는 있는데 한쪽 끝이 열려 있다.

| # | 이슈 | 무엇이 반쯤인가 |
|---|---|---|
| [#311](https://github.com/shinkeonkim/codekr/issues/311) | 로그인한 채로 회원가입 화면이 열린다 | #73 이 **링크만 감췄다.** 로그인 화면에는 가드가 있고 가입 화면에는 없다. **제출하면 계정이 바뀐다** |
| [#312](https://github.com/shinkeonkim/codekr/issues/312) | 활동 그래프 맨 윗줄 툴팁이 잘린다 | #133 이 툴팁을 만들었는데 **첫 줄에서만 안 보인다.** `overflow-x-auto` 가 세로도 자른다 |
| [#313](https://github.com/shinkeonkim/codekr/issues/313) | SQL 시드 문제 다섯 개 | #60 이 유형을 다 만들었는데 **SQL 문제가 0개다.** `ready = true` 는 코드가 있다는 뜻일 뿐 |
| [#314](https://github.com/shinkeonkim/codekr/issues/314) | 올린 아바타가 안 보인다 | #116 이 올리는 것까지 만들었다. **주소가 상대 경로**라 `<img>` 가 `API_BASE_URL` 을 못 탄다 |
| [#379](https://github.com/shinkeonkim/codekr/issues/379) | 문제 목록에서 제목을 눌러도 안 간다 | #204 가 번호를 맨 앞에 넣자 **링크가 제목에서 번호로 옮겨 갔다** |

**#311 이 먼저다.** 나머지는 안 보이거나 없는 것이지만, 이것은 **하려던 일과 다른
결과**를 낸다 — 가입 폼을 제출하면 `signIn` 이 불려 원래 계정에서 조용히 밀려난다.
`next` 가 없으면 첫 화면으로 보낸다 (정해졌다).

**#379 는 규칙 하나가 낳은 회귀다.** `Table` 이 행 링크(`href`)를 **`index === 0`
에만** 넘긴다. 그때는 첫 칸이 제목이었으니 맞았는데, #204 가 번호를 앞에 넣으면서
**열 순서를 바꿨을 뿐인데 링크가 따라 옮겨 갔다.** `Column.href`(#197)가 이미 열마다
목적지를 선언하는 길이고 `rowHref` 는 쓰는 곳이 하나뿐이다 — **그 규칙을 없애는 쪽**을
본다. **#288 이 제출 번호를 맨 왼쪽으로 옮기고 있어 같은 함정에 들어갈 수 있다.**

**#313 의 순서는 정해졌다: #285(초기화) → #280(사칙연산 1~4번) → #313(5~9번).**
번호가 무엇이 되는지보다 **처음 온 사람이 만나는 순서**가 기준이다.
그리고 이것은 문제를 넣는 일이자 **SQL 채점 경로를 처음 돌리는 일**이다 —
#251 이 남긴 교훈과 같은 자리다. 만들어 두고 안 써 본 것은 안 되는 것과 구분되지 않는다.

### M13 — 계정을 잃지 않게

로그인 수단이 이메일과 비밀번호 하나뿐인데 **둘 중 하나를 잊으면 끝이다.**
메일을 보낼 방법 자체가 없어서(#233) 되찾을 길이 없다.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#233](https://github.com/shinkeonkim/codekr/issues/233) | 이메일 인증 | 발송 경로가 없다. **아래 전부의 선행이다** (PR #344) |
| [#355](https://github.com/shinkeonkim/codekr/issues/355) | 메일 발송을 운영에 붙인다 | #344 는 **로컬까지다.** 차트에 설정이 없고, Daum 은 465/SSL 인데 코드는 STARTTLS 뿐이다 |
| [#315](https://github.com/shinkeonkim/codekr/issues/315) | 비밀번호 찾기 | 잊으면 **푼 문제·랭킹·티어를 통째로 잃는다** |

**순서는 뒤집을 수 없다.** #233 이 발송 경로를 주기도 하지만, 더 중요한 것은
**인증되지 않은 주소로 재설정을 보내면 안 된다**는 것이다 — 아무 주소나 적고 가입할 수
있는 지금 상태에서 재설정을 붙이면 **남의 주소로 가입한 계정을 그 주소 주인이
가져간다.** 순서가 뒤바뀌면 이 기능이 계정 탈취 경로가 된다.

**#355 가 그 사이에 낀다.** #344 를 머지해도 차트에 `CODEKR_MAIL_*` 이 없어
`host` 가 비고, 그러면 설계대로 **"보낼 수 없으면 요구하지도 않는다"** 가 발동해
**인증 기능이 꺼진 채 배포된다.** 안전하게 도는 것이지 켜진 것이 아니다.
발송 계정은 `contact@shinkeonkim.com`(Daum 스마트워크)로 정해졌다 —
**인증 계정과 발신 주소가 같아야** SPF/DKIM 이 맞는다.

**그리고 이 저장소는 public 이다.** 앱 비밀번호가 한 번 들어가면 커밋을 지워도
되돌릴 수 없다. `values.yaml` 에는 **시크릿 이름만**, 값은 SOPS 에만 둔다 —
`postgres.secretName`·`storage.secretName` 이 이미 그 패턴이다.
`docs/09` §7 의 시크릿 표에 **어디에 두는지와 새 나갔을 때 무엇을 하는지**가 없다.

### M14 — CI 를 손으로도 돌릴 수 있게

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#317](https://github.com/shinkeonkim/codekr/issues/317) | CI 수동 트리거와 옵션 | 커밋을 밀지 않으면 아무것도 안 돈다. **30분짜리 `sandbox-live` 만 따로 돌릴 방법이 없다** |
| [#343](https://github.com/shinkeonkim/codekr/issues/343) | 여러 대상 선택 + 이미지 빌드 옵션 | #317 이 **하나만 고르게** 뒀다. `workflow_dispatch` 에 **다중 선택 타입이 없다** |
| [#348](https://github.com/shinkeonkim/codekr/issues/348) | 배포 전용 워크플로 | **특정 버전을 다시 배포할 길이 없다.** #248 때 손으로 메울 방법이 없었다 |

`workflow_dispatch` 한 줄을 넣는 일이 아니다. 모든 잡이 `paths-filter` 의 결과
(`needs.changes.outputs.*`) 뒤에 있는데, **수동 실행에는 비교할 PR 이 없다.**
그 게이트를 입력과 어떻게 잇느냐가 실제 작업이다.

**조심할 것 셋:** main 에서 수동 실행하면 `images`·`release` 가 돌아
**손으로 CI 를 돌린 것이 배포가 된다.** `concurrency` 가 같은 ref 의 진행 중 CI 를
**취소한다.** 그리고 옵션을 다 끄면 **아무것도 안 돌고 초록이다** — #248 이 겪은 모양
그대로다.

**#317 은 그 셋을 가장 좁게 풀었다** — 대상을 하나만 고르게 하고, `images`·`release`
를 push 전용으로 아예 막고, `concurrency` 그룹에 이벤트 이름을 넣었다.

**넓히되 배포는 CI 에 넣지 않는다.** CI 는 "이 커밋이 쓸 만한가" 를 판정하는 곳이고,
**재배포에는 이미지 빌드가 필요 없다**(이미 ghcr 에 있다). 그래서 #343 은 다중 선택과
이미지 빌드만 맡고, **배포는 #348 이 전용 워크플로로 한다.** 경로가 둘이 되는 것은
같지만 **의도가 분명한 둘**이 된다 — 자동(머지)과 수동(재배포).

**#348 이 먼저 갈라야 할 것:** "재배포" 가 셋이다. (a) 다른 sha 로 바꾸기,
(b) CI 가 태그를 못 적은 경우(#248) — 이 둘은 워크플로가 푼다. 그런데
**(c) 같은 sha 를 그대로 다시**는 `values-release.yaml` 이 안 바뀌므로 커밋이 없고,
**ArgoCD 는 아무 일도 하지 않는다.** 조용히 성공하지 않게 하는 것이 중요하다.
그리고 CI 의 `release` 는 `needs: images` 로 이미지 존재를 보장받지만
**수동 배포에는 그 보호가 없다** — 오타 하나가 ImagePullBackOff 다.

### M15 — 배포가 남기는 것을 치운다

CD 가 붙고(#246) 나서 **배포가 자주 나가기 시작했다.** 그러면서 배포가 남기는 것도
같이 쌓이는데, 치우는 규칙을 아무 데도 적어 두지 않았다.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#320](https://github.com/shinkeonkim/codekr/issues/320) | 오래된 ReplicaSet 이 쌓인다 | `revisionHistoryLimit` 이 **어디에도 없다.** 기본 10 × 워크로드 6개 |

**앱 다섯이 `global.imageTag` 하나를 공유한다.** #246 이 머지마다 그 태그를 커밋 sha 로
갱신하므로 **머지 한 번에 다섯 개가 모두 새 리비전을 만든다** — 웹만 고친 머지에도.
열 번 머지하면 트리가 찬다.

그리고 **이 이력은 쓰이지 않는다.** §5.1 이 정한 되돌리기는 `kubectl rollout undo` 가
아니라 **git revert → 태그 되돌리기**다. 쓰지 않는 이력을 워크로드마다 열 개씩 들고 있다.

### M16 — 역할은 갖췄는데 그 역할이 쓸 화면이 없다

#103 이 역할 체계를 **끝까지 만들어 두었다.** 한 사람이 여러 역할을 갖고
(`Set<UserRole>`), `RoleHierarchy` 가 SUPERUSER → ADMIN → 나머지를 잇고,
#198 의 `@AdminApi(UserRole.X)` 가 엔드포인트마다 역할을 선언하며,
웹의 `visibleNav()` 가 그 위계로 메뉴를 거른다. `UserRole` 주석이 예까지 적어 뒀다 —
"문제도 내고 게시판도 보는 사람이 있다."

**문제는 거를 메뉴가 없다는 것이다.** 어드민 화면은 문제·큐·운영 작업 셋뿐이라,
`CONTEST_MANAGER` 나 `BOARD_MANAGER` 를 줘도 **빈 화면을 본다.**

| # | 이슈 | 무엇이 없나 |
|---|---|---|
| [#335](https://github.com/shinkeonkim/codekr/issues/335) | 대회 관리 화면 | **API 는 다 있다**(`AdminContestController`). 화면만 없다 |
| [#336](https://github.com/shinkeonkim/codekr/issues/336) | 어드민에서 글·댓글 관리 | **API 부터 없다.** 지울 수는 있는데 지울 것을 찾을 수 없다 |
| [#337](https://github.com/shinkeonkim/codekr/issues/337) | 문제 폼을 구획으로 | #127 은 **코드**를 쪼갰다. 화면은 일곱 덩어리가 세로로 쌓인 채다 |
| [#223](https://github.com/shinkeonkim/codekr/issues/223) | 어드민 회원 목록·관리 | 역할을 **줄 화면**이 없다. `PUT /admin/users/{id}/roles` 는 있다 |

**순서:** #335 → #223 → #336. #335 는 화면만 붙이면 되고, #223 이 있어야 역할을
줄 수 있으며, #336 은 API 부터라 가장 크다.

**#337 은 따로 간다.** 조사가 앞서고(탭·단계·접기·목차 중 무엇인지),
`Tabs` 가 #291 에서 와야 한다. 여기서 정한 방식을 **#335 의 대회 폼이 따른다.**

**정해진 것 셋.** 진행 중인 대회는 **고칠 수 없다** — 대회 중에 시작 시각이나 문제가
바뀌면 이미 제출한 사람과 아닌 사람이 다른 대회를 본 것이 된다. **서버가 막는다.**
`Board` 는 **enum 셋으로 둔다** — 늘릴 계획이 없으므로 데이터로 옮기지 않는다
(#336 이 만드는 것은 게시판 관리가 아니라 **글·댓글 관리**다).
문제 폼의 **등록과 수정은 모양이 달라도 되지만 겹치는 것은 한 벌**이다 —
구획 컴포넌트(#127 이 쪼갠 것)와 **검증·저장**은 반드시 공유한다.

### M17 — 만들어 둔 문제를 운영에 올린다

문제가 저장소에 12개 쌓였다 — 기본 셋, 사칙연산 넷(#280), SQL 다섯(#313).
**그런데 운영에 넣을 길이 없다.** `make seed` 는 `localhost` 를 고정으로 보고,
어드민을 만드는 단계가 `docker exec ... psql` 이라 로컬 compose 를 전제한다.

| # | 이슈 | 왜 지금인가 |
|---|---|---|
| [#342](https://github.com/shinkeonkim/codekr/issues/342) | 시드 문제를 운영에 넣을 경로 | #280 이 "**운영 등록 경로를 정하고 기록한다**" 로 미뤘고, 체크 안 된 채 닫혔다 |

**생각보다 작다.** `seed.sh` 는 이미 어드민 API(`POST /api/v1/admin/problems`)를 부르고
`409` 를 건너뛰어 여러 번 돌아도 안전하다. 걸리는 것은 셋뿐 — 주소 고정,
`promote_admin` 의 `docker exec`, 그리고 **운영에 만들면 안 되는 데모 계정**이다.
**"계정 준비" 와 "문제 주입" 을 가르는 것**이 이 이슈의 실제 작업이다.

**#338 이 먼저다.** 12개 지문이 전부 마크다운인데 지금 그것이 안 그려진다 —
넣어 봐야 깨져 보인다.

### M18 — 써 보고 한 번에 나온 것들

한 번 훑고 나온 열 가지다. **버그 셋이 먼저다** — 나머지는 없는 것이지만 이 셋은
있는 것이 잘못 도는 것이다.

| # | 이슈 | 성격 |
|---|---|---|
| [#383](https://github.com/shinkeonkim/codekr/issues/383) | 제출한 코드가 내가 쓴 것이 아니다 | **버그.** 틀린 판정이 기록으로 남고 재채점으로도 안 풀린다 |
| [#386](https://github.com/shinkeonkim/codekr/issues/386) | 질문 탭에서 탭 줄이 두 번 | **버그.** 레이아웃과 화면이 둘 다 그린다 |
| [#379](https://github.com/shinkeonkim/codekr/issues/379) | 문제 목록에서 제목이 안 눌린다 | **버그** (M12 에 있다) |
| [#384](https://github.com/shinkeonkim/codekr/issues/384) | 코드 블록 하이라이팅 | 언어는 이미 `data-language` 로 읽고 있다 |
| [#385](https://github.com/shinkeonkim/codekr/issues/385) | 공개 범위 문구 통일 | 세 곳이 다르게 말한다. #140 이 같은 것을 한 곳으로 모은 전례 |
| [#388](https://github.com/shinkeonkim/codekr/issues/388) | 질문 작성 마크다운 편집기 | **미리보기와 실제 글이 같아야** 한다 |
| [#389](https://github.com/shinkeonkim/codekr/issues/389) | 질문 글 이미지 첨부 | 올릴 곳은 #115 가 이미 만들었다. **용량·장수 제한**이 핵심 |
| [#390](https://github.com/shinkeonkim/codekr/issues/390) | 채점기도 조정한다 | #40 이 실행기만 만들었다. **대회 채점기(#62)를 못 늘린다** |
| [#391](https://github.com/shinkeonkim/codekr/issues/391) | 랭킹에 0점 사용자 | #207 의 "일부만 보이는 순위표는 순위가 아니다" 를 끝까지 민다 |
| [#393](https://github.com/shinkeonkim/codekr/issues/393) | 어드민 문제집 관리 | **#335·#336 과 같은 빈칸.** 사용자가 만든 것마다 볼 곳이 필요하다 |
| [#394](https://github.com/shinkeonkim/codekr/issues/394) | 아희·엄랭 런타임 | **이미지를 직접 만들어야 한다.** 공식 이미지가 없다 |

**#383 이 가장 급하다.** 사용자가 안 쓴 코드가 그 사람 기록으로 남고, 그 위에
정답률·랭킹이 쌓인다. **재채점(#107)으로도 안 풀린다** — 그 코드를 다시 채점할 뿐이다.

**#388 → #389 순서다.** 이미지 첨부는 편집기 위에 붙는다.

**#393 은 #208 과 경계를 나눈다.** #208 이 "어드민이 내릴 수 있게" 를 한 줄로 안고
있는데, 그것을 기다릴 이유가 없다 — 문제집은 지금도 링크로 공유되고 있고
**무엇이 담겼는지 볼 방법이 없다.** 양쪽에 남겨 두면 둘 다 반쯤 한다.

### M20 — 함수만 구현하는 문제 (#421)

**지금 낼 수 있는 문제는 프로그램 하나를 통째로 쓰는 것뿐이다.** 그래서 사용자는 입력을
읽고 출력을 찍는 것을 매번 다시 쓴다 — 그것이 문제의 주제가 아닐 때도.

기획서: [`docs/plans/함수형_문제_하네스_기획.md`](plans/함수형_문제_하네스_기획.md) (#421)

**결론은 합치는 방법을 런타임 정의가 안다는 것이다.** 하네스와 사용자 코드를 **파일로
나눠** 놓고 하네스를 돌린다 — 문자열로 이어 붙이면 **오류의 줄 번호가 통째로 어긋나고,
틀린 곳을 못 찾는 오류 메시지는 없는 것만 못하다.** 그리고 **하네스를 쓴 언어가 곧
허용 목록**(#419)이라 새 규칙을 만들지 않는다.

| 순서 | # | 무엇 |
|---|---|---|
| 1 | [#445](https://github.com/shinkeonkim/codekr/issues/445) | 실행기가 파일 여럿을 받는다 — **가장 어렵고 나머지가 여기 기댄다** |
| 2 | [#446](https://github.com/shinkeonkim/codekr/issues/446) | 문제에 언어별 하네스 (`JUDGE_FUNCTION`) |
| 3 | [#447](https://github.com/shinkeonkim/codekr/issues/447) | 채점기가 하네스를 싣고 **새지 않게** 한다 |
| 4 | [#448](https://github.com/shinkeonkim/codekr/issues/448) | 어드민이 하네스를 쓴다 |
| 5 | [#450](https://github.com/shinkeonkim/codekr/issues/450) | 사용자 화면 — 껍데기와 안내 |

**부분 점수는 열지 않는다.** 함수 단위 호출이면 가능해지지만 판정 모델(#107)과
점수(#57)가 "맞았다/틀렸다" 를 전제로 서 있다 — 열면 랭킹까지 흔들린다.

### M19 — 닿을 거리에 있는 순위 (#240)

전체에서 300등인 것은 **대부분의 사람에게 아무 뜻이 없다.** 같은 학교·같은 회사·
같이 준비하는 사람들 사이의 순위는 다르다 — 닿을 수 있는 거리라서 다음 한 문제를 부른다.

기획서: [`docs/plans/그룹_소속_기획.md`](plans/그룹_소속_기획.md) (#240, PR #403)

**결론은 소속과 그룹이 둘이라는 것이다.** 소속은 어드민이 등록하고 **이메일 도메인이**
붙이며, 그룹은 누구나 만들고 사람이 넣는다. 신뢰 수준이 다른 것을 한 표에 담으면
**"서울대학교" 라는 이름의 그룹을 아무나 만들 수 있고**, 그 랭킹이 진짜 옆에 같은
모양으로 놓인다.

| 순서 | # | 무엇 |
|---|---|---|
| 1 | [#396](https://github.com/shinkeonkim/codekr/issues/396) | 확인한 메일 주소 여러 개 (#233 확장) — **이것 없이는 아무것도 못 한다** |
| 2 | [#397](https://github.com/shinkeonkim/codekr/issues/397) | 소속 도메인 어드민 관리 (소속 1 : 도메인 N) |
| 3 | [#398](https://github.com/shinkeonkim/codekr/issues/398) | 소속 붙이기·떼기 + 프로필 표시 (**사람 1 : 소속 N**) |
| 4 | [#399](https://github.com/shinkeonkim/codekr/issues/399) | 소속 안 랭킹 — 기존 랭킹에 필터 하나 |
| 5 | [#400](https://github.com/shinkeonkim/codekr/issues/400) | 소속끼리 랭킹 — 상위 N명 합, 최소 인원 |
| 6 | [#401](https://github.com/shinkeonkim/codekr/issues/401) | 그룹 만들기·초대·가입 — 1~5와 병렬로 갈 수 있다 |
| 7 | [#402](https://github.com/shinkeonkim/codekr/issues/402) | 그룹 안 랭킹 |

**#233(이메일 인증)이 전부의 선행이다.** 주소를 확인하지 않으면 `@snu.ac.kr` 이라고
적기만 하면 서울대가 된다 — M13 이 그것을 세운다.

**정해진 것 셋.** 소속은 **여럿**이다 — 확인한 메일마다 붙고 주 소속을 고르지 않는다.
**졸업·퇴사는 다루지 않는다** — 계정 회수 시점을 우리가 알 수 없고, 주기적 재확인은
쓰는 사람에게 계속 일을 시키는 것이다. 도메인 목록은 **요청을 받아** 어드민이 넣는다 —
요청 폼은 만들지 않는다. 요청이 실제로 오는 것을 보고 만든다.

**그룹끼리의 랭킹은 안 만든다.** 합이면 머릿수가 이기고, 평균이면 **잘하는 사람만
남기고 내보내는 유인**이 생긴다. 뒤엣것은 사람에게 나쁘게 작동한다.
소속끼리는 겨룬다 — 인원을 도메인이 정하므로 조작할 수 없다.

### M21 — 낼 수 있는 문제의 종류를 늘린다

지금까지 문제는 사실상 하나였다 — **stdin 을 읽어 stdout 에 찍는 프로그램.** 배울 것이
그것뿐은 아니다. 기획서: [`docs/plans/문제_유형_확장_기획.md`](plans/문제_유형_확장_기획.md)

| # | 이슈 | 무엇이 달라지는가 |
|---|---|---|
| [#452](https://github.com/shinkeonkim/codekr/issues/452) | 스페셜 저지 — 출제자가 채점 코드를 쓴다 | **정답이 여럿인 문제**를 낼 수 있다 |
| [#453](https://github.com/shinkeonkim/codekr/issues/453) | 상태를 바꾸는 SQL 문제 | 견주는 것이 결과 집합이 아니라 **끝난 뒤의 상태**다 |
| [#454](https://github.com/shinkeonkim/codekr/issues/454) | SQL 을 MariaDB 로도 | 같은 언어라 **런타임만** 얹으면 됐다 |
| [#455](https://github.com/shinkeonkim/codekr/issues/455) | Redis 문제 유형 | 제출이 **명령의 연속**이다. 유형이 곧 제품이다 |
| [#456](https://github.com/shinkeonkim/codekr/issues/456) | bash 런타임 | 셸 자체가 답인 문제 |
| [#457](https://github.com/shinkeonkim/codekr/issues/457) · [#497](https://github.com/shinkeonkim/codekr/issues/497) · [#498](https://github.com/shinkeonkim/codekr/issues/498) | 파일이 여럿인 문제 | 파일을 나누는 것부터가 문제의 일부다 |
| [#473](https://github.com/shinkeonkim/codekr/issues/473) | 부분 점수 | 99개를 맞힌 것이 **0개 맞힌 것과 같지 않다** |
| [#474](https://github.com/shinkeonkim/codekr/issues/474) | 인터랙티브 문제 | 답을 아는 것이 아니라 **답에 이르는 길**을 묻는다 |
| [#421](https://github.com/shinkeonkim/codekr/issues/421) | 함수만 구현하는 문제 (M20) | 하네스가 입출력을 맡는다 |
| [#527](https://github.com/shinkeonkim/codekr/issues/527) | MongoDB 문제 | **아직이다.** 유형을 어떻게 가를지가 첫 판단이다 |

**여기서 한 번 배운 것**: 제품을 늘리는 방법이 유형마다 다르다. SQL 은 PostgreSQL 과
MariaDB 가 **같은 언어**를 써서 런타임만 더하면 됐지만(#454), Redis 와 MongoDB 는 질의
언어가 아예 달라 그 방법이 통하지 않는다. 그래서 #455 는 이름을 `JUDGE_NOSQL` 이 아니라
**`JUDGE_REDIS`** 로 두었다 — 이름이 할 수 있는 것보다 넓으면 다음 사람이 속는다.

### M22 — 대회를 닫을 수 있게, 그리고 나간 것이 남게

대회는 지금 **열거나 안 열거나** 둘뿐이고, 나가도 **남는 표시가 없다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#465](https://github.com/shinkeonkim/codekr/issues/465) | 비공개 대회 | `DRAFT`(준비 중)와 `PUBLISHED`(목록에 뜸) 사이가 없다 |
| [#466](https://github.com/shinkeonkim/codekr/issues/466) | 참가 신청과 승인 | `contest_registrations` 에 **상태가 없다.** 누르면 그 순간 참가자다 |
| [#463](https://github.com/shinkeonkim/codekr/issues/463) | 대회 참가 뱃지 | `BadgeEventType` 이 `PROBLEM_ACCEPTED`·`STREAK_UPDATED` **둘뿐**이다 |

**#465 와 #466 은 직교한다.** 하나는 *누가 볼 수 있는가*, 다른 하나는 *누가 낼 수
있는가*다. **한 값으로 합치면 "링크를 아는 사람은 다 참가"** 라는 조합이 사라진다 —
스터디에서 가장 흔한 모양이다.

**#465 는 문제집이 먼저 푼 문제다.** `CollectionVisibility` 의 `UNLISTED` 를 그대로
옮길 수 있다. 다만 대회는 **문제와 순위표가 시작 전에 새면 되돌릴 수 없어서**,
목록에서 빼는 것만으로는 부족하다.

**#463 은 얹기만 하면 된다.** #200~#203 으로 뱃지가 이미 데이터·규칙 엔진이라
**배포 없이 새 뱃지를 만들 수 있다** — 규칙이 볼 사건에 대회를 더하는 일이다.

### M23 — 화면이 좁아지면 드러나는 것들

| # | 이슈 | 무엇이 |
|---|---|---|
| [#459](https://github.com/shinkeonkim/codekr/issues/459) | 로그인 없이 코드 제출 탭이 열린다 | `RequireAuth` 를 쓰는 아홉 화면에 **이것만 빠져 있다** (#113) |
| [#462](https://github.com/shinkeonkim/codekr/issues/462) | 좁은 화면에서 게시글 제목이 잘린다 | 제목만 `flex-1 truncate` 라 **가장 중요한 것이 먼저 사라진다** |
| [#460](https://github.com/shinkeonkim/codekr/issues/460) | 언어별 제한 카드에 여백이 없다 | 형제들은 각자 `mt-*` 를 드는데 이것만 안 들었다 |
| [#523](https://github.com/shinkeonkim/codekr/issues/523) | 슬라이드쇼가 되감기고 넘길 단추가 없다 | 겹쳐 놓고 투명도만 바꾸면 **방향이 없다** — 3 → 1 로 뛴다 |
| [#520](https://github.com/shinkeonkim/codekr/issues/520) | `make smoke` 가 회원가입에서 죽는다 | 화면이 아니라 **검사 쪽**이다. 서버가 규칙을 더했는데 스크립트가 안 따라갔다 |

**#459 가 먼저다.** 지금은 에디터를 열고 코드를 다 쓴 뒤에야 막힌다 —
#113 이 "안내하고, 로그인 후 그 자리로 돌려보낸다" 로 정한 것과 반대다.

**#462 는 표에서 이미 푼 문제다.** #193 이 `hideBelow` 로 열을 감췄는데,
게시글 목록은 카드라 그 장치가 없다. 카드로 그리는 목록이 여럿이라 함께 볼 만하다.

**#520 은 성격이 하나 다르다.** 나머지는 보이는 것이고 이것은 **보이지 않는 것**이다 —
CI 가 스모크를 돌지 않아서, #235(약관)·#189(제출 간격)가 들어온 뒤로 계속 깨져 있는데도
아무도 몰랐다. 고친 뒤에도 그 구조는 그대로다: 이 종류는 **사람이 돌려야 발견된다.**

### M24 — 사용자가 사이트에 기여하는 통로

지금 사용자가 만드는 것은 전부 **자기 것**이다 — 제출·글·댓글·질문.
**사이트 자체를 낫게 만드는 일**은 할 수 없다.

| # | 이슈 | 무엇이 |
|---|---|---|
| [#477](https://github.com/shinkeonkim/codekr/issues/477) | 난이도 투표 | solved.ac 가 BOJ 위에서 별도 서비스가 된 이유. **누구의 표를 반영하는가**가 전부 |
| [#478](https://github.com/shinkeonkim/codekr/issues/478) | 문제 오류·테스트케이스 신고 | 질문(#139)과 섞이면 **어드민이 못 찾는다.** 기여자를 문제 페이지에 남긴다 |
| [#475](https://github.com/shinkeonkim/codekr/issues/475) | 프로필 배지 SVG | #277·#278 은 남이 걸어 줄 때 돈다. **이건 사용자가 스스로 건다** |
| [#476](https://github.com/shinkeonkim/codekr/issues/476) | 점수 변화 그래프 | 지금이 얼마인지만 보인다. **어떻게 왔는지가 없다** |

**#477 의 문턱은 지금 정하면 틀린다.** solved.ac 는 Platinum V 이상인데,
**우리는 그 티어에 아무도 없을 수 있다** — 높게 시작하면 켜져 있는데 아무도 못 쓴다.
설정으로 빼고 낮게 시작한다.

**#194 가 #477·#476 둘의 선행에 가깝다.** 난이도가 바뀌면 반영된 점수가 안 따라오는데,
투표로 난이도가 자주 움직이면 그것이 매번 나고, 그래프는 과거가 흔들린다.

### M25 — 문제를 만드는 일을 도구로

테스트케이스가 백 개를 넘으면 **어드민 폼으로는 못 만든다.**
그리고 만드는 일의 대부분은 글쓰기가 아니라 **빠진 것을 찾는 일**이다.

| # | 이슈 | 무엇이 |
|---|---|---|
| [#479](https://github.com/shinkeonkim/codekr/issues/479) | 문제 데이터 업로드 (JSON·zip) | **뼈대.** 형식이 정해져야 아래 셋이 선다 |
| [#480](https://github.com/shinkeonkim/codekr/issues/480) | 문제 초안 스킬 | 아이디어를 받아 **빠진 것을 되묻는다** |
| [#481](https://github.com/shinkeonkim/codekr/issues/481) | 정답 코드 스킬 | **돌아가는 코드와 정답 코드는 다르다** — 의도한 복잡도를 지켜야 한다 |
| [#482](https://github.com/shinkeonkim/codekr/issues/482) | 반례 데이터 스킬 | 두 풀이를 비교해 찾는다. #481 이 재료 |
| [#537](https://github.com/shinkeonkim/codekr/issues/537) | 맨 JSON 수용 · 미리보기 경로 | #479 가 **zip 만** 받는다. 시드도 스킬도 맨 JSON 이다 |
| [#538](https://github.com/shinkeonkim/codekr/issues/538) | 올리는 화면 | #479 가 **api 만 넣고 닫혔다.** #537 이 선행 |

**순서: #479 → #480 → #481 → #482 → #537 → #538.** 형식이 먼저고, 셋은 그 형식으로 주고받는다.

> ⚠️ **#479 는 반만 들어갔다.** PR #502 에 `apps/web` 파일이 하나도 없다 —
> `/admin/problems/import` 라우트도, `problems/imports` 를 부르는 코드도 없다.
> 완료 조건 다섯 중 **"화면에서 만들 수 있다"** 와 **"저장 전에 무엇이 들어올지 보인다"**
> 가 안 됐다. 뒤엣것은 화면만의 문제가 아니다 — 서버에도 **파싱만 하는 경로가 없다.**
>
> 그리고 **시드 18개와 스킬 셋이 내놓는 것은 전부 맨 JSON 인데 API 는 zip 만 받는다.**
> `docs/13` 의 "테스트케이스는 파일이 이긴다" 규칙대로면 맨 JSON 은
> **"테스트케이스 파일이 없는 묶음"** 이라, 형식을 바꾸지 않고 받아주기만 하면 된다.

> ⚠️ **산출물은 저장소에 커밋되면 안 된다.** 이 저장소는 public 이고
> 초안에는 **테스트케이스와 정답**이 들어간다. #424 에서 본 대로 한 번 들어가면
> 되돌릴 수 없다. 스킬 문서는 저장소에 있되 **산출물만 `.gitignore`** 안에 둔다.
> 지금 `.claude/` 에는 `commands/` 뿐이고 **`skills/` 가 없다** — #480 이 첫 스킬이다.

**#479 의 zip 은 압축 폭탄과 경로 탈출을 막아야 한다.** 선택이 아니라 필수다.

### M26 — SQL 문제가 표를 다루는데 표가 안 보인다

SQL 유형(#60)과 시드 문제(#313)는 들어갔는데, **표를 다루는 화면이 아직 글자다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#526](https://github.com/shinkeonkim/codekr/issues/526) | 지문에 예시 데이터 | **쿼리를 쓰기 전.** `[표 구조]` 는 이미 있고 **값이 없다** |
| [#525](https://github.com/shinkeonkim/codekr/issues/525) | 실행 결과를 표로 | **쿼리를 쓴 뒤.** `-tA -F'\|'` 라 **열 이름조차 없다** |

**둘은 서로를 대신하지 못한다.** 실행해 보며 표를 알아 가는 것은 문제를 푸는 것이
아니라 표를 탐색하는 것이 된다.

> ⚠️ **#525 는 채점의 비교 형식을 건드린다.** 하네스가 정답 쿼리 결과를
> `--- codekr:expected` 로 함께 내놓는다 — **그것이 실행 응답에 실려 화면까지 오면
> 답이 그대로 보인다.** 먼저 확인할 것.

### M27 — 인증을 못 한 사람을 어드민이 도울 수 없다

#233·#344 는 인증을 안 한 계정의 **쓰기를 막는다.** 그런데 메일이 안 가면
(#355 의 SPF 처럼) **DB 를 직접 고치는 것 말고 방법이 없다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#524](https://github.com/shinkeonkim/codekr/issues/524) | 인증 메일 재발송 · 강제 인증 | `AdminUserController` 에는 조회 둘뿐. **인증에 손대는 경로가 없다** |

**재료는 다 있다** — `User.emailVerifiedAt`, `EmailVerificationService`(#233),
어드민 회원 화면(#223). 새로 만들 것은 그 둘을 잇는 것뿐이다.

> **강제 인증은 "확인했다" 를 확인 없이 표시하는 일**이다. #233 이 인증을 넣은 이유를
> 건너뛰므로 **#225(어드민 관리 기록)가 남겨야 할 종류**다. 주소 오타는 이것으로
> 안 풀린다 — 그것은 #396(여러 이메일) 쪽이다.

### M28 — API 는 있는데 화면이 없는 것들

#479 에서 한 번 겪은 일이 **여러 곳에서 같은 모양으로 있었다.** 이슈는 `[api][web]` 로
열렸는데 PR 에 `apps/web` 이 없이 닫힌 것들이다.

컨트롤러의 엔드포인트 **172개를 웹이 부르는 경로와 메서드까지 대조**해 찾았다.
`GET`/`POST` 가 같은 경로를 쓰는 경우가 있어 **경로만 보면 놓친다** — 뱃지 규칙 저장이
그렇게 숨어 있었다.

| # | 이슈 | 서버에 있는데 아무도 안 부르는 것 |
|---|---|---|
| [#541](https://github.com/shinkeonkim/codekr/issues/541) | **대회에 제출할 수 없다** | `POST /contests/{s}/problems/{s}/submissions` |
| [#543](https://github.com/shinkeonkim/codekr/issues/543) | 대회 참가 승인 화면 | `applicants` 조회·승인·거절 |
| [#544](https://github.com/shinkeonkim/codekr/issues/544) | 대회 운영자 도구 | `unfreeze` · `exclusion` · 대회/공지 삭제 |
| [#545](https://github.com/shinkeonkim/codekr/issues/545) | 대회 부정 감사 | `audit/shared-addresses` |
| [#546](https://github.com/shinkeonkim/codekr/issues/546) | 내가 신청한 대회 | `GET /contests/registered` |
| [#548](https://github.com/shinkeonkim/codekr/issues/548) | 문제 오류 신고 | 신고·조회·처리 셋 다 |
| [#549](https://github.com/shinkeonkim/codekr/issues/549) | 뱃지 규칙 **저장** | 규칙 생성·수정, 뱃지 생성 |
| [#551](https://github.com/shinkeonkim/codekr/issues/551) | 문제 통계 어긋남 | `stats/drift` · `stats/recompute` |
| [#552](https://github.com/shinkeonkim/codekr/issues/552) | 소프트 삭제 정리 | `retention/cleanup` |
| [#553](https://github.com/shinkeonkim/codekr/issues/553) | 태그 수정 | `PUT /admin/tags/{id}` |
| [#554](https://github.com/shinkeonkim/codekr/issues/554) | 그룹 공개 가입 | `POST /groups/{id}/members` |

**대회가 절반이다** (#541·#543·#544·#545·#546). 그리고 **#541 이 가장 크다** —
대회를 만들고 신청받고 순위표까지 그렸는데 **아무도 제출할 수 없어서** 순위표가
영원히 비어 있다. 나머지 넷을 다 해도 #541 없이는 대회가 돌지 않는다.

> **미리보기만 있고 저장이 없는 것**(#549)과 **모으기만 하고 보는 곳이 없는 것**(#545)이
> 특히 나쁘다. 화면이 있으니 된다고 믿게 되고, 안 되는 것을 발견하는 데 시간이 걸린다.

**이 목록은 `[api][web]` 이슈를 web 없이 닫지 않으면 다시 생기지 않는다.**
#479 → #537·#538 이 그 첫 사례다.

### M29 — 눌러 보고 나온 손질

만들어 놓은 화면을 실제로 눌러 보니 나온 것들이다. **넷 다 화면 쪽이고 서버는 그대로다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#578](https://github.com/shinkeonkim/codekr/issues/578) | 드로어가 툴팁·메뉴 아래에 깔린다 | **층 토큰이 Tailwind 가 읽는 이름이 아니었다** — 클래스가 없었다 |
| [#579](https://github.com/shinkeonkim/codekr/issues/579) | 통계 선 그래프에 눈금선도 툴팁도 없다 | `<polyline>` 이 전부라 **모양만 보이고 값을 못 읽는다** |
| [#580](https://github.com/shinkeonkim/codekr/issues/580) | 소속 관리 화면이 테두리에 붙어 있다 | `Card` 는 여백을 바깥에서 받는데 **이 둘만 안 줬다** (#460 과 같은 종류) |
| [#581](https://github.com/shinkeonkim/codekr/issues/581) | 프로필 상자에 저장 버튼이 둘 | 하나를 빠뜨리면 **조용히 잃는다.** 서버는 이미 한 번에 받는다 |

**#578 은 보이는 것보다 컸다.** 드로어 한 줄이 아니라 **#134 가 만든 층 체계 전체가
한 번도 동작한 적이 없었다** — 토큰을 `--z-*` 로 적었는데 Tailwind 는 `--z-index-*` 를
읽는다. 색 토큰(#286)과 같은 함정이고, 다른 점은 **색은 튀어 보이고 층은 겹치기 전까지
아무 일도 일어나지 않는다**는 것뿐이다. `layers.test.ts` 가 같은 방식으로 막는다.

**#580 은 규칙이 만드는 종류다.** #291 3단계가 `Card` 를 평평하게 두기로 했으므로,
여백을 빠뜨려도 아무도 알려주지 않는다.

#### 정한 것 (2026-08-14)

세 곳의 "판단이 필요한 지점" 을 정했다.

| 이슈 | 정한 것 | 따라오는 것 |
|---|---|---|
| #554 | **`openJoin` 을 살린다** | **공개 그룹을 둘러보는 화면**이 이 이슈에 들어온다. 들어갈 문만 만들고 찾을 길이 없으면 지금과 같다 |
| #544 | **대회 삭제를 어드민에서 연다** | 서버가 이미 **DRAFT 만, 그것도 소프트 삭제**로 막고 있다 (ADR-0007 과 안 부딪힌다). 화면은 **DRAFT 가 아닌 대회에 버튼을 보이지 않아야** 한다 |
| #549 | **규칙을 좁히면 뱃지를 회수한다** | **#41 의 "회수하지 않는다" 를 뒤집는다** → 별도 이슈 **#558** |

> ⚠️ **#558 은 결정을 뒤집는 이슈다.** 지금 코드 두 곳(`BadgeRepository.award`,
> `AdminBadgeRuleController`)에 **"회수하지 않는다" 가 근거와 함께 적혀 있고**,
> #203 의 미리보기가 바로 그 대체물로 만들어졌다.
>
> 뒤집는 이상 그 반대 근거를 갚아야 한다 — 원래 근거가
> **"회수하면 뱃지가 사라지는데 사용자는 그 이유를 알 길이 없다"** 였으므로,
> **알림(#106)이 이 결정의 전제**다. 삭제 쿼리를 더하는 일이 아니다.
>
> 이번 결정의 범위는 **규칙 변경**이다. 주석이 함께 든 **재채점으로 정답이 뒤집힌
> 경우**는 트리거가 다르므로 #558 에서 따로 정한다.

### M30 — 운영에서 처음 눌러 보고 드러난 것

M29 는 화면을 눌러 본 것이고, 이쪽은 **운영에서 처음 써 본 것**이다. 둘의 차이가 중요하다 —
**여기 있는 것들은 로컬에서도 CI 에서도 멀쩡했다.**

| # | 이슈 | 어디서 드러났나 |
|---|---|---|
| [#588](https://github.com/shinkeonkim/codekr/issues/588) | 자체 빌드 런타임이 레지스트리에 없다 | 아희 문제를 **어드민에서 검증 실행**하니 `SYSTEM_ERROR` |
| [#590](https://github.com/shinkeonkim/codekr/issues/590) | 지문의 표가 파이프 그대로 보인다 | **문제 9 를 열어 보니** 표가 글자였다 |

**#588 은 절차의 구멍이었다.** zot 은 `library/*` 만 온디맨드로 당겨 오므로
`codekr-runtime-*` 는 사람이 밀어야 하는데, `images/README.md` 의 "새 이미지를 추가할 때"
에 **그 단계가 없었다.** csharp·kotlin(#171)은 넣었고 아희·엄랭(#394)은 넣은 적이 없다.

빠뜨려도 티가 안 난다는 것이 핵심이다 — 로컬은 `make build-runtimes` 로 만든 것이 노드에
있어 돌고, CI 는 런타임 매트릭스를 뺀다. `make check-runtime-images` 가 그것을 잡는다.

**#590 은 넓다.** 시드 20개 중 **13개**가 표를 쓴다. SQL 문제는 표를 읽어야 쿼리를 쓸 수
있는데 그 지문이 지금 읽히지 않는다. #338 이 마크다운을 넣은 근거에 **"표가 필요하다" 가
적혀 있고 구현에는 없다.**

> ⚠️ **#590 은 렌더러를 늘리는 일이지 라이브러리를 들이는 일이 아니다.**
> #137 이 마크다운 라이브러리를 안 쓴 이유가 있다 — `dangerouslySetInnerHTML` 없이
> React 엘리먼트로만 만들어 **구멍이 날 수 없게** 한 것이고, 그 렌더러는 지문뿐 아니라
> **게시글·댓글·대회 공지**도 그린다. 표 블록 하나를 더하는 쪽이 그 성질을 지킨다.

### M31 — 낼 수 있는 것과 고를 수 있는 것이 어긋난다

유형(#455·#527·#456)은 늘었는데 **분야가 따라오지 않았다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#592](https://github.com/shinkeonkim/codekr/issues/592) | Redis·MongoDB·셸 문제가 알고리즘으로 분류돼 있다 | 셋 다 `ALGORITHM` 이다 — **분야로 찾을 길이 없다** |

```
15-bash-log-levels     ALGORITHM   ← "셸로만 푸는 문제입니다" (awk·sort)
17-redis-stock-restock ALGORITHM   ← JUDGE_REDIS
20-mongo-order-…       ALGORITHM   ← JUDGE_MONGODB
```

**알고리즘을 눌러 들어온 사람은 Redis 문제를 만나고, Redis 를 연습하려는 사람은 찾지
못한다.** `problems.category` 에 CHECK 제약이 없어(V1) **마이그레이션은 필요 없다** —
`problem_kind` 와 다른 점이다.

> **정할 것이 둘 있다.** Redis·MongoDB 를 **둘로 둘지 `NOSQL` 하나로 묶을지** —
> 유형에서는 넓은 이름이 속인다는 이유로 나눴지만(#455), **분야는 "무엇을 배우는가"**
> 라 뜻이 다르다. 그리고 **`SHELL` 이 `LANGUAGE`("프로그래밍 언어")와 겹치는지** —
> bash 문제가 묻는 것은 문법이 아니라 `awk`·`sort` 로 일을 엮는 것이다.
>
> **옮기려면 지금이 낫다.** `CATEGORY_10` 뱃지가 분야별로 세므로(V45), 나중에 옮기면
> **이미 받은 뱃지의 근거가 조용히 바뀐다.** 지금은 10개를 채운 사람이 없다.

### M32 — 만들어 둔 것에 닿지 못한다

**대부분은 "없어서" 가 아니라 "닿지 못해서" 다.** 공개 문제집도, 문제 신고도,
마크다운 편집기도 이미 만들어 놓았는데 **문이 없거나 좁다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#599](https://github.com/shinkeonkim/codekr/issues/599) | 페이지마다 가로 폭이 제각각 | 셸이 `max-w-6xl` 로 정했는데 **페이지가 다시 좁힌다** |
| [#600](https://github.com/shinkeonkim/codekr/issues/600) | 문제별 제출 내역이 늘 비어 있다 | 상세는 번호를 받는데 **제출 거르개는 slug 만 푼다** |
| [#601](https://github.com/shinkeonkim/codekr/issues/601) | 공개 문제집으로 가는 길이 없다 | `/collections/explore` 가 **내비에 없다** (#208 이 만든 화면) |
| [#602](https://github.com/shinkeonkim/codekr/issues/602) | 편집기가 편집기처럼 안 느껴진다 | 칸이 작고, 버튼이 다섯이고, **미리보기가 쓰던 칸을 덮는다** |
| [#603](https://github.com/shinkeonkim/codekr/issues/603) | 신고·제안이 GitHub 으로 나간다 | #548 로 받을 곳을 만들어 놓고 **푸터는 밖으로 보낸다** |
| [#604](https://github.com/shinkeonkim/codekr/issues/604) · [#605](https://github.com/shinkeonkim/codekr/issues/605) | Redis 문제가 하나뿐 | 채점(#455)도 분야(#592)도 있는데 **들어오면 문제가 한 개** |
| [#611](https://github.com/shinkeonkim/codekr/issues/611) | 로그인 못 하는 사람의 신고 통로 | #603 이 남긴 것 — **가장 급한 신고가 로그인 바깥에 있다** |
| [#618](https://github.com/shinkeonkim/codekr/issues/618) | 문제 목록을 언어로 거를 수 없다 | 허용 언어(#419)는 **데이터도 화면 표시도 있는데** 목록 필터만 없다 |

**#618 은 빠진 자리가 기록에도 없다.** #239 가 "허용 언어" 를 필터 항목으로 적어 두고
닫혔는데, 그 이슈의 표는 근거를 **#97(언어별 *제한*)** 로 적었다 — 실제 허용 목록은 그보다
뒤인 #419 가 만들었다. 사이에 항목이 사라졌고 **PR #319 의 "하지 않은 것" 에도 없다.**

**#600 은 조용한 종류다.** slug 가 `"9"` 인 문제는 없으므로 조건이 빈 집합이 되고,
**"제출이 없다" 와 "못 찾았다" 가 화면에서 구별되지 않는다.** 없는 문제를 가리켜도
오류가 아니라 빈 목록이 나온다.

**#599 와 #602 는 같이 봐야 한다.** 질문 작성 폼이 좁은 것은 `max-w-3xl` 때문이고,
편집기가 작게 느껴지는 것은 그 좁은 폭 안에서 `rows=14` 이기 때문이다.

**#604 를 #605 보다 먼저 한다.** 기초에서 `verifyCommands` 로 무엇을 확인할지와
막을 명령을 정하고 나야, 심화에서 트랜잭션·만료 같은 **답을 정하기 어려운 것**을
다룰 수 있다.

> **이미 되어 있어 이슈로 만들지 않은 것이 둘 있다** (#82 선례).
> **페이지네이션**은 `paginationView` 가 이미 "총 N건" 을 페이지가 하나여도 보이고
> 누를 데 없는 버튼만 감춘다(#77·#181), 그리고 페이지를 쓰는 화면은 모두 그것을 그린다.
> **로컬 전용 문제 보관함**도 `.problem-drafts/` 가 이미 `.gitignore` 되어 있고
> 문제 초안 스킬이 거기에만 쓰도록 되어 있다.

#### 정한 것 (2026-08-17)

이슈에 "판단이 필요한 지점" 으로 남겨 둔 것들을 작업하며 정했다.

| 이슈 | 정한 것 | 왜 |
|---|---|---|
| #600 | **서버가 번호·slug 를 다 받고 이름을 `problemKey` 로 바꾼다** | 화면만 고치면 거르개에 `9` 를 친 사람은 그대로 빈 목록을 본다. 번호를 받는데 이름이 slug 면 다음 사람이 또 속는다 |
| #600 | **못 찾은 값을 오류로 되돌리지 않는다** | 거르개가 자유 입력이라 글자를 하나 칠 때마다 404 가 된다. 이슈의 완료 조건 하나를 일부러 빼고 근거를 남겼다 |
| #599 | **`wide` / `reading` / `form` 세 단.** 설정·질문 작성은 `wide` | 둘 다 표와 편집기가 들어가 좁으면 손해다. 긴 글을 읽는 질문 상세·알림만 `reading` |
| #601 | **탭을 경로로 둔다** (`?tab=` 이 아니라) | `ProblemTabs`(#35)와 같은 이유이고, 덤으로 **`/collections/explore` 가 그대로 살아 있다** — 옮길 것이 없다 |
| #601 | **로그인 안 한 사람은 공개 문제집으로 보낸다** | 로그인이 필요한 것은 "내" 것이지 문제집 전체가 아니다 |
| #603 | **`problem_reports` 를 넓히지 않고 `site_feedbacks` 를 새로 둔다** | 저기는 `problem_id` 가 NOT NULL 이고 종류가 전부 문제 내용이다. 섞으면 **한 enum 에 두 축**이 들어가고 어드민이 행마다 따져야 한다 |
| #603 | **로그인한 사람만 받는다** | 막을 것(속도 제한·캡차)이 없다. "가입이 안 된다" 를 알릴 길은 **#611** 로 뗐다 |
| #611 | **`/api/v1/feedbacks/anonymous` 를 따로 둔다** (기존 통로에 비로그인 허용을 붙이지 않고) | 같은 통로에 두면 `reporter_id` 유무로 갈리는 가지가 알림·목록·처리에 모두 생긴다. 입구가 둘이면 **막는 규칙도 둘로 나눌 수 있다** — 회원 것은 지금대로 두고 비회원 것만 IP 로 조인다 |
| #611 | **답을 주지 않는다고 화면에 먼저 적는다** | 받을 주소를 묻는 칸을 두면 답이 갈 것처럼 읽힌다. 알림은 회원에게만 가고 비회원에게 갈 곳이 없다 — 연락이 필요하면 **본문에 적어 달라고** 한다 |
| #611 | **IP 기준 한 시간에 5건.** Redis 가 답하지 않으면 **막지 않고 통과시킨다** | 로그인 못 하는 사람의 통로다. 여기가 Redis 사고로 함께 닫히면 **막힌 사람이 막혔다고 알릴 길까지 없어진다** |
| #604 | **막을 명령을 따로 정할 것이 없었다** | 하네스가 이미 solver 계정에서 `@dangerous`·`@scripting`·`@admin`·`@pubsub` 를 뺀다 |
| #605 | **트랜잭션 문제를 넣지 않는다** | 채점이 상태를 견주는데 **`MULTI` 를 썼는지는 상태에 남지 않는다.** `WATCH` 는 다른 연결이 끼어들어야 뜻이 생기는데 하네스는 제출을 한 번에 흘려 넣는다 |
| #605 | **만료는 `EXPIREAT`/`EXPIRETIME` 으로 절대 시각을 쓴다** | "몇 초 뒤" 로 걸면 확인 시점에 남은 시간이 흔들려 **같은 답이 때에 따라 틀린다** |

> **#605 에서 문제 하나가 실수를 못 가르는 것을 잡았다.** 45번(사전순 범위)은 처음에
> 범위 끝을 `+` 로 열어 둔 틀린 답도 `ACCEPTED` 였다 — 데이터에 `bank` 다음 낱말이
> 없어 결과가 같았다. **정답만 돌려 보면 이것을 놓친다.** 그래서 Redis 문제 25개는
> 정답뿐 아니라 **일부러 틀린 답도 제출해** 갈리는 것을 확인했다.

### M33 — 어드민 문제 관리가 손으로 하는 일에 갇혀 있다

**만드는 길은 일괄인데 다루는 길이 한 건씩이다.** 묶음 올리기(#479)는 수백 개를 한 번에
넣지만, 들어온 초안을 공개하는 것도 찾는 것도 **한 번에 하나씩**이다.

| # | 이슈 | 무엇이 |
|---|---|---|
| [#625](https://github.com/shinkeonkim/codekr/issues/625) | 어드민 문제 목록이 카드다 | 회원 관리도 사용자 목록도 표인데 **여기만 남았다** — 훑어서 비교되지 않고 좁히면 버튼이 잘린다 |
| [#626](https://github.com/shinkeonkim/codekr/issues/626) | 어드민에 검색·필터가 없다 | 서버는 `q`·`category`·`tier`·`sort` 를 **이미 받는데 화면이 안 보낸다.** 정작 필요한 공개 여부만 서버에도 없다 |
| [#627](https://github.com/shinkeonkim/codekr/issues/627) | 일괄 공개/비공개가 없다 | 체크 하나에 **테스트케이스 전 행이 지워졌다 다시 들어간다** — 공개 여부만 바꾸는 통로가 없다 |

**셋은 순서가 있다.** 표(#625)가 없으면 체크박스를 놓을 격자가 없고, 필터(#626)가 없으면
일괄로 고를 대상을 좁힐 수 없다. 그래서 #625 → #626 → #627 로 쌓는다.

**#627 이 드러낸 것이 하나 더 있다.** 문제 생성·수정·삭제는 **관리 기록(#225)에 남지 않는다.**
회원 역할 변경도 그룹 내리기도 남는데 문제만 빠졌다.

### M34 — 시험이 있는데 돌지 않았고, 얼마나 덮는지도 몰랐다

**두 가지가 한꺼번에 드러났다.** 웹 시험 184개가 **아무 데서도 돌지 않았고**(CI 도
`make test` 도 린트·타입 체크만 봤다), 세 스택 어디에도 **커버리지를 잰 적이 없었다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#641](https://github.com/shinkeonkim/codekr/issues/641) | 웹 테스트 184개가 CI 에서도 make 에서도 안 돈다 | **버그.** "안 돌았는데 초록"(#172, #248)의 세 번째 사례 |
| [#642](https://github.com/shinkeonkim/codekr/issues/642) | 커버리지를 잰 적이 없다 — 세 스택의 측정과 리포트 | 숫자가 없으면 "시험이 있다" 까지만 말할 수 있다 |
| [#645](https://github.com/shinkeonkim/codekr/issues/645) | 실시간 채점 중계에 시험이 한 건도 없다 | **#642 가 찾았다.** `realtime` 이 26.5% |
| [#646](https://github.com/shinkeonkim/codekr/issues/646) | 화면을 그려 보는 시험이 한 건도 없다 | **#642 가 찾았다.** `Table`·`ConfirmDialog`·`httpClient` 가 0% |

**#641 이 먼저다.** 커버리지를 재기 전에 시험이 돌아야 한다.

**잰 값이 판단 하나를 대신 해 줬다.** Kover(Kotlin 공식)가 이 프로젝트의
`integrationTest` 소스셋에서 설정 단계부터 죽어 JaCoCo 로 갔는데, 그 소스셋을 빼면
**잴 것이 거의 없다** — 단위만 8.8%, 통합까지 합치면 89.5% 다.
시험 파일 114개 중 95개가 통합 시험이라 이 차이는 사소하지 않다.

| 스택 | 덮인 줄 | 비율 |
|---|---:|---:|
| api (단위 + 통합) | 7,883 / 8,806 | 89.5% |
| web | 1,166 / 3,231 | 36.1% |
| go · executor | 268 / 3,723 | 7.2% |
| go · judge | 308 / 1,067 | 28.9% |

**`go · executor` 가 낮은 것은 없는 시험이 아니라 안 돌린 시험이다** — 샌드박스 코드
대부분이 `CODEKR_SANDBOX_TEST=1` 에서만 지나간다.

> **문턱(최소 커버리지)은 걸지 않는다.** 지금 걸면 숫자를 채우려고 값싼 시험이 들어오고,
> **덮였다고 말하는데 아무것도 막지 않는** 상태가 된다. 먼저 재고, 어디가 비었는지 보고 정한다.

### M35 — AI 출제 보조를 제공자에서 떼어 낸다

기획서: [`plans/AI_출제_보조_기획.md`](plans/AI_출제_보조_기획.md)

**이미 절반은 되어 있다.** #230 이 "지문 → 폼 초안" 을 만들었고 PR #387 로 들어갔다.
`baseUrl` 이 이미 환경 변수라 **LiteLLM 프록시는 api 코드를 한 줄도 고치지 않고 붙는다.**

| # | 이슈 | 무엇이 |
|---|---|---|
| [#647](https://github.com/shinkeonkim/codekr/issues/647) | 초안 만들기가 **운영에 꺼져 있다** | **버그.** 차트에 `CODEKR_DRAFT_*` 가 한 줄도 없다 — #355 와 같은 모양 |
| [#648](https://github.com/shinkeonkim/codekr/issues/648) | LiteLLM 프록시를 로컬에 세운다 | 제공자 교체가 **배포가 아니라 설정 한 줄**이 된다 |
| [#649](https://github.com/shinkeonkim/codekr/issues/649) | 프록시를 운영에 올리고 api egress 를 좁힌다 | 나가는 파드가 **하나로 줄어든다** |

**#647 은 #648 을 기다리지 않는다.** 프록시가 없어도 기본값이 OpenAI 를 가리킨다.

**왜 라이브러리가 아니라 프록시인가.** LiteLLM 은 파이썬이라 Kotlin 에 넣을 수 없다.
그런데 그것이 오히려 낫다 — **제공자 키가 api 에서 사라지고**(api 는 가상 키만 든다),
폴백과 예산이 우리 코드 없이 온다. 그리고 **api 의 egress 를 좁힐 수 있다**(#47 과 같은 방식).

> **AI 가 문제를 저장하지 않는다.** #230 이 정한 것을 그대로 지킨다 — 마지막은 사람이고,
> 마지막 방어선은 정답 코드 검증(#39)이다. **사용자 쪽에는 열지 않는다** —
> 틀린 힌트가 학습을 망가뜨리는 것은 틀린 초안이 검토에 걸리는 것과 급이 다르다.

### M36 — 묻는 것을 늘린다 (푸는 도구가 아니라)

기획서: [`plans/개발자_지식_문제_유형_기획.md`](plans/개발자_지식_문제_유형_기획.md)

M21 까지는 **"무엇으로 푸는가"** 를 늘렸다(SQL·Redis·Mongo·셸·함수·인터랙티브).
여기서 늘리는 것은 **"무엇을 묻는가"** 다 — 그리고 절반은 **빈 파일에서 시작하지 않는다.**

| # | 이슈 | 제출하는 것 | 채점이 보는 것 |
|---|---|---|---|
| [#650](https://github.com/shinkeonkim/codekr/issues/650) | 개념 퀴즈 | 고른 보기 · 짧은 답 | 값이 맞는가 (**실행기를 안 쓴다**) |
| [#651](https://github.com/shinkeonkim/codekr/issues/651) | 읽고 고치기 | 고친 파일들 | 숨긴 시험 통과 + **안 바꿀 것이 그대로** |
| [#652](https://github.com/shinkeonkim/codekr/issues/652) | 테스트 쓰기 | 시험 코드 | **심어 둔 버그를 잡는가** |
| [#653](https://github.com/shinkeonkim/codekr/issues/653) | 정규식 | 패턴 하나 | 맞을 것과 아닌 것이 갈리는가 |
| [#654](https://github.com/shinkeonkim/codekr/issues/654) | Git | 명령의 연속 | 끝난 뒤 저장소 상태 |

**순서: #650 → #651 → #652 → #653 → #654.** 싼 것부터가 아니라 **"정답이 무엇인가" 를
정하기 쉬운 것부터**다. 퀴즈는 정답이 값이고, 고치기는 시험이고, 테스트 쓰기는
**시험을 시험하는 것**이다.

**#650 은 자리가 이미 있다** — `QUIZ("객관식 · 단답", ready = false)` 가 #59 때 들어가
그대로 비어 있고, 분야(`NETWORK`·`OS`·`LANGUAGE`)도 이미 있다.

**#652 가 가장 새롭다.** 우리가 시험을 숨기고 사용자가 구현을 내던 것이 뒤집혀,
**사용자가 시험을 내고 우리가 구현을 숨긴다.** 정답은 심은 버그를 전부 잡고 멀쩡한 구현은
통과시키는 시험이다 — 그래야 "시험을 잘 썼다" 를 취향 없이 잴 수 있다.
#605 에서 우리가 직접 겪은 일(틀린 답도 `ACCEPTED` 였다)을 사용자에게 시키는 것이다.

> ⚠️ **Docker · Kubernetes 실습은 열지 않는다.** 컨테이너 안에서 컨테이너를 돌리는
> 일이고, 그것은 샌드박스 위협 모델(`docs/07`, #48)이 **정확히 막으려는 것**이다.
> 대신 정적 검사와 개념 퀴즈로 덮되, **"실제로 빌드해 봐야 아는 것" 은 못 묻는다고 적어 둔다** —
> 할 수 있다고 이름 붙이면 다음 사람이 속는다 (#455 가 `JUDGE_NOSQL` 을 피한 이유).

> **분야를 함께 정한다.** #592 가 겪은 것이 이것이다 — 유형은 늘었는데 분야가 안 따라와
> Redis 문제가 `ALGORITHM` 에 있었다. 늘릴 것: `VERSION_CONTROL` · `REGEX` · `TESTING` · `DEBUGGING`.
> **기존 문제를 옮기는 것만 조심한다** — `CATEGORY_10` 뱃지가 분야별로 센다(V45).

### M37 — 돌고 있는 것을 볼 수 없다

홈랩(`oh-my-homelab`)에 **Grafana · Prometheus · Loki 가 이미 돌고 있다.** codekr 만
거기에 안 붙어 있다.

| # | 이슈 | 무엇이 막고 있나 |
|---|---|---|
| [#676](https://github.com/shinkeonkim/codekr/issues/676) | api 를 못 긁는다 | `/actuator/prometheus` 가 `anyRequest().authenticated()` 에 걸려 401 |
| [#677](https://github.com/shinkeonkim/codekr/issues/677) | 수집 설정이 없다 | 차트에 ServiceMonitor 가 없고, **채점기·실행기는 Service 조차 없다** |
| [#678](https://github.com/shinkeonkim/codekr/issues/678) | 그릴 것이 없다 | Go 앱이 `promhttp.Handler()` 만 붙여 놓아 goroutine 수뿐 |
| [#684](https://github.com/shinkeonkim/codekr/issues/684) | 큐·stale 이 시계열로 안 남는다 | api 만 아는 값이라 #678 에서 갈라냈다 |
| [#686](https://github.com/shinkeonkim/codekr/issues/686) | api 로그 레벨 설정이 죽어 있다 | 패키지가 `codekr.api` 인데 `kr.codekr` 로 적혀 있다 |
| [#679](https://github.com/shinkeonkim/codekr/issues/679) | 로그를 고를 수 없다 | api 만 줄글이다. judge·executor 는 이미 slog JSON |
| [#680](https://github.com/shinkeonkim/codekr/issues/680) | 볼 화면이 없다 | Grafana 에 codekr 대시보드가 없다 |
| [#681](https://github.com/shinkeonkim/codekr/issues/681) | 이을 수 없다 | 제출 하나를 세 앱의 로그에서 잇는 식별자가 없다 |

**순서: #676 → #677 → #678 → #684 → #679 → #680.** #681 은 뒤에 따로 온다.

**로그 수집은 이미 되어 있다.** Promtail 이 DaemonSet 으로 네임스페이스 제한 없이
모든 파드 로그를 Loki 로 보낸다 — codekr 이 한 일도, 할 일도 없다. 이 마일스톤이
만드는 것은 **수집이 아니라 고르는 것과 잇는 것**이다.

**홈랩 저장소를 거의 안 고쳐도 된다.** ArgoCD 의 `codekr` Application 이
`deploy/charts/codekr` 를 그대로 당겨 오고, kube-prometheus-stack 은
`serviceMonitorSelectorNilUsesHelmValues: false` 라 라벨 없이 CR 을 잡아가며,
Grafana 사이드카는 `grafana_dashboard: "1"` ConfigMap 을 모든 네임스페이스에서 읽는다.
**우리 차트에 넣으면 그대로 뜬다** — 저쪽에 남는 일은 `monitoring.enabled` 를 켜는
한 줄뿐이다.

> 선례가 이미 차트 안에 있다. `backup-alert.yaml` 이 `PrometheusRule` 을
> `backup.alerts.enabled` 뒤에 두고 조건부로 만든다 (#281). 관측 CR 은 전부 같은
> 모양으로 둔다 — **Prometheus Operator 가 없는 클러스터에서는 이 CR 자체가 안 뜬다.**

> ⚠️ **어드민 큐 화면(#9, #13)을 대체하지 않는다.** 그쪽에는 실행기 수를 조절하는
> **조작**이 붙어 있고, 그것은 Grafana 가 할 일이 아니다. 겹치는 것은 큐 대기 수
> 하나인데, 그 수를 **누가 재는가**는 #678 에서 정한다 — 두 곳이 각자 세면 언젠가 갈린다.

## 3. 작업 순서 (의존 그래프)

```
#1 docs
  └─ #2 monorepo ─┬─ #3 api-core ─ #4 auth ─ #5 problems ─ #6 admin-api ─┐
                  │                                                      ├─ #9 submission+ws
                  ├─ #7 executor ─ #8 judge ─────────────────────────────┘
                  │                                                      │
                  ├─ #10 web-core ─ #11 web-auth ─ #12 web-problems ─ #13 web-admin
                  │                                                      │
                  └─ #14 ci                                          #15 e2e
```

## 4. 브랜치 · PR 전략 (Stacked PR)

> 실제 작업 절차와 규칙은 [`docs/10_작업_방식.md`](10_작업_방식.md) 에 있다.
> 이 절은 개요만 남긴다.

작업이 밀려도 리뷰가 막히지 않도록 **스택 형태로 브랜치를 쌓고 PR 을 모두 열어둔다**.

```
main
 └─ feat/01-docs                 (PR → main)
     └─ feat/02-monorepo         (PR → feat/01-docs)
         └─ feat/03-api-core     (PR → feat/02-monorepo)
             └─ …
```

- 각 PR 은 이슈 하나에 대응하고 본문에 `Closes #N` 을 포함한다.
- 커밋 메시지 형식: `<type>(<scope>): <요약> (#<issue>)`
  - 예: `feat(api): JWT 인증 필터 및 로그인 API 추가 (#4)`
- 앞선 PR 이 머지되면 뒤따르는 PR 의 base 를 순서대로 옮긴다.

## 5. MVP 완료 정의 (Definition of Done)

- [ ] 클린 체크아웃에서 `make up` 으로 6개 서비스가 모두 healthy
- [ ] `make seed` 로 데모 계정과 시드 문제 3종 생성
- [ ] 브라우저에서 가입 → 로그인 → 문제 목록 → 상세 → 코드 실행 → 제출 → 실시간 채점 → 판정 확인
- [ ] 어드민 계정으로 문제 등록/수정/삭제, 큐 모니터링 화면 확인
- [ ] 무한 루프 제출이 `TIME_LIMIT_EXCEEDED` 로, 과다 메모리 할당이 `MEMORY_LIMIT_EXCEEDED` 로 판정
- [ ] api/judge/executor 의 헬스 엔드포인트가 모두 200
