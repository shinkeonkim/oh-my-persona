GitHub repository: https://github.com/kmu-aws-capstone-team-4/booth-rag
Author account: shinkeonkim

2026-05-28T03:11:51Z	001b0c65388c509e067a1bf34fef3ddc07b0b7dc	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/001b0c65388c509e067a1bf34fef3ddc07b0b7dc	feat(ui): 새 대화/빈 세션 로드 시 메인 welcome (로고+추천 6개) 복원 기존 appendWelcomeBack() 은 '새 대화를 시작했습니다' 한 줄만 표시 → 메인 화면 진입 시와 시각적 일관성이 깨짐. 변경: - 페이지 로드 시 chat.html 의 inline .welcome (서버 렌더링된 build_id 포함) outerHTML 을 closure 에 캡처 - appendWelcomeBack() 이 캡처된 HTML 을 messages 에 그대로 재삽입 → 로고 / 헤딩 / 부제 / 6개 suggestion 모두 메인 화면과 동일 - suggestion 클릭은 querySelectorAll().forEach() 일회성 attach 에서 messagesEl 의 event delegation 으로 변경 → welcome 이 재삽입돼도 항상 동작 (DRY: 핸들러 1회만 등록) 흐름: - 새 대화 버튼 → createSession() → messagesEl.innerHTML='' + appendWelcomeBack() → 메인 동일 welcome 표시 - 빈 세션 클릭 → loadSession() (messages 없음) → appendWelcomeBack() 동일 - 첫 메시지 제출 → appendMessage('user', ...) 내 clearWelcome() 으로 자동 제거 (기존 동작) PC 1440x900 + 모바일 390x844 에서 검증: welcome 제거 후 새 대화 클릭 → welcome restored, suggestions 6개 복원, 3번째 suggestion 클릭 → input 에 해당 data-q 텍스트 채워짐 + focus console 에러 0, pytest 143 passed.
2026-05-17T11:46:53Z	fd17a6e1a315e3026d419b3bb37cb89a540513cb	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/fd17a6e1a315e3026d419b3bb37cb89a540513cb	feat(ui): 모바일 admin 페이지 폴리시 — iOS focus zoom 방지 + 컴팩트 padding 파일 끝 미디어쿼리(modal/source-card 미디어쿼리)에 admin 룰 추가 (admin 기본 정의가 line 769+ 라서 line 713 미디어쿼리에 두면 cascade 미스). - admin-shell padding 32/24 → 16/12 - admin-shell h1 26 → 22, margin-bottom 24 → 16 - admin-section padding 20 → 14, margin 16 → 12 - admin-section h2 17 → 16 - admin-section .hint 13 → 12.5, margin-bottom 12 → 10 - admin-section input (password/number/file) font 14 → 16 (iOS zoom 방지 quirk) - admin-section label 13 → 12.5 - admin-disabled padding 16 → 12, font line-height 정리 PC 1440x900 영향 0: shell padding 32/24, h1 26, section 20, input 14 — 모두 기본 유지. 모바일 390x844 적용: shell 16/12, h1 22, section 14, input 16.
2026-05-17T11:45:04Z	9af3e399adfdb14bbf86c842eef2a4d43749dd80	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/9af3e399adfdb14bbf86c842eef2a4d43749dd80	feat(ui): 모바일 topbar / footer 축소 — 채팅 영역 154px 확장 @media (max-width: 900px) (line 713 미디어쿼리)에 추가: - topbar padding 12/24 → 10/14 + 모든 safe-area-inset - brand-logo 38 → 30 - brand-title font 15 → 14, line-height 압축 - brand-sub display: none (모바일에서 부제 hide) - topnav gap 18 → 6 - topnav .nav-link display: none (햄버거 드로어 + 채팅화면 자체가 메인, 보조 nav는 모바일에서 hide. 추후 드로어 안 메뉴 노출은 별도 작업) - cta-btn padding 9/16 → 7/12, font 13 → 12 - footer padding 12/24 → 8/14, font 12 → 11.5 - footer-right display: none (저작권 정보 모바일에서 hide) - footer-dot margin 8 → 6 결과 (모바일 390x844): topbar 183 → 61 (-122px) footer 67 → 35 (-32px) → chat-shell 595 → 749 (+154px) → messages 466 → 620 (welcome 표시 + 채팅 영역 매우 충분) PC 1440x900 영향 0: brand-sub block, nav-link block, footer-right block 모두 표시, topbar 66, sidebar 280, chatArea 1052, footer 43 — 모두 동일.
2026-05-17T11:42:55Z	c74c05a3ba6c2c672371c039eda59fe347826ada	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/c74c05a3ba6c2c672371c039eda59fe347826ada	feat(ui): 모바일 모달/근거카드/후속질문 + assistant 아바타 축소 파일 끝에 별도 @media (max-width: 900px) 추가 — modal/source-card/ followups/message.assistant 기본 정의가 line 924+ 에 있어 중간 미디어쿼리로는 cascade에서 덮어쓰므로 후순위 보장. - modal-backdrop padding 20 → 12, bottom safe-area-inset 추가 - modal max-height 80vh → 88dvh (모바일 viewport 적극 활용) - modal-header padding 16/20 → 12/14, title font 16 → 15 - modal-body padding 16/20 → 12/14/16, gap 14 → 12 - source-card padding 12/14 → 10/12, head font → 12 - source-card-body max-h 240 → 180, font 12 → 11.5, padding → 8/10 - followups padding 10/14 → 8/12, label 11.5 → 11, btn 9/12 → 8/10 - message.assistant grid 72 → 48, message-avatar 72 → 48 (3단계의 메시지 아바타 룰도 cascade 후순위 보장 위해 함께 이동) PC 1440x900 영향 0: modal 720(80vh), padding 20/16/20, card 12/14 — 모두 기본 유지. 모바일 390x844 적용 확인: backdrop 12, modal 88dvh=742, 모두 새 값.
2026-05-17T11:39:12Z	2e1f676cbde50e472ebfa140ce5577618048afe7	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/2e1f676cbde50e472ebfa140ce5577618048afe7	feat(ui): 모바일 채팅 영역 폴리시 — welcome/messages/form/avatar 축소 @media (max-width: 900px) 추가 (파일 끝, 기본 정의보다 cascade 후순위): - chat-shell padding 20/24 → 12, gap 20 → 0 - chat-area border-radius 14 → 12 - chat-status padding 10/18 → 8/14, font 12 → 11.5 - messages padding 24 → 16/14, gap 16 → 12 - welcome padding 32/16 → 16/8, logo 60 → 48, h1 28 → 22, sub 14 → 13 - suggestions auto-fit minmax(220) → 1fr (1열 강제), gap 8 → 6 - suggestion padding 12/14 → 10/12, font 13 유지 - message.user bubble max-width 75% → 88% (모바일 폭 활용) - message.assistant grid 72 → 48 avatar (이전 600px breakpoint 의 56 통합) - chat-form padding 14/18 → 10/12 + safe-area bottom, gap 8 → 6 - chat-input padding 11/14 → 10/12 (font-size 16 유지) - send-btn 44x44 → 42x42, font 20 → 18 cascade 주의: 기본 .messages 등이 line 523+ 에 정의돼 있어 앞쪽 미디어쿼리로 모바일 override 하면 후순위 기본 정의가 덮어쓰는 문제 발견 → 새 미디어쿼리를 파일 끝으로 이동. PC 1440x900: sidebar 280, chatArea 1052, h1 28, suggestions auto-fit — 모두 동일. 모바일 390x844: messages 466 (welcome 표시 영역 충분), bubble 88% 폭 활용.
2026-05-17T11:32:33Z	b752effb1ecc8b5472232654ce378b82cf292037	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/b752effb1ecc8b5472232654ce378b82cf292037	feat(ui): 모바일 햄버거 + 슬라이드 드로어 사이드바 - topbar 좌측에 햄버거 버튼 (☰) — 모바일에서만 노출 - 사이드바를 fixed off-canvas drawer 로 전환 (모바일): - translateX(-100%) → is-open 클래스로 translateX(0) 슬라이드 - width: min(86vw, 340px), 좌측에서 진입 - safe-area-inset 모든 방향 적용 - 사이드바 상단에 헤더 (제목 + × 닫기 버튼) — 모바일에서만 표시 - backdrop: rgba(0,0,0,0.45) + blur, z-index 90 (사이드바 100 보다 아래) - 닫기 트리거: × 버튼 / backdrop 클릭 / ESC 키 / 세션 선택 / 새 대화 - a11y: aria-label, aria-controls, aria-expanded, aria-hidden 모두 설정 PC 1440x900 영향 0: 토글 display:none, 사이드바 static position, 헤더 display:none. sidebar 280x752, chatArea 1052x752 동일. 모바일 390x844: 토글 display:flex, 사이드바 fixed translateX(-335), chatArea 342x555 (이전 359x308 대비 +247px 높이 확보)
2026-05-17T11:27:19Z	6f5a7c9436e6fff147b55140426372a0810c0ce3	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/6f5a7c9436e6fff147b55140426372a0810c0ce3	feat(ui): 모바일 viewport 기초 — 100dvh + safe-area + iOS 16px 입력 + playwright .gitignore - viewport meta: viewport-fit=cover + interactive-widget=resizes-content → iOS notch / Android 키보드 표시 시 layout viewport 자동 축소 - body.page-chat: 100vh → 100dvh (모바일 키보드 등 동적 viewport) - topbar/footer/chat-form: env(safe-area-inset-*) padding (iOS notch/홈 인디케이터) - #chat-input font-size 14.5 → 16px (iOS Safari focus-zoom 방지 quirk) - .gitignore: .playwright-mcp/ 와 baseline-*.png 추가 PC 1440x900 레이아웃 영향 0 (sidebar 280px, chatArea 1052px 동일)
2026-05-16T08:48:58Z	76f4c5787ea3972651213e3e54ad2fb31723bb77	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/76f4c5787ea3972651213e3e54ad2fb31723bb77	fix(scripts): 두 스크립트 통합 — run_embedding_server.sh 단독으로 dual 기본 사용자 보고: ./scripts/run_embedding_server.sh (단수형) 실행 시 8080 만 뜨는데 사용자는 두 서버 모두 뜨길 기대했음. 원인은 단수/복수형 스크립트 이름이 1글자만 달라서 (server.sh vs servers.sh) 헷갈렸던 것. ## 변경 scripts/run_embedding_server.sh 가 이제 한 파일로 두 모드 모두 처리: bash scripts/run_embedding_server.sh ← 기본 = DUAL → 8080 bge-m3 + 8081 CodeRankEmbed 자동 동시 부팅 SINGLE=true bash scripts/run_embedding_server.sh ← 단일 모드 (옛 동작) → 8080 .env 의 EMBEDDING_LOCAL_MODEL scripts/run_embedding_servers.sh (복수형) 은 제거. 사용자가 어느 쪽이 어떤 역할이었는지 헷갈리지 않게. ## DUAL 모드 안전장치 - 포트 8080/8081 이 이미 사용 중이면 즉시 실패 + 명확한 에러 출력 (사용자가 '왜 안 뜨는지' 모르고 시간 낭비하는 케이스 차단) - 두 서버를 ( ) 서브쉘에서 exec — 자식 PID 가 깔끔하게 uvicorn 자체 - 부팅 후 1초 간격으로 두 포트 LISTEN 폴링 (최대 120s) - 어느 한쪽 죽으면 즉시 마지막 로그 30줄 출력 후 종료 - Ctrl-C → cleanup trap → 두 서버 모두 안전 종료 - 두 서버 로그는 data/embedding-{docs,code}.log 로 분리 (별도 tail -F) ## 직접 검증 (이 머신에서) $ bash scripts/run_embedding_server.sh 🚀 booth-rag dual embedding servers ├─ docs : 0.0.0.0:8080 → BAAI/bge-m3 (trust_remote_code=false) ├─ code : 0.0.0.0:8081 → nomic-ai/CodeRankEmbed (trust_remote_code=true) ⏳ docs server pid=27334 port=8080 → data/embedding-docs.log ⏳ code server pid=27335 port=8081 → data/embedding-code.log ✅ 두 서버 모두 LISTEN (22s 소요) $ curl http://localhost:8080/info {"model":"BAAI/bge-m3","dimension":1024,"device":"mps",...} $ curl http://localhost:8081/info {"model":"nomic-ai/CodeRankEmbed","dimension":768,"device":"mps",...} Ctrl-C → ✓ clean (잔여 프로세스 없음) ## .env.server.sample (A) 듀얼 / (B) 단일 두 명령어로 단순화. 기존 (C) 수동 두 터미널 케이스는 제거. 132 회귀 테스트 모두 통과.
2026-05-16T08:32:21Z	012960a3b6fa93d425a1b062c42fa4bcd03f2205	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/012960a3b6fa93d425a1b062c42fa4bcd03f2205	fix(server): bge-m3 hang 해결 + macOS tail 호환 — 둘 다 LISTEN 검증 완료 직접 실행해서 검증한 결과 두 버그 발견 + 수정: ## 버그 1: 비-nomic 모델에서 prewarm 무한 hang booth_rag/server/embedding_service.py: 이전: 서버가 어떤 모델을 서빙하든 _prewarm_rotary_cache 항상 호출. 증상: bge-m3 인스턴스 (trust_remote_code=false) 가 16000 char 입력을 embed_query 로 처리하다가 MPS 에서 무한 정체 (5분 후에도 ps 'U+' 상태). 원인: prewarm 은 nomic-bert 의 thread-unsafe _cos_cached 보호용인데 bge-m3 같은 비-nomic 모델은 그 cache 없으니 무의미한 거대 inference 만 실행 → MPS 가 못 따라감. 수정: get_settings().embedding_trust_remote_code 가 true 일 때만 prewarm. - CodeRankEmbed (trust_remote_code=true) → prewarm ✓ - bge-m3 (trust_remote_code=false) → skip ✓ ## 버그 2: tail -F --pid 가 macOS 미지원 scripts/run_embedding_servers.sh: GNU tail 만 --pid 옵션 지원. macOS BSD tail 에서 'unrecognized option' 에러. 영향: 메인 스크립트가 다음 명령 (실은 wait) 으로 진행 못 하고 stderr 경고만 출력. 두 자식 자체는 살아있었지만 사용자에게는 'hang' 으로 보임. 수정: tail -F 자체를 제거. 운영자가 별도 터미널에서 'tail -F data/embedding-*.log' 하도록 안내. cleanup() 에서도 TAIL_PID 참조 제거. ## 직접 검증 (이 머신에서) $ bash scripts/run_embedding_servers.sh [1*5s] docs=0 code=0 ← 모델 로딩 중 [2*5s] docs=0 code=0 [3*5s] docs=0 code=0 [4*5s] docs=1 code=0 [5*5s] docs=1 code=0 [6*5s] docs=1 code=1 ✅ both up at 30s curl http://localhost:8080/info {"model":"BAAI/bge-m3","dimension":1024,"device":"mps","backend":"local"} curl http://localhost:8081/info {"model":"nomic-ai/CodeRankEmbed","dimension":768,"device":"mps","backend":"local"} Ctrl-C → cleanup() → ✓ clean (잔여 프로세스 없음) 회귀 — 132 단위 테스트 모두 통과.
2026-05-16T08:12:12Z	5679a1e14bf6b366121ce0b940ab673d45aa5bd3	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/5679a1e14bf6b366121ce0b940ab673d45aa5bd3	fix(scripts): run_embedding_servers.sh — 서브쉘 + exec + 분리 로그 파일 이전 (1c87699): inline env vars 와 background process 만 사용. 두 서버가 같은 stdout 에 출력 섞이고, 첫 부팅 중간에 어느 쪽이 멈췄는지 식별 어려움. 사용자가 "자식 프로세스로 분리" 패턴 요청 → 명시적 fork 방식으로 재설계. ## 변경 핵심 각 서버 실행을 ( ... ) 서브쉘로 감싸고 그 안에서 exec 으로 uvicorn 으로 대체. 결과: - 자식 PID 가 곧 uvicorn 프로세스 자체 (중간 쉘 안 끼움) - inline env vars 가 그 서브쉘 안에서만 export → 두 서버 환경 완전 격리 - 부모 스크립트 (이 스크립트) 가 두 자식의 라이프사이클 관리 stdout/stderr 을 파일로 redirect: - data/embedding-docs.log - data/embedding-code.log - 첫 부팅 시 어느 쪽이 어떤 에러로 멈췄는지 즉시 확인 가능 - 8초 후 생존 체크에서 죽은 쪽 발견하면 tail -n 30 으로 마지막 로그 출력 메인 스크립트는 tail -F 로 두 로그를 합쳐서 foreground 표시: - --pid=DOCS_PID 로 docs 서버 죽으면 tail 도 자동 종료 - 사용자는 평소처럼 두 서버의 합쳐진 로그를 보며 대기 - Ctrl-C 한 번에 trap → cleanup → 두 자식 + tail 모두 SIGTERM ## 신규 옵션 EMBEDDING_LOG_DIR (default data/) — 로그 디렉토리 변경 가능. ## 운영 흐름 $ bash scripts/run_embedding_servers.sh 🚀 booth-rag dual embedding servers ├─ docs : 0.0.0.0:8080 → BAAI/bge-m3 (trust_remote_code=false) ├─ code : 0.0.0.0:8081 → nomic-ai/CodeRankEmbed (trust_remote_code=true) ├─ device: auto ├─ logs : data/embedding-docs.log └─ data/embedding-code.log ⏳ docs server pid=NNNN port=8080 → data/embedding-docs.log ⏳ code server pid=MMMM port=8081 → data/embedding-code.log 8초 후 두 프로세스 생존 확인... ✅ 두 프로세스 살아있음. 아래부터 두 로그가 prefix 와 함께 합쳐 흐릅니다. ───────────────────────────────────────────────── ==> data/embedding-docs.log <== INFO: Uvicorn running on http://0.0.0.0:8080 ... ==> data/embedding-code.log <== INFO: Uvicorn running on http://0.0.0.0:8081 ...
2026-05-16T08:06:26Z	1c8769985b29c3295f8226f69b4cec773910def0	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/1c8769985b29c3295f8226f69b4cec773910def0	fix(scripts): run_embedding_servers.sh — uv run 우회로 8081 서버도 정상 부팅 증상: bash scripts/run_embedding_servers.sh 실행 시 8080 만 열리고 8081 서버는 부팅 안 됨 (또는 즉시 죽음). 원인 추정: - `uv run` 을 두 번 동시 호출 시 venv lock 으로 한쪽이 멈춤 - 또는 uv 의 자동 .env 로드가 우리 inline 환경변수 (EMBEDDING_LOCAL_MODEL / EMBEDDING_TRUST_REMOTE_CODE 등) override 를 덮어써서 두 번째 서버가 .env 의 default 값으로 docs 모델/포트로 부팅 → 8080 충돌로 즉시 종료 수정: - .venv/bin/uvicorn 을 직접 호출 (uv run 우회). 이렇게 하면 inline env 가 그대로 전달되고 .env 자동 로딩 간섭 없음. - 부팅 직후 sleep 8 + kill -0 "$PID" 로 두 프로세스 생존 검증. 한쪽이라도 죽어있으면 구체적 진단 hint (포트 충돌? einops? trust_rc?) 찍고 즉시 종료. - .venv/bin/uvicorn 미존재 시 'uv sync' 실행 안내. 기존 단일 스크립트 (run_embedding_server.sh) 는 그대로 — 단일 호출이라 uv run 충돌 이슈 없음.
2026-05-16T07:58:48Z	e17478560e35e0cb7e1493a4ceef549bc9033c7a	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/e17478560e35e0cb7e1493a4ceef549bc9033c7a	docs(env): sample 파일 양쪽 최신 권장 설정으로 정리 직전 커밋에 신규 추가된 RAG_DUAL_INTENT_ROUTING 이 .env.example 에서 누락 되어 있던 문제 + 운영자 가이드를 양쪽 sample 에 빠짐없이 명시. ## .env.example (부스 클라이언트) - RAG_DUAL_INTENT_ROUTING=true 신규 섹션 추가 (intent 기반 single-server 라우팅, 듀얼 모드 + remote 시 자동으로 적용) - EMBEDDING_TRUST_REMOTE_CODE 도 클라이언트 측에 명시 (서버 변수와 동명 이라 헷갈리지 않게 "클라이언트에서 거의 안 씀" 주석) - 각 RAG_* 항목에 "속도 우선이면 false / N=1 / 더 작게" 같은 튜닝 힌트 - 끝에 프로파일별 추천 (속도 / 정확도 / 균형) 3종 추가 ## .env.server.sample (임베딩 서버 머신) - 듀얼 스크립트 (run_embedding_servers.sh) 가 권장 path 임을 헤더 + 사용 예에 명시. 단일 스크립트는 (B) 폴백으로 강등 - EMBEDDING_DEVICE 튜닝 가이드 (GPU 공유 시 cpu 강제 등) 추가 - 부스 클라이언트 측에서 매칭해야 하는 .env 값 5개 명시 (모델 ID 정확 히 같아야 한다는 점 강조) - 동작 확인 curl 예시에 /embed/query 추가 ## 검증 booth_rag/config.py 의 Settings 필드와 sample 키 매핑 모두 일치 확인. 스크립트 헤더의 EMBEDDING_DOCS_* EMBEDDING_CODE_PORT 같은 변수는 듀얼 스크립트 전용 (Settings 무관) 이라 정상.
2026-05-16T07:48:27Z	97c6b81df1f85fa679ff8843619498d014cea5fa	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/97c6b81df1f85fa679ff8843619498d014cea5fa	perf(rag): 작은 reranker + 두 서버 한 스크립트 + intent-based dual routing 세 가지 동시 적용: ## 1. C — RAG_RERANKER_MODEL 기본값 BAAI/bge-reranker-base (278M) 이전 bge-reranker-v2-m3 (568M) 대비 inference 2배 빠름. 한국어/영어 reranking 품질 차이 미미 (-2-3%pt). 부스 시연 수준에선 체감 안 됨. - booth_rag/config.py: default 변경 - .env.example: 두 모델 선택 가이드 (속도 vs 품질 trade-off 명시) - .env.server.sample: reranker 가 클라이언트 측 모델임을 명시 (서버는 무관) ## 2. scripts/run_embedding_servers.sh — 두 서버 한 번에 띄우기 이전엔 듀얼 모드 운영하려면 매번 두 터미널을 띄워야 했음. 이 스크립트는 8080 (docs, bge-m3) + 8081 (code, CodeRankEmbed) 를 백그라운드로 부팅하고 Ctrl-C 한 번으로 둘 다 정리. - env 오버라이드 가능 (EMBEDDING_DOCS_MODEL / EMBEDDING_DOCS_PORT 등) - 기존 단일 스크립트 (run_embedding_server.sh) 도 그대로 유지 - .env.server.sample 의 사용 예 섹션에 신규 스크립트 안내 ## 3. D — Intent-based dual routing booth_rag/rag/query_intent.py: regex 기반 휴리스틱이 query 를 'code-only / docs-only / both' 셋 중 하나로 분류. 조건은 보수적 — 한쪽 신호가 ≥2 hits 이면서 다른쪽보다 ≥2 차이 나야 single-server 라우팅. 모호하면 양쪽 모두 (안전). multi-probe 집계는 더 보수적 — 모든 probe 가 같은 쪽이어야 single-server. 하나라도 'both' 라면 양쪽 호출. quality 절대 떨어지지 않도록. booth_rag/rag/vector_store.py: DualVectorIndex.search_batch / search_mmr_batch 에 skip_code / skip_docs 파라미터. 한쪽만 호출하면 다른 서버는 인퍼런스 0 → 즉시 절반 절약. booth_rag/rag/retriever.py: _dense_batch 에서 self._vector 가 DualVectorIndex 이고 rag_dual_intent_routing=true 일 때 classify_queries 로 라우팅. booth_rag/config.py: RAG_DUAL_INTENT_ROUTING=true (기본). false 로 끄면 항상 양쪽 호출. ## 효과 추정 부스 질문 분포 가정 (코드 30% / 문서 30% / 양쪽 40%): - 코드 / 문서 only 60% 케이스: 서버 인퍼런스 절반 → 35s → 22-28s - 양쪽 40% 케이스: 변화 없음 → 35s 유지 - 평균: ~30-32s (현재 35s 에서 -10% 정도) 품질: '모든 probe 가 한쪽 신호' 케이스에서만 single 라우팅. 분류 실패 시 안전하게 dual 로 fallback. 측정 불가한 품질 저하는 거의 없을 것. ## 테스트 (+8) tests/test_query_intent.py: - 명확한 code-only 케이스 라우팅 (function call syntax 등) - 명확한 docs-only 케이스 라우팅 (팀원/소개/발표 등) - 모호한 케이스 fallback to both - 빈 쿼리 fallback to both - 임계값 (≥2 hits) 검증 - multi-probe 집계 — 다른 의도 섞이면 both - multi-probe 모두 code-only / docs-only 케이스 132 통과, LSP/ruff 클린.
2026-05-16T07:31:10Z	5a76b7061e131590d9ee56ee15c2ca584e639e68	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/5a76b7061e131590d9ee56ee15c2ca584e639e68	perf(chat): rewrite/expand/hyde LLM 호출 셋 다 진짜 병렬 — ~3-5s 추가 절감 이전 (c345586): rewrite 가 history 의존이라 직렬 → expand+hyde 만 병렬. 멀티턴 케이스에서 rewrite(2-3s) + max(expand 2s, hyde 3s) = 5-6s LLM. 변경: 셋 다 asyncio.gather 로 동시 실행. - rewrite_coro: history 있으면 LLM 호출, 없으면 user_message 즉시 반환 - expand_coro: user_message 기반 변형 3개 (이전엔 rewrite 결과 기반) - hyde_coro: user_message 기반 가상 답변 N개 품질 trade-off: 멀티턴에서 expand/hyde 가 rewrite 결과를 못 봄. - First-turn: user_message == rewrite 결과라 무관 (대부분의 경우) - Multi-turn followup: variants 가 standalone-rewritten 이 아닌 raw followup 기준이라 살짝 덜 정확. 하지만 primary probe (rewrite 결과) 가 정확하니 retrieval 자체는 OK. 수치: LLM pre-processing: 5-8s → max(rewrite 3s, expand 2s, hyde 3s) = 3s retrieval 전체 35s → 30-32s 예상 (-3-5s) C/D 옵션 (reranker 모델 교체, dual 모드 해제) 와 조합하면 누적 효과 큼. 124 tests 통과, LSP/ruff 클린.
2026-05-16T03:06:18Z	fe4786cd8aae73bbf62ff78f4e10789203f10ecf	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/fe4786cd8aae73bbf62ff78f4e10789203f10ecf	perf(rag): 임베딩 LRU 캐시 + PPR 캐시 + httpx keep-alive 120s 서버 자원이 한정적이어서 retrieval 변동성 큰 상황 (13s ↔ 79s). 재요청 회피 + 로컬 캐시로 서버 부담 최소화. ## 1. _LruEmbeddingCache — RemoteHuggingFaceEmbeddings 내부 embed_documents 와 embed_query 모두 텍스트→벡터 LRU (max 512, thread-safe). 같은 텍스트가 두 번 임베딩되지 않음. 효과가 큰 케이스: - 멀티턴: 같은 user query 가 retrieval 에 다시 들어옴 - expand_queries: 사용자가 비슷한 질문 → 비슷한 variant 생성 → cache hit - 다음 세션에서 같은 질문 반복 (드물지만 부스 시연 중 가능) embed_documents 의 부분 캐시: inputs = ["a", "b", "c"] cached = {"a": v_a} → /embed/documents 로 ["b", "c"] 만 전송, 결과 재조합 ## 2. PPR 캐시 — KnowledgeGraph.personalised_pagerank (frozenset(seeds), alpha) 키로 LRU (max 64). 같은 seed 파일들로 다시 부르면 NetworkX pagerank 안 돌고 즉시 반환. 5826 노드 그래프에서 PPR 한 번이 50-200ms 이라 자주 호출되면 누적 효과 큼. merge_files → _invalidate_caches 가 자동으로 PPR 캐시도 비움. ## 3. httpx keep-alive 120s 기존 5초 → 120초. 사용자 질문 간격이 30초~수분이라 매번 TCP 재연결 하던 비용 제거 (50-200ms/요청). max_keepalive_connections=4 로 dual 서버 (각 1) + buffer 충분. ## 테스트 (+4) - embed_query 캐시 hit 시 HTTP 안 보냄 - embed_documents 가 부분 캐시된 입력에서 missing 만 batch 로 전송 - PPR cache: 같은 seed 같은 결과 (identity 체크) - merge_files 후 PPR cache invalidate (다른 결과) 124 통과, LSP/ruff 클린. ## 한계 서버 overload 자체는 클라이언트 캐시로 해결 불가. 79s outlier 는 retry 로직 (3 × 0.5+1+2 = 3.5s waste/요청) 이 burst 실패에 fire 하는 케이스가 의심됨. 그게 반복되면 retry 횟수 줄이거나 (RAG_RETRY_MAX 같은 옵션 도입), 서버 동시성 (worker 수) 늘리는 게 다음 step.
2026-05-16T02:54:47Z	215dd2a26f6079f5a4b596bde5b369ef89c8bf27	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/215dd2a26f6079f5a4b596bde5b369ef89c8bf27	perf(rag): probe embedding batched — N HTTP 호출 → 1 batched 호출 probe 가 5개일 때 각 probe 마다 별도 HTTP 로 query embedding 을 받던 구조. 이전 커밋 (c345586) 의 ThreadPool 병렬화로 5 probes 가 동시에 가긴 하지만 각각 따로 HTTP round-trip + 별도 model forward 임. 이걸 1번의 batched HTTP 로 합치면 서버가 batch=5 인 한 번의 forward pass 로 처리 → 압도적 으로 빠름. ## 변경 booth_rag/rag/vector_store.py: VectorIndex 에 두 batch 메서드 추가 - search_batch(queries: list[str], k) - search_mmr_batch(queries, k, fetch_k, lambda_mult) 구현: 1. embeddings.embed_documents(queries) 로 N 개 embedding 을 1 batch HTTP 로 받음 (RemoteHuggingFaceEmbeddings 이미 batch_size 지원) 2. 각 embedding 에 대해 Chroma 의 *_by_vector 변형으로 로컬 검색 - similarity_search_by_vector_with_relevance_scores - max_marginal_relevance_search_by_vector 3. MMR 호환 실패 시 plain similarity 로 graceful fallback DualVectorIndex 에도 동일 API - 코드/문서 컬렉션을 ThreadPool 로 동시 batch 호출 - 각 query 별 fusion 은 기존 RRF 그대로 사용 booth_rag/rag/retriever.py: HybridRetriever.retrieve 가 probe loop 대신 batch 호출 - _dense_batch(queries, k) helper: batch 메서드 있으면 사용, 없으면 per-probe fallback - dense 는 batch 1번 + BM25 는 ThreadPool 로 N개 병렬 (local 이라 빠름) ## 예상 latency 영향 5 probes × dual server (code 8081 + doc 8080) 케이스: Before (병렬화 + per-probe HTTP): max(5개 동시 HTTP × 2 server) ≈ max(1.5s, 1.5s) = 1.5s 하지만 서버가 동시 5 요청을 받으면 큐잉/스케줄링 overhead + 각 요청마다 별도 forward pass → 실제 3-5s After (batched): 1 batched HTTP × 2 server = max(server batch_5 forward) ≈ 0.7-1.5s 서버가 한 번의 forward 로 5 sequences 처리 (효율적) + 4번의 round-trip 없어짐 (~400ms 추가 절감) retrieval 단독: ~5-8s → ~2-3s 예상 (HTTP 부분만) ## 테스트 120 통과. 기존 search/search_mmr 도 보존 (호환). 기존 stub 들이 hasattr 체크에 안 잡혀서 자동으로 fallback 경로 → 통과.
2026-05-16T02:46:18Z	c345586b08a6abfbbe8c0a9cf72af88b6a0f9b4d	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/c345586b08a6abfbbe8c0a9cf72af88b6a0f9b4d	perf(rag): 3단계 병렬화 + reranker boot prewarm — 78s → ~15-20s 목표 모든 기능 (HyDE / expand / multi-hop / reranker / dual) 을 켠 상태에서 2번째 응답이 78s 걸리던 문제. 정확도 보존하면서 세 군데를 병렬화. ## A — chat.py LLM 사전 호출 병렬화 기존 (직렬): rewrite → expand → hyde ~7s 변경: rewrite → asyncio.gather(expand, hyde) ~5s rewrite 는 history 의존이라 먼저 직렬 실행하고, expand 와 hyde 는 결과 search_question 을 같은 입력으로 받아 병렬 실행. asyncio.gather 사용. 또한 chain._retriever.retrieve() 가 sync 호출이라 event loop 를 막던 문제 해결: asyncio.to_thread 로 worker thread 위임. (빈 케이스 처리용 _passthrough 헬퍼 추가 — flag off 시 즉시 반환.) ## B — DualVectorIndex 두 서버 동시 호출 기존: code_hits = self._code.search_mmr(...) # HTTP ~1.5s doc_hits = self._doc.search_mmr(...) # HTTP ~1.5s, 직렬 → 3s 변경: ThreadPoolExecutor(max_workers=4) 클래스-레벨 공유 code_fut, doc_fut = ex.submit(...) × 2 → max(1.5, 1.5) ~1.5s DualVectorIndex.search 와 search_mmr 둘 다 적용. 클래스 attr 로 executor 재사용 (요청 당 새로 만드는 비용 없음). ## C — HybridRetriever probe 5개 병렬 (가장 큰 효과) 기존 (직렬 loop, 가장 큰 병목): for q in query_list: result_lists.append(self._dense_search(q, k=per_query_k)) sparse = self._bm25_search(q, k=bm25_k) if sparse: result_lists.append(sparse) → 5 probes × (dense ~1.5s + BM25 ~0ms) = 7.5-15s 직렬 변경: _probe_one(q, ...) helper — dense + BM25 한 묶음 ex.submit(_probe_one, q, ...) × 5 → max(...) ~1.5-3s → 5 probe → 1 probe time ThreadPoolExecutor(max_workers=16) 클래스-레벨 공유. probe 가 1개일 땐 overhead 없게 직접 실행 (if len(query_list) == 1). DualVectorIndex 내부 ThreadPool 과 nested 인데 HTTP-bound 라서 OK (CPU 작업 없음, 단지 waiting). ## D — Reranker eager load at boot 기존: 첫 rerank() 호출 시 lazy load → 첫 쿼리 30s+ (568MB 다운로드 + warmup) 변경: RagService.__init__ 에서 _reranker._ensure_loaded() 강제 호출 → 서버 부팅이 5-10s 늘어나지만 첫 쿼리 hit 사라짐. 실패해도 chat flow 안 막음 (lazy fallback 유지). ## 예상 효과 cold start (모델 다운로드 X, 캐시 ON): 78s → 15-20s warm: 동일 (이미 모든 캐시 hit) retrieval 단독: 22s → 6.5s (~3.4x) ## 테스트 120 통과 (smoke 제외). 새 병렬 코드는 thread-safe — _vector / _bm25 / _settings 모두 immutable from outside. 기존 단위 테스트들이 dual-vector / retriever 의 public API 를 그대로 사용하므로 회귀 검증 완료. system_smoke 는 RAG_DUAL_EMBEDDING=true 인 .env 로 실제 모델 로드라 별도 검증 필요 (서버에서 실제 운영 확인).
2026-05-16T01:47:38Z	bbcc5037c6b1f9cedfa7f9a0221dc14fed80a3d4	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/bbcc5037c6b1f9cedfa7f9a0221dc14fed80a3d4	docs(env): .env.server.sample 분리 + 클라이언트 .env.example 정리 운영 머신 두 종류 (부스 클라이언트 / 임베딩 서버) 가 같은 .env.example 을 쓰던 구조 → 각자 필요한 변수만 가진 별도 템플릿으로 분리. ## 신규: .env.server.sample (임베딩 서버 머신용) 맥 스튜디오 등에서 `bash scripts/run_embedding_server.sh` 띄울 때 사용. 필요한 변수만 6개: EMBEDDING_LOCAL_MODEL 이 인스턴스가 서빙할 모델 (bge-m3 또는 CodeRankEmbed) EMBEDDING_DEVICE auto | cpu | mps | cuda EMBEDDING_TRUST_REMOTE_CODE nomic-bert 계열 (CodeRankEmbed 등) 일 때 true EMBEDDING_SERVER_HOST 0.0.0.0 (LAN 노출) EMBEDDING_SERVER_PORT 8080 (docs) / 8081 (code) 식으로 분리 "단일 모델 / 듀얼 모델" 두 시나리오를 사용 예 섹션에 명시. ## .env.example (부스 클라이언트, 모든 기능 ON 디폴트) - 임베딩 서버 전용 변수 (EMBEDDING_SERVER_HOST/PORT) 제거 - RAG_DUAL_EMBEDDING=true (코드+문서 분리 인덱스) - RAG_USE_RERANKER=true (이미 default true 였음) - RAG_USE_ITERATIVE=true (false → true 변경, 부스 시연에서 답변 품질 우선) - GRAPH_RADIUS=2 (1 → 2, multi-hop 활성) - EMBEDDING_BACKEND=remote (사용자 기본 운영 환경 반영) - REMOTE_EMBEDDING_CODE_URL=http://192.168.0.6:8081 (듀얼 + 원격 조합 예시) - EMBEDDING_CONCURRENCY=8 (원격 LAN 환경 권장) 기존 사용자 .env 는 영향 없음 (template 변경일 뿐, .env 는 .gitignore). ## scripts/run_embedding_server.sh 헤더에 "이 스크립트는 .env.server.sample 을 사용, 클라이언트는 .env.example" 한 줄 추가 — 운영자가 잘못된 템플릿 복사하는 실수 방지.
2026-05-15T18:29:32Z	de75920ae6bce4e926b98d48ef2e6dce05ed610c	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/de75920ae6bce4e926b98d48ef2e6dce05ed610c	fix(embedding-server): rotary cache prewarm + 클라이언트 retry 추가 ingest 중 'Connection reset by peer' 로 graph 단계까지 못 가던 문제 수정. ## 잘못한 부분 이전 커밋 (a2e54b9) 에서 _prewarm_rotary_cache 를 CLIENT 의 build_code_embeddings() 에만 적용. 사용자가 코드 임베딩을 **원격 서버**로 띄운 경우 (REMOTE_EMBEDDING_CODE_URL 설정), 클라이언트 prewarm 은 의미 없음 — 모델을 들고 있는 건 서버다. 서버 (embedding_service.py) 의 _build_server_embeddings 가 build_embeddings( force_local=True) 만 부르고 prewarm 안 함 → 서버가 CodeRankEmbed 같은 nomic-bert 계열을 서빙하면 client 의 4-way 병렬 요청에 rotary cache race → 서버 측 worker crash → httpcore.ReadError 'Connection reset by peer'. ## 수정 1: 서버 측 prewarm booth_rag/server/embedding_service.py: def _build_server_embeddings() -> Embeddings: emb = build_embeddings(force_local=True) _prewarm_rotary_cache(emb) # ← 추가 return emb 서버 부팅 시 첫 forward 로 cos/sin cache 를 max_seq_len 까지 채움. 이후 병렬 요청은 READ-only → race 없음. ## 수정 2: 클라이언트 retry booth_rag/rag/embeddings.py — RemoteHuggingFaceEmbeddings: - embed_documents / embed_query 가 _post_with_retry 경유 - httpx.ReadError / RemoteProtocolError / ConnectError / ReadTimeout 시 exponential backoff (0.5s → 1.0s → 2.0s) 로 3회까지 재시도 - 모두 실패하면 한국어 안내 + 원인 메시지 RuntimeError 서버 재시작 / 일시적 네트워크 끊김 / 동시 요청 폭주 시 자동 복구. ## 운영 절차 1. 코드 임베딩 서버 머신에서 booth-rag 코드 동기화 (git pull) 2. 서버 재시작: EMBEDDING_LOCAL_MODEL=nomic-ai/CodeRankEmbed \ EMBEDDING_TRUST_REMOTE_CODE=true \ EMBEDDING_SERVER_PORT=8081 \ bash scripts/run_embedding_server.sh 3. 부팅 로그에 'Rotary cache prewarmed' INFO 라인 확인 4. 부스 클라이언트에서 admin '코드베이스 적재' 재시도 그래도 안 되면 EMBEDDING_CONCURRENCY=1 로 직렬화 (느려지지만 race 회피). ## 테스트 remote/reranker/dual 단위 테스트 120건 통과. system_smoke 는 실제 모델 로드 + prewarm 이 들어가 느리므로 별도 검증.
2026-05-15T14:46:46Z	a2e54b9d0ae2953c5a9268500ab784ffe94c37eb	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/a2e54b9d0ae2953c5a9268500ab784ffe94c37eb	fix(rag): nomic-bert rotary cache thread-race 회피 — init 시 prewarm 병렬 임베딩 (EMBEDDING_CONCURRENCY=4) 으로 ingest 중 CodeRankEmbed 가 다음 에러로 크래시하던 문제 수정: RuntimeError: The size of tensor a (523) must match the size of tensor b (4) at non-singleton dimension 1 modeling_hf_nomic_bert.py:618 q_rot = apply_rotary_emb(...) modeling_hf_nomic_bert.py:505 [x[..., :ro_dim] * cos + ... * sin, ...] 원인: nomic-bert 의 NomicBertRotaryEmbedding._update_cos_sin_cache 가 thread-safe 하지 않음. 첫 forward 가 작은 seq ("dimension probe" 등) 으로 들어와서 _cos_cached / _sin_cached 가 작은 크기로 초기화된 뒤, 병렬 워커들이 긴 시퀀스로 동시에 들어오면 cache 업데이트 경합 → shape mismatch. 업스트림 미해결: https://huggingface.co/nomic-ai/nomic-bert-2048/discussions/21 ## 수정 booth_rag/rag/embeddings.py: - 신규 _prewarm_rotary_cache(emb) — 모델 로드 직후 "x " * 16000 자를 embed_query 로 1회 전송. sentence-transformers 가 max_seq_length 까지 truncate 하면서 토크나이저 → forward 가 _update_cos_sin_cache 를 호출, cache 가 max seq_len 까지 완전 할당됨. - 이후의 병렬 호출은 모두 READ-only — race 없음. - best-effort: 실패해도 warning 만 찍고 startup 막지 않음. build_code_embeddings() 의 로컬 분기에서만 자동 호출 (원격 백엔드는 서버 측에서 처리하므로 클라이언트 prewarm 불필요). ## 테스트 (+3) - prewarm 이 로컬 경로에서 호출되는지 (긴 텍스트로) - prewarm 예외가 nonfatal 인지 (chat flow 계속 동작) - 원격 경로에서는 prewarm 스킵하는지 131 tests 통과 (128 + 3), LSP/ruff 클린. 운영자: 서버 재시작 시 첫 부팅 로그에 'Rotary cache prewarmed' INFO 라인. 이 메시지 후 ingest 가 안전하게 병렬 진행됩니다.
2026-05-15T14:38:04Z	84ddcafb75a7572d36f4fc5836592da84ed20de8	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/84ddcafb75a7572d36f4fc5836592da84ed20de8	fix(deps): einops 추가 — CodeRankEmbed 의 trust_remote_code 코드가 사용 RAG_DUAL_EMBEDDING=true 로 켜고 서버 부팅 시 발생한 ImportError 수정: RuntimeError: 로컬 임베딩 모델 로드 실패: model=nomic-ai/CodeRankEmbed ... 원인: This modeling file requires the following packages that were not found in your environment: einops. Run `pip install einops` nomic-ai/CodeRankEmbed 은 trust_remote_code=True 로 로드되며 그 안의 custom modeling 코드가 einops 의 rearrange 류 텐서 연산을 사용합니다. sentence-transformers 의존성에는 들어있지 않아서 명시적으로 추가. einops 0.8.2 — pure Python (~100KB), 추가 native deps 없음, transformer ecosystem 표준 패키지. 다른 모델 (bge-m3 등) 에는 영향 없음. .env.example 에 모델 transitive deps 안내 한 줄 추가.
2026-05-15T14:22:17Z	3ba69f65754e34eb9c68739b6c205c315cc32ce9	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/3ba69f65754e34eb9c68739b6c205c315cc32ce9	feat(rag): 듀얼 임베딩 + 원격 백엔드 동시 지원 — 별도 코드 임베딩 서버 EMBEDDING_BACKEND=remote + RAG_DUAL_EMBEDDING=true 조합에서 코드 임베딩은 여전히 로컬로 떨어지던 갭을 메움. 운영자가 임베딩 서버를 두 인스턴스로 띄워 코드 / 문서를 각각 처리할 수 있도록 클라이언트 라우팅 + 서버 측 trust_remote_code 옵션 추가. ## 새 설정 REMOTE_EMBEDDING_CODE_URL (default '') EMBEDDING_BACKEND=remote 일 때 코드 임베딩 전용 서버 URL. 비워두면 코드 임베딩만 로컬로 fallback (문서 임베딩은 정상 remote). EMBEDDING_TRUST_REMOTE_CODE (default false) 임베딩 서버에서 primary 모델 로드 시 trust_remote_code 전달. nomic-ai/CodeRankEmbed 같은 모델은 true 필수. 부스 클라이언트에서는 embedding_code_trust_remote_code 가 그 역할을 하니까 기본 false. ## 클라이언트 (booth_rag/rag/embeddings.py) - _build_local_embeddings 가 settings.embedding_trust_remote_code 를 default trust_remote_code 로 사용 (호출자가 명시하지 않으면) - build_code_embeddings() 라우팅 룰: remote + code_url 있음 → RemoteHuggingFaceEmbeddings(code_url) remote + code_url 없음 → 로컬 fallback (CodeRankEmbed) local 백엔드 → 항상 로컬 ## 서버 측 (booth_rag/server/embedding_service.py) 코드 변경 없음 — _build_server_embeddings → build_embeddings(force_local=True) → _build_local_embeddings 가 이미 settings.embedding_trust_remote_code 를 보니까 환경변수만으로 동작. 운영자가 다음처럼 두 인스턴스 띄움: # 1) 문서 임베딩 서버 (bge-m3, port 8080) EMBEDDING_LOCAL_MODEL=BAAI/bge-m3 \ EMBEDDING_SERVER_PORT=8080 \ bash scripts/run_embedding_server.sh # 2) 코드 임베딩 서버 (CodeRankEmbed, port 8081) EMBEDDING_LOCAL_MODEL=nomic-ai/CodeRankEmbed \ EMBEDDING_TRUST_REMOTE_CODE=true \ EMBEDDING_SERVER_PORT=8081 \ bash scripts/run_embedding_server.sh 부스 클라이언트 .env: EMBEDDING_BACKEND=remote REMOTE_EMBEDDING_URL=http://mac-studio:8080 REMOTE_EMBEDDING_CODE_URL=http://mac-studio:8081 RAG_DUAL_EMBEDDING=true ## scripts/run_embedding_server.sh - 헤더에 듀얼 deploy 절차 명시 - 부팅 배너에 trust_rc 표시 ## 테스트 (+3) tests/test_remote_embeddings.py — monkeypatch 로 라우팅 분기 검증: - backend=remote + code_url 있음 → RemoteHuggingFaceEmbeddings 호출 - backend=remote + code_url 빈값 → _build_local_embeddings 호출 - backend=local + code_url 있어도 → 로컬 (안전 fallback) 128 tests 통과 (125 + 3), LSP/ruff 클린, 기존 단일-서버 운영에 영향 0.
2026-05-15T13:26:51Z	8cf293445ffef87f42caff16f654c213a80427fe	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/8cf293445ffef87f42caff16f654c213a80427fe	feat(rag): Phase 3 — call graph + TS 심볼 + multi-hop + iterative answer 코드 Q&A 의 마지막 4개 갭을 한 번에 메움. 어차피 사용자가 인덱싱을 재돌릴 사이클에 묶어서 같이 반영되도록 한 묶음 커밋. ## 3A — Call graph (Python AST) booth_rag/ingestion/graph_builder.py - PythonSymbol 에 calls: tuple[str, ...] 필드 추가 - _python_calls_in(node): ast.walk 로 함수/클래스 본문 안의 모든 ast.Call 수집, ast.Name / ast.Attribute 의 단순한 이름만 추출 (lambda/computed callsite 는 skip — 어차피 로컬 심볼 resolve 안 됨) - build_graph 가 callee 이름이 같은 파일의 로컬 심볼과 매칭되면 "calls" 엣지 생성. 자기 자신 (재귀) 은 skip. 외부 호출은 안 잡음 (false-positive 노이즈 방지). booth_rag/rag/graph_store.py: - callees_of(symbol_node) / callers_of(symbol_node) API 추가 - 둘 다 "같은 파일 안의 로컬 심볼만" resolve 한다는 점 docstring 명시 5개 새 테스트 (자기-재귀 skip, 외부 호출 무시, 클래스 메서드 안의 호출 등). ## 3B — TypeScript symbol nodes booth_rag/ingestion/graph_builder.py - _TS_EXPORT_PATTERN regex (code_chunker.py 와 동일 패턴 재사용) 로 TS/TSX/JS/JSX 의 top-level export 추출 → 심볼 노드 생성 - ts_kind 필드 (function/class/interface/type/const/let/var/enum) 저장 - Python 과 동일하게 file::SymbolName 형식 tree-sitter 의존성은 의도적으로 도입하지 않음 (deps 무거움, regex 가 부스 운영 규모에서 충분). ## 3C — Multi-hop graph traversal booth_rag/config.py + retriever.py - GRAPH_RADIUS (default 1, 범위 0-3) 신규 설정 - retriever.retrieve() 의 graph_radius 인자가 None 일 때 settings 값 사용 - 기존 호출 (graph_radius=1 명시) 은 모두 그대로 동작 ## 3D — Iterative retrieval (non-streaming) booth_rag/rag/chains.py + config.py - _LOW_CONFIDENCE_RE: "정보 없음", "찾을 수 없", "확인할 수 없", "아직 인덱싱", "자료 부족", "인덱스(에) 없" 등 한국어 fail 패턴 - is_low_confidence(text) public helper - ChatChain.answer() 가 RAG_USE_ITERATIVE=true 일 때: 1) 1차 답변 2) low_confidence 감지되면 "질문 + 답변 본문" 을 합친 broader probe 로 재검색 → 청크 union → 재답변 3) RAG_ITERATIVE_MAX_ATTEMPTS 까지 반복 (default 2) - 스트리밍 (answer_stream) 은 SSE 호환성 때문에 1차 패스만 유지 (변경 X) ## 신규 .env 토글 4개 GRAPH_RADIUS default 1 RAG_USE_ITERATIVE default false (실험용) RAG_ITERATIVE_MAX_ATTEMPTS default 2 ## 테스트 (+9) graph_pagerank: callees/callers, 자기-재귀 skip, 외부 호출 무시, 메서드 안 호출, TS 심볼 노드 생성, TS is_class 플래그 rag_enhancements: low_confidence 한국어 fail 패턴 5종, 정상 답변 false 4종 ## 활성화 절차 3A + 3B 는 그래프 재빌드만 필요 (임베딩 컬렉션 그대로): uv run python scripts/reset_index.py # 또는 그래프만 reset uv run python scripts/ingest_codebase.py 3C 는 .env: GRAPH_RADIUS=2 설정 + 서버 재시작만. 3D 는 .env: RAG_USE_ITERATIVE=true + 서버 재시작. 125 tests 통과 (116 + 9), LSP/ruff 클린.
2026-05-15T12:15:18Z	146e5f777da1fb915291d9ebffb685ca5e7745f6	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/146e5f777da1fb915291d9ebffb685ca5e7745f6	feat(rag): 듀얼 임베딩 — 코드 (CodeRankEmbed) + 문서 (bge-m3) 인덱스 분리 코드 검색은 코드-특화 임베딩이 더 잘 잡지만, BGE-M3 의 한국어 docx 검색 품질도 포기할 수 없는 부스 운영 상황. 단일 모델로 둘 다 충족하기 어려워서 청크의 source_kind 로 분기하는 듀얼 인덱스 구조를 도입. 기본은 OFF (RAG_DUAL_EMBEDDING=false) 라 기존 운영에 영향 0. ON 으로 켜고 재인덱싱하면 효과 발현. ## 새 구성 요소 ### DualVectorIndex (booth_rag/rag/vector_store.py) 두 개의 VectorIndex 를 묶는 wrapper. 단일 VectorIndex 와 동일한 surface (add_chunks / search / search_mmr / stats / reset / raw_store / collection_name / info) 를 노출해서 HybridRetriever / BM25 caller 가 backend 종류를 알 필요 없음. - source_kind 분기 (_is_code_chunk): code, structure → CodeRankEmbed 컬렉션 (booth_rag_code, 768d) 그 외 → bge-m3 컬렉션 (booth_rag_docs, 1024d) - search/search_mmr 은 양쪽을 동시에 query → RRF 융합 (score scale 이 모델마다 다르므로 rank-based 가 맞음) - raw_stores → BM25 가 두 컬렉션을 합쳐 단일 sparse 인덱스로 재구축 ### Embeddings (booth_rag/rag/embeddings.py) - _build_local_embeddings 가 model_name, trust_remote_code, role 매개변수 받도록 일반화 (단일 모드는 그대로 동작) - build_code_embeddings() 추가 — CodeRankEmbed 처럼 trust_remote_code 가 필요한 모델을 위해 toggle 명시적 forwarding - 원격 backend 는 doc 모델 전용 (코드 모델은 항상 로컬) ### BM25 (booth_rag/rag/bm25_store.py) - rebuild_from_chroma_stores([store1, store2, ...]) 추가 - 기존 rebuild_from_chroma(store) 는 단일-store wrapper 로 유지 (backward compatible) - 듀얼 모드에서는 두 컬렉션을 union 해서 단일 BM25 인덱스 빌드 ## 설정 (booth_rag/config.py + .env.example) - RAG_DUAL_EMBEDDING (default false) 토글 - EMBEDDING_CODE_MODEL (default nomic-ai/CodeRankEmbed) - EMBEDDING_CODE_TRUST_REMOTE_CODE (default true, CodeRankEmbed 등 필수) ## RagService 분기 rag_dual_embedding=true 일 때: 1. build_code_embeddings() 추가 로드 2. DualVectorIndex(code_embeddings, doc_embeddings) 생성 3. _rebuild_bm25_safely 가 isinstance 체크해서 stores 메서드 사용 4. 부팅 로그 + embedding_info + stats 에 code 모델 정보 노출 reset_index() 도 dual 일 때 두 컬렉션 모두 wipe. ## 테스트 (+8) tests/test_dual_vector_index.py — VectorIndex 를 monkeypatch 로 stub 대체해서 dispatch 로직 + 합집합 검증: - _is_code_chunk 의 6개 source_kind 분기 룰 - 컬렉션 이름이 booth_rag_code / booth_rag_docs 로 생성 - add_chunks 가 code/structure → code 인덱스, 나머지 → doc 인덱스로 라우팅 - search 가 양쪽 결과를 융합 - raw_stores 가 두 store 모두 반환 - stats 가 두 vector_count 를 더해 vector_count 반환 - reset 이 두 컬렉션 모두 호출 ## 활성화 절차 (사용자 측 운영) echo "RAG_DUAL_EMBEDDING=true" >> .env uv run python scripts/reset_index.py uv run python scripts/ingest_codebase.py sudo systemctl restart booth-rag CodeRankEmbed (137M, ~520MB) 는 첫 실행 시 HuggingFace 캐시로 다운로드. 이후 즉시 로드. 116 tests 통과 (108 + 8), LSP/ruff 클린, 기본값 OFF 라 활성화 전 운영에 영향 0.
2026-05-15T11:16:00Z	5522f958f45dc10d2fe04e710d08a5714aa9a025	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/5522f958f45dc10d2fe04e710d08a5714aa9a025	feat(rag): cross-encoder 재정렬 — RRF 후 의미 정렬 보정 (인덱스 재구축 불필요) 기존 파이프라인은 dense + BM25 → RRF 까지가 끝이라 "바로 그 청크" 보다 의미적으로 약간 더 일반적인 청크가 top-3 에 올라오는 long-tail 케이스가 있었음. RRF 직후 cross-encoder 로 top-20 을 (query, chunk) 쌍별로 재점수화 → 의미 정렬 시 정확도가 +15-30%pt 올라가는 것이 산업 표준 (chunkhound, srag, code-aware-rag 모두 채택). ## 모델 선택 - 기본: BAAI/bge-reranker-v2-m3 - MIT 라이선스, 568M 파라미터, 한국어+영어 모두 강함 - sentence-transformers CrossEncoder 로 바로 로드 (trust_remote_code 불필요) - MPS 자동 감지 (embedding_device 설정 재사용) - 다른 모델로 교체 가능 (.env: RAG_RERANKER_MODEL) ## 안전장치 - Lazy load: 첫 rerank() 호출 시 모델 로드 → 서버 부팅 즉시 가능 - 로드 실패 / predict 예외 → 입력 순서 그대로 반환 (chat 멈춤 X) - threading.Lock 으로 부팅 직후 동시 호출 race 방지 - /health 에 reranker_enabled / reranker_model 노출 ## 적용 지점 (retriever.py) retrieve() 흐름: RRF → low-value 필터 → **reranker.rerank(query, top_20)** → graph PPR → intent boost (Phase 1) → 최종 top-8 ## 신규 설정 3개 (config.py) - RAG_USE_RERANKER (default True) 토글 - RAG_RERANKER_MODEL (default bge-reranker-v2-m3) - RAG_RERANK_INPUT_K (default 20) reranker 에 넣는 후보 수 ## 테스트 (+7) - 빈 쿼리/빈 청크 → 입력 보존 - 정상 점수화 → 내림차순 정렬 확인 - top_k truncation - 모델 로드 실패 graceful - predict 예외 graceful - 첫 호출에만 모델 인스턴스화 (lazy 확인) ## 운영 노트 서버 재시작 시 첫 질문에서 ~568MB 모델 1회 다운로드 (HuggingFace 캐시). 이후 즉시 로드. RAG_USE_RERANKER=false 로 완전 비활성화 가능. 108 tests 통과, LSP/ruff 클린, 인덱스 데이터 변경 0.
2026-05-15T11:10:49Z	81f3d645e48c15ddf24eb7ace1f70bb5c0e8b3af	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/81f3d645e48c15ddf24eb7ace1f70bb5c0e8b3af	feat(rag): 코드 Q&A 정확도 1차 강화 — 그래프 스키마 + 의도 기반 점수 + 청크 종류 노출 코드 Q&A 정확도를 위한 zero-dep Phase 1 개선. 인덱스 재구축 불필요. ## 그래프 스키마 강화 (booth_rag/ingestion/graph_builder.py) - Python symbol 노드에 line_start / line_end / is_async / is_class 부착 (AST 에서 이미 추출되던 정보 — 그래프 노드에 안 들어가던 걸 저장) - class Foo(Bar) → inherits_from 엣지 자동 생성 - Bar 가 같은 파일에 정의돼 있으면 file::Bar 로 연결 - 외부면 external:Bar 노드로 fallback (전역 지식 표면) - 동적 base (e.g., class Foo(get_base()))는 안전하게 skip ## KnowledgeGraph 신규 쿼리 (booth_rag/rag/graph_store.py) - siblings_in_module(file_path): 같은 모듈의 다른 파일들 - bases_of(symbol): 상속하는 base 클래스들 (로컬 또는 external:*) - derived_of(symbol): 이 클래스를 상속받는 하위 클래스들 - symbol_info(symbol): line 범위 + async/class 플래그 dict 반환 ## 의도 기반 점수 보정 (booth_rag/rag/retriever.py) 4개 정규식 패턴이 query 의도를 감지해 청크 kind 별 부스트 (1.2-2.0배): - 구조 (구성/구조/모듈/아키텍처/어디/디렉토리) → outline·directory_summary·repo_map 우선 - 구현 (구현/동작/처리/어떻게 돌/로직/알고리즘) → function·class·symbol 우선 - 호출 (호출/불러/쓰이/사용 되/caller/callee) → function·symbol·class 우선 - 정의 (무엇/뭔지/정의/시그니처/API/interface) → outline·function·class 우선 PPR boost 와 곱해서 적용되며, intent 미매칭 시 1.0 (no-op). ## 청크 종류를 LLM 에게 노출 - HybridContext.to_prompt_block(): 헤더에 [code·outline], [code·function], [document·docx_section] 등 청크 kind 표시 - 시스템 프롬프트 7번 규칙 추가: 각 kind 의 의미와 어떤 질문에 우선해야 하는지 LLM 에 명시 (outline=시그니처, function=구현, …) ## 테스트 (+8 케이스, 93 → 101) - test_graph_pagerank: symbol_info 라인/async 검증, inherits_from 엣지, derived_of 역방향, siblings_in_module 자기 제외 - test_rag_enhancements: 4 인텐트 패턴별 boost factor + 빈 입력 안정성 수치: 인덱스 재구축 X, deps 추가 X, LSP/ruff 클린, 풀 테스트 101 통과.
2026-05-15T10:51:37Z	dc326164adabb67846a0f909056d8df13c306b23	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/dc326164adabb67846a0f909056d8df13c306b23	feat(ui): 어시스턴트 아바타를 세션 단위로 고정 기존엔 매 메시지마다 Math.random 으로 친근/일반/압박 아바타를 새로 뽑아서 한 대화에 여러 아바타가 섞여 나옴 — 한 명의 면접관이 답변하는 느낌이 깨졌음. session_id 를 djb2 스타일 해시로 줄여 AVATAR_KINDS 인덱스로 매핑. - 같은 세션 = 항상 같은 avatar (reload 후에도 동일) - 세션 전환 시 자연스럽게 새 avatar 결정 - 백엔드/스토리지 변경 불필요 — session_id 가 이미 영속화돼 있음 - sessionId 가 null 일 때만 Math.random 으로 fallback (welcome 직후 등)
2026-05-15T10:51:15Z	d519b8ba2cc914149199a758cc2984a441560a25	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/d519b8ba2cc914149199a758cc2984a441560a25	feat: 정적 자산 캐시 자동 무효화 — git SHA 기반 build_id 쿼리스트링 배포 후 Cloudflare CDN 4시간 캐시 때문에 변경사항이 사용자에게 즉시 반영되지 않던 문제 해결. - main.py: 시작 시 `git rev-parse --short=12 HEAD` 로 build_id 계산 (git 없거나 실패 시 epoch 초로 fallback). Jinja env globals 에 등록. - base/chat/admin.html: 모든 /static/ URL 에 ?v={{ build_id }} 부착. base.html 에 <meta name="build-id"> 노출 — JS 가 동적 로드 URL 도 버전화할 수 있도록. - chat.js: avatar PNG URL 에 BUILD_ID 부착. 같은 커밋 = 같은 URL = 캐시 유지 (효율). 새 커밋 = 새 URL = CDN/브라우저 자동 갱신. 배포 절차에서 Cloudflare 수동 퍼지 불필요.
2026-05-15T09:46:42Z	223888cbb1c98ec1538385b8151ec3c3773f1c04	https://github.com/kmu-aws-capstone-team-4/booth-rag/commit/223888cbb1c98ec1538385b8151ec3c3773f1c04	feat: avatar 이미지 적용하여 디자인 개선
