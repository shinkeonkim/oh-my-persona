GitHub repository: https://github.com/shinkeonkim/paeon
Pull request author: shinkeonkim

2026-08-28T08:52:59Z	#3	MERGED	https://github.com/shinkeonkim/paeon/pull/3	fix(deploy): correct clusterIssuer name to cloudflare-dns01	merged=2026-08-28T09:10:03Z	closed=2026-08-28T09:10:03Z	commits=1	files=1	+3/-1	letsencrypt-prod doesn't exist in this cluster (kubectl get clusterissuer shows only cloudflare-dns01) - the Certificate has been stuck 'Issuing' unable to actually issue since deploy.
2026-08-28T08:01:51Z	#2	MERGED	https://github.com/shinkeonkim/paeon/pull/2	fix(ci): also push :latest tag	merged=2026-08-28T08:14:44Z	closed=2026-08-28T08:14:44Z	commits=1	files=1	+3/-1	The Helm chart defaults image.tag to 'latest', but CI only ever pushed :${{ github.sha }} - so every pod (Deployment, migrate initContainer, CronJob) ImagePullBackOffs pulling a tag that never existed. Push both tags, matching boj-solution-archiving-site's CI.
2026-08-28T04:06:52Z	#1	MERGED	https://github.com/shinkeonkim/paeon/pull/1	feat: Django 6.1 upgrade, remove dead SERVICE_KEY code, k8s deploy	merged=2026-08-28T07:48:38Z	closed=2026-08-28T07:48:39Z	commits=3	files=16	+266/-97	## Summary - **Django 4.0.1 → 6.1** (Python 3.9 → 3.14). Removed `django-heroku` (replaced with `dj-database-url` + `whitenoise`), removed `USE_L10N` (removed upstream in Django 5.0), added `STORAGES` setting. - **Removed dead code**: the live government-API lookup path (`get_openapi_result`/`parse_openapi_result`/`SERVICE_KEY`/`API_DEV_MODE` in `main/utils.py` + `config/settings.py`, and `main/dump.py`) was never reachable — `views.py`'s only route exclusively uses the local-DB lookup (`get_similar_company_list_by_registration_from_pension_company`). Verified via repo-wide grep before deleting. - `ALLOWED_HOSTS`/`CSRF_TRUSTED_ORIGINS` updated for `paeon.xn--hy1by51c.kr` (paeon.코드.kr), replacing the dead `.herokuapp.com` entries. - New `Dockerfile` + `deploy/charts/paeon` Helm chart: Deployment (with a `migrate` initContainer), Service, Ingress, and a **monthly CronJob** running `manage.py reload_pension_company` — the government 사업장가입내역 dataset this app searches updates monthly (`자료생성년월`), so this replaces having to run that command by hand. DB moves to Postgres via a `DATABASE_URL` Secret (falls back to SQLite locally) since the reload does a full delete+bulk_create every run. ## Verified locally - `python manage.py check` — no issues - `python manage.py migrate` — applies cleanly on fresh SQLite - `python manage.py collectstatic` — whitenoise manifest builds fine - `docker build` succeeds; ran the built container and got a real `200` from `/?registration_number=...&registration_name=...` - `helm template deploy/charts/paeon` renders without error ## Security No secret values anywhere in this PR. `SECRET_KEY`/`DATABASE_URL` are only referenced as env var names / `secretKeyRef` in the Helm chart — actual values come from a `paeon-secrets` Kubernetes Secret that a separate oh-my-homelab change (SOPS-encrypted, different repo) will supply. ## Test plan - [ ] Review Django settings diff for anything upgrade-specific I missed - [ ] Confirm `paeon-secrets` Secret keys (`DATABASE_URL`, `SECRET_KEY`) match what oh-my-homelab's ArgoCD Application will provide - [ ] Merge → CI builds `ghcr.io/shinkeonkim/paeon` → oh-my-homelab ArgoCD Application picks it up 🤖 Generated with [Claude Code](https://claude.com/claude-code)
